# Code Review Report

**Issue ID:** 20260806-bug-test-plan-tc-untracked
**Date:** 2026-08-11
**Reviewer(s):** Claude

Scope: `git diff develop...HEAD` + рабочее дерево —
`skills/pf-test/SKILL.md`, `skills/pf-test-plan/SKILL.md`,
`test/pf-test-tc-mapping-static.sh`.

Шесть проходов. Четыре P0 найдены и закрыты, каждый подтверждён механически до
принятия и большинство — ещё и поведенчески, живым прогоном в изолированной
копии. Финальный проход не нашёл ни P0, ни P1, ни P2.

---

## История проходов

| Проход | Находки | Класс дефекта |
|---|---|---|
| 1 (2026-08-10) | 2 P0, 1 P1, 2 P2 | коллизия TC-ID между issue; позиционное правило, противоречащее своему же примеру |
| 2 | 1 P0, 1 P1, 3 P2 | нет правила агрегации нескольких меток одного TC-ID |
| 3 | 1 P0, 2 P1 | агрегация сделала `✓` недостижимым для `if/else`-сайтов; `✗` недостижим из-за stderr |
| 4 | 1 P0, 1 P2 | метка с интерполяцией (`$var`) не совпадает с реальным выводом никогда |
| 5 | 1 P1, 3 P2 | самопротиворечие в формулировке якорения; неточное обоснование; длинный абзац; дрейф 3.3 → 3.4 |
| 6 | — | чисто |

Проходы 1-3 — ошибки самого правила. Проход 4 — иной класс: модель верна,
несостоятельна механика сопоставления. Проход 5 — только формулировки.

---

## Findings

### P0 (Blocker)

_None._

### P1 (Important)

_None._

### P2 (Minor)

_None._

---

## Проверено, нареканий нет

**Правило якорения (§3.3).** Противоречие устранено: две свободы разведены
явно — позиция внутри метки не свободна (matchable form обязан совпадать с
началом метки), выбор строки вывода свободен. Это согласуется с реальным
форматом: `pf_pass`/`pf_fail` (`test/lib.sh:60-68`) печатают `"  PASS  %s\n"` /
`"  FAIL  %s\n"`, то есть метка не начинается с нулевой колонки, и якорение «к
началу метки, не строки» — единственно верное.

**Обоснование guard-параграфа.** Пересчитано по всем реальным сайтам с
интерполяцией: минимум — ровно 19 статических символов после TC-ID
(`test/pf-close.sh:58,73,88`). Текст цитирует именно этот сайт и его matchable
form `TC-001: PARENT-BRANCH = '` — совпадает с исходником буква в букву.

**§3.4** покрывает оба случая непопадания в маппинг (кандидатов не собрано
вовсе; кандидаты собраны, но ни один не появился) и согласован с тремя исходами
агрегации §3.3 и с предусловием Phase 5.

**Разбивка §3.3 на абзацы** не осиротила ни одной ссылки: «as just decided
above» и «the `✗` outcome below» сохранили адресатов; секционные вырезы сьюта
берутся по заголовкам `### 3.2`/`### 3.3`/`### 3.4`, а не по абзацам.

**Четыре закрытых P0 держатся текущим текстом** — проверено построчно, и
поведенчески в TC-008 round 2 (изолированная копия, две прогонки):

| Что проверено | Результат | Что было бы до фикса |
|---|---|---|
| кандидат fail-формы невыполненной ветки не появился | TC-001 → `✓` | навсегда `[ ]` |
| `step 1` PASS + `step 4` FAIL | TC-002 → `✗` | ложный `✓` |
| чужой сьют печатает `PASS TC-003 step 2/3` | фикстурный TC-003 → `[ ]` | ложный `✓` от чужой issue |
| stdout-only против объединённого потока | 0 попаданий против 1 | провал читался как «not matched» |
| префикс метки с `$var` против реальной `FAIL`-строки | совпал → `✗` | не совпадал никогда |

**Сьюты.** `test/pf-test-tc-mapping-static.sh` — 16/16, все секционные привязки
корректны после правок. `test/skills-role-matrix-static.sh` — 8/8, не затронут.

---

## Verdict

**PASS**
