# Test Plan: TC из `/pf-test-plan` не отслеживаются в `/pf-test`

**Issue:** 20260806-bug-test-plan-tc-untracked
**Тип:** bug · **Tier:** small
**Источник:** `analysis.md`

---

## Обзор и цели

`analysis.md` фиксирует три независимых дефекта связки `/pf-test-plan` →
`/pf-test` (Д1, Д2, Д3) и один производный (Д4). Цель фикса — сделать так,
чтобы любой TC, который `/pf-test-plan` пометил `Auto`, был гарантированно
найден и корректно проставлен `/pf-test`, независимо от того, на каком языке
(JS/Python или bash-конвенция этого репозитория) написан покрывающий его
тест, и независимо от того, изменён ли файл теста в текущей ветке.

Задачи этого тест-плана:

- проверить, что каждый из трёх независимых дефектов (Д1 — сканирование не
  покрывает bash-конвенцию репозитория; Д2 — незамкнутая ветка Phase 4 →
  Phase 5; Д3 — область поиска Phase 3.1 ограничена диффом ветки) закрыт
  явным, механически проверяемым правилом в тексте `pf-test/SKILL.md` (и,
  где уместно, `pf-test-plan/SKILL.md`);
- проверить, что ранее работавшие JS/Python-конвенции обнаружения TC-ID не
  сломаны фиксом (регрессия);
- проверить сцепку целиком: TC, объявленный `Auto` в `/pf-test-plan`, реально
  находится и получает статус в `/pf-test` при живом прогоне — то есть
  явление, названное в заголовке issue, устранено не только в тексте
  инструкции, но и в поведении.

Большая часть фикса — правки текста двух `SKILL.md`-файлов, поэтому
большинство TC ниже — `Auto`-проверки присутствия/формулировки конкретных
правил в этом тексте (аналог `TC-009` из `test/skills-role-matrix-static.sh`
для `pf-roles/SKILL.md`). `Manual` оставлен только для одного кейса — реального
сквозного прогона `/pf-test` в живой сессии, потому что только так можно
подтвердить, что агент действительно интерпретирует новое правило так, как
задумано, а не просто то, что правило присутствует в тексте.

## Предпосылки (Prerequisites)

- Фикс реализован: `skills/pf-test/SKILL.md` и `skills/pf-test-plan/SKILL.md`
  правлены в рамках `implementation_plan.md` этой issue.
- Для `Auto`-кейсов: доступен рабочий чекаут репозитория по пути
  `/home/stac/dev/planning-framework` (сами `Auto`-проверки — по большей
  части `grep`/`sed` по разделам `skills/pf-test/SKILL.md` и
  `skills/pf-test-plan/SKILL.md`, а также, для TC-006/TC-007, по реальным
  файлам репозитория — `tools/manual-test-ui/test/checklist-patch.test.js` и
  `git diff --name-only` над `test/`; скрипт для всех них добавляется как
  `test/pf-test-tc-mapping-static.sh` в рамках той же issue и подключается к
  `make test` — см. `Makefile`, таргет `test:`, который перебирает
  `test/*.sh`).
- Для `Manual`-кейса: фикс установлен в `~/.claude/skills/` (через
  `/pf-update` или эквивалент), доступна интерактивная сессия Claude Code в
  этом репозитории.

---

## Функциональные тесты

### TC-001: Phase 3.2 покрывает TC-ID внутри строковых литералов/аргументов

**Description:** Проверяет закрытие Д1 — раздел сканирования TC-ID в
`pf-test/SKILL.md` (Phase 3.2 или его преемник) документирует конвенцию, при
которой TC-ID находится внутри строкового литерала/аргумента, переданного
хелперу (`pf_pass "TC-009 step 1: ..."`), а не только внутри форм
`it(...)`/`describe(...)`/`test(...)` или в имени функции/метода.

**Preconditions:**
- Фикс `pf-test/SKILL.md` внесён.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Найти границы подраздела 3.2: `grep -n '^### 3.2' skills/pf-test/SKILL.md` (начало) и `grep -n '^### 3.3' skills/pf-test/SKILL.md` (конец, не включая) | Оба якоря найдены — диапазон однозначен |
| 2 | Вырезать этот диапазон (`sed -n "${start},$((end-1))p"`) и выполнить на нём `grep -qiE "string (literal\|argument)\|substring\|passed to.*helper\|pf_pass"` | Совпадение найдено — правило описывает обнаружение TC-ID не только в имени функции/it-блока, но и в строковом литерале/аргументе, переданном тестовому хелперу |
| 3 | Найти границы подраздела 3.3 тем же способом (`^### 3.3` → `^### 3.4`), вырезать и выполнить на нём `grep -qiE "substring\|same (technique\|approach\|method)\|not (limited\|restricted) to (test names\|function names)"` | Совпадение найдено — текст явно распространяет тот же приём (сканирование подстрок) на сопоставление с выводом раннера (Phase 3.3), а не оставляет его привязанным только к «именам тестов» |
| 4 | В диапазоне 3.2–3.4 проверить три независимых семантических признака правила агрегации: «отсутствие кандидата — не свидетельство провала», «появившийся провалившийся кандидат перевешивает», «различие matched/unmatched по наличию хотя бы одного появившегося» | Все три признака найдены — правило агрегации описано по существу, а не просто присутствует абзацем с заголовком |
| 5 | В диапазоне 3.2–3.3 проверить два независимых признака исключения баннера: безусловность печати и отсутствие в нём вердикта | Оба признака найдены — форма баннера (`printf '=== TC-NNN: ...'`) названа среди исключаемых, с обоснованием |
| 6 | Проверить сквозную согласованность потоков: Phase 2 требует объединённого stdout+stderr, а 3.3 ищет именно в объединённом выводе и называет stderr | Оба утверждения найдены — ветка `✗` достижима, `pf_fail` в stderr не теряется |
| 7 | В диапазоне 3.2–3.3 проверить три признака правила интерполяции: наличие переменной части метки, усечение/префиксное сопоставление по ней, guard для вырожденного префикса | Все три признака найдены — метка с подстановкой (`pf_fail "TC-NNN ... $var"`) сопоставима с реальным выводом |

**Expected Outcome:** `pf-test/SKILL.md` содержит проверяемое правило, которое
покрывает конвенцию `pf_pass "TC-NNN ..."` этого репозитория, а не только три
JS/Python-конвенции, — включая случаи, вскрытые код-ревью: несколько меток на
один TC-ID, невыполненная ветка `if/else`, `pf_fail` в stderr и подстановка
переменных внутри метки.

**Примечание о шагах 4–7.** Шаги 1–3 написаны на стадии `/pf-test-plan`.
Шаги 4–7 добавлены позже, на стадии `/pf-codereview`: каждый из четырёх
проходов ревью вскрывал дефект, который сформулированное правило допускало, и
соответствующая защита добавлялась в сьют вместе с правкой текста. Шаги
пронумерованы по фактическим ассертам `test/pf-test-tc-mapping-static.sh`,
чтобы план и сьют не разъезжались.

**Priority:** Critical

---

### TC-002: Phase 3.1 не ограничена диффом ветки

**Description:** Проверяет закрытие Д3 — область поиска тестовых файлов
больше не сводится исключительно к `git diff --name-only develop...HEAD`, так
что TC, покрытый уже существующим (неизменённым на ветке) тестом, остаётся
видимым.

**Preconditions:**
- Фикс `pf-test/SKILL.md` внесён.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Найти границы подраздела 3.1: `grep -n '^### 3.1' skills/pf-test/SKILL.md` (начало) и `grep -n '^### 3.2' skills/pf-test/SKILL.md` (конец, не включая) | Оба якоря найдены |
| 2 | Вырезать этот диапазон и выполнить `grep -qiE "existing test files\|regardless of (whether\|the branch)\|not limited to (the diff\|files changed)\|whole test suite\|entire test suite"` | Совпадение найдено — текст явно утверждает, что поиск не ограничен файлами, изменёнными на ветке |
| 3 | На том же диапазоне выполнить `grep -qiE "not the only source\|in addition to (the diff\|git diff)\|beyond (the diff\|files changed on)"` | Совпадение найдено — присутствует отдельная позитивная формулировка, что `git diff` — не единственный источник, а не подразумевается по умолчанию |

**Expected Outcome:** TC, покрытый неизменённым на ветке тестом (типичный
случай багфикса, который ничего не добавляет в `test/`), обнаруживается
Phase 3.1/3.2 наравне с TC из диффнутых файлов.

**Priority:** Critical

---

### TC-003: Ветка Phase 4 → Phase 5 замкнута явным стоп-сообщением

**Description:** Проверяет закрытие Д2 — когда Failure Gate (Phase 4) не
срабатывает (нет строк `Auto` + `✗`), но предусловие Phase 5 («все `Auto`
TC имеют статус `✓`, либо строк `Auto` нет вовсе») не выполнено из-за
оставшихся `[ ]`, `pf-test/SKILL.md` определяет явное поведение — стоп-
сообщение по аналогии с Phase 4, а не необозначенный провал в пустоту.

**Preconditions:**
- Фикс `pf-test/SKILL.md` внесён.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Найти границы «зазора» между Phase 4 и Phase 5: `grep -n "Do NOT proceed to Phase 5 when this gate triggers" skills/pf-test/SKILL.md` (начало) и `grep -n '^### 5.1' skills/pf-test/SKILL.md` (конец, не включая) | Оба якоря найдены — диапазон однозначно охватывает текст между разрешением гейта Phase 4 и первым подшагом Phase 5, включая заголовок самой Phase 5 |
| 2 | Вырезать этот диапазон и выполнить `grep -qiE "unmatched\|precondition\|left (as\|unset)"` | Совпадение найдено — ветка «остались непроставленные `Auto`-строки (`[ ]`), при этом `✗`-строк нет» названа явно, а не подразумевается через «иначе» |
| 3 | На том же диапазоне выполнить `grep -q "Stop with message"` | Совпадение найдено — для этой ветки определено собственное стоп-сообщение (в этом диапазоне стоп-сообщение Phase 4 уже не встречается — оно находится раньше начала диапазона), а не просто констатация факта без предписанного действия |

**Expected Outcome:** Для случая «нет `✗`, но остались непроставленные
`Auto`-строки» `pf-test/SKILL.md` предписывает конкретное действие
(стоп-сообщение), а не оставляет поведение агента неспецифицированным.

**Priority:** High

---

### TC-004: Правило сопоставления исключает TC-ID внутри тестовых фикстур/данных

**Description:** Проверяет находку ревью тест-плана (риск over-matching): как
только Д1 расширяет Phase 3.2/3.3 до сканирования произвольных строковых
литералов/аргументов, а Д3 снимает ограничение диффом ветки, обычные
строковые фикстуры — данные для теста, а не сам тестовый код — становятся
уязвимы к ложному совпадению. Конкретный контрпример в этом репозитории:
`tools/manual-test-ui/test/checklist-ru.test.js` проходит фильтр Phase 3.1
(путь содержит `test`, расширение `.test.js`) и содержит внутри строковых
фикстур литералы `## TC-001:` (строка 14), `## TC-002:` (строка 28) и
повторно `## TC-001:` (строка 65, `LOST_SECTION_FIXTURE`) — это данные
теста, а не тестовый код, но без явного исключения расширенное правило
Д1/Д3 способно ошибочно проставить `✓`/`✗` в Status Tracker любой
посторонней issue, чей трекер содержит строку `TC-001`.

**Preconditions:**
- Фикс `pf-test/SKILL.md` внесён (Д1 и Д3 закрыты).

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Найти границы диапазона, покрывающего Phase 3.2 и 3.3: `grep -n '^### 3.2' skills/pf-test/SKILL.md` (начало) и `grep -n '^### 3.4' skills/pf-test/SKILL.md` (конец, не включая) | Оба якоря найдены |
| 2 | Вырезать этот диапазон и выполнить на нём **два** независимых `grep -qiE` (ERE не выражает «И» одной альтернацией, поэтому оба обязательны): (a) `grep -qiE "exclud\|ignor\|does not count\|must not match"` — намерение исключения; (b) `grep -qiE "fixture\|test data\|sample data\|string constant"` — то, что именно исключается | Оба совпадения найдены — правило одновременно (a) формулирует исключение и (b) называет его предметом фикстуры/тестовые данные, а не просто упоминает слово «fixture» в другом контексте (например, при расширении сканирования на фикстуры, что было бы противоположностью нужного фикса) |

**Expected Outcome:** Расширенное правило сопоставления (Д1/Д3) не
принимает за совпадение TC-ID, найденный внутри тестовых фикстур вроде
`tools/manual-test-ui/test/checklist-ru.test.js` — контрпример остаётся
корректно неучтён.

**Priority:** High

---

### TC-005: `pf-test-plan/SKILL.md` описывает объявление, которое `pf-test` может найти

**Description:** Проверяет сторону `/pf-test-plan` связки, названной в
заголовке issue: инструкция по колонке `Type: Auto` в `pf-test-plan/SKILL.md`
(Step 4 / Status Tracker) явно увязана с тем, что `/pf-test` теперь умеет
находить — то есть авторы теста плана не остаются с прежней инструкцией
«поставь Auto, если тест автоматический» без указания, что этот тест должен
быть обнаружим механизмом Phase 3 из `pf-test`.

**Preconditions:**
- Фикс `pf-test-plan/SKILL.md` внесён.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Найти границы Step 4: `grep -n '^### Step 4' skills/pf-test-plan/SKILL.md` (начало) и `grep -n '^### Step 5' skills/pf-test-plan/SKILL.md` (конец, не включая) | Оба якоря найдены |
| 2 | Вырезать этот диапазон и выполнить `grep -qiE "findable\|discoverable\|matched by\|see (skills/)?pf-test/SKILL\.md"` | Совпадение найдено — раздел про колонку `Type: Auto` явно увязывает её с тем, что `/pf-test` способен обнаружить сканированием |

**Expected Outcome:** `pf-test-plan/SKILL.md` не оставляет читателя с
инструкцией, оторванной от того, что `pf-test` реально способен найти.

**Priority:** High

---

## Регрессия

### TC-006: JS/Python-конвенции TC-ID по-прежнему распознаются

**Description:** Проверяет, что фикс Д1 расширяет правило сканирования, а не
заменяет его — три исходные JS/Python-конвенции (TC-ID в имени функции/
`it(...)`/`describe(...)`/`test(...)`, комментарий `# TC-001`/`// TC-001`
непосредственно над тестом, заголовок describe-блока) остаются описаны в
`pf-test/SKILL.md` дословно или их описание не сузилось.

**Preconditions:**
- Фикс `pf-test/SKILL.md` внесён.
- В репозитории присутствует реальный пример этой конвенции:
  `tools/manual-test-ui/test/checklist-patch.test.js:106` —
  `test("TC-013 step 1: the existing checklist-ru.test.js passes unmodified", ...)`.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Найти границы подраздела 3.2 (`grep -n '^### 3.2' skills/pf-test/SKILL.md` → `grep -n '^### 3.3' skills/pf-test/SKILL.md`, не включая конец), вырезать диапазон и выполнить на нём `grep -qE "it\(\.\.\.|describe\(\.\.\.|test\(\.\.\."` (или аналогичный литерал, описывающий эту конвенцию) | Совпадение найдено — исходная конвенция (имя функции/it-test-описание) осталась именно в разделе сканирования TC-ID после расширения фиксом Д1, а не переехала/пропала оттуда |
| 2 | `grep -qE 'test\("TC-013 step 1' tools/manual-test-ui/test/checklist-patch.test.js` | Совпадение найдено — реальный пример этой конвенции (`test("TC-013 step 1: the existing checklist-ru.test.js passes unmodified", ...)`) по-прежнему существует в репозитории в форме, которую правило из шага 1 явно описывает |

**Expected Outcome:** Расширение правила (Д1) не сузило и не сломало
распознавание уже работавших JS/Python-конвенций.

**Priority:** High

---

### TC-007: Ни один существующий файл под `test/` не изменён фиксом

**Description:** Общая проверка «не сломали окружающее» — так как фикс этой
issue правит только `skills/pf-test/SKILL.md` и `skills/pf-test-plan/SKILL.md`
(prose-файлы, не исполняемый код), ни один уже существующий файл под `test/`
не должен быть модифицирован. Единственное допустимое изменение в `test/` —
новый файл `test/pf-test-tc-mapping-static.sh` с Auto-проверками для
TC-001..TC-007 этой же issue, включая проверку самого себя из TC-007 (по
образцу `test/skills-role-matrix-static.sh`; добавление, не правка
существующего).

**Preconditions:**
- Фикс внесён и закоммичен на ветке issue.
- `implementation_plan.md` этой issue называет новый файл проверок именно
  `test/pf-test-tc-mapping-static.sh` — если реализация выбрала другое имя,
  шаг 2 нужно поправить перед прогоном.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Выполнить `git diff --name-only --diff-filter=M develop...HEAD -- test/` | Пусто — ни один существующий файл под `test/` не модифицирован |
| 2 | Выполнить `git diff --name-only --diff-filter=A develop...HEAD -- test/` | Ровно один файл: `test/pf-test-tc-mapping-static.sh` — ничего лишнего |

**Expected Outcome:** Фикс не задевает исполняемый тестовый код репозитория —
только добавляет свою собственную проверку.

**Priority:** Medium

---

## Интеграционный / сквозной тест

### TC-008: Сквозная сцепка — Auto TC, объявленный `/pf-test-plan`, находится `/pf-test` в живом прогоне

**Description:** Основной сценарий, названный в заголовке issue: TC,
объявленный `Type: Auto` в `test_plan.md` и покрытый тестом по bash-конвенции
этого репозитория (`pf_pass "TC-NNN ..."`), должен быть найден и получить
статус `✓` при реальном прогоне `/pf-test` — не только в статическом тексте
`SKILL.md`, но и в поведении агента, интерпретирующего инструкцию. Это
поведение нельзя проверить bash-сьютом — оно требует, чтобы агент в живой
сессии применил обновлённое NL-правило к конкретным файлам.

**Preconditions:**
- Работа ведётся в отдельной временной копии/git worktree репозитория (по
  образцу изоляции `pf_repo_copy`/S-1/S-5 из `test/lib.sh`), а не в основном
  рабочем чекауте.
- В этой копии удалён remote `origin` (см. шаг 1) — без этого `/pf-test`'s
  Phase 7 (commit & push) отправит фикстурную ветку в реальный
  `git@github.com:stacmv/planning-framework.git`: push-guard в
  `~/.claude/skills/pf-git/SKILL.md` пропускает push только для веток
  `main`/`master` или при отсутствии remote, а копия наследует `origin` из
  `.git/config` и ветка фикстуры не подпадает ни под одно исключение.
- В этой копии удалена реальная issue `docs/issues/open/20260806-bug-test-plan-tc-untracked/`
  (см. шаг 2) — иначе в копии остаются две issue в `docs/issues/open/`, и
  `/pf-test` не может однозначно определить активную.
- В этой копии фикс установлен в `~/.claude/skills/pf-test/SKILL.md` и
  `~/.claude/skills/pf-test-plan/SKILL.md` (например, через `/pf-update`).
- Открыта интерактивная сессия Claude Code, указывающая на эту копию.
- Фикстура-issue `docs/issues/open/zz-fixture-tc-link/` подготовлена заранее
  внутри этой копии (см. Требуемые данные) — не настоящая issue, временная.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | В копии репозитория выполнить `git remote remove origin`, затем `git remote -v` | Вывод `git remote -v` пуст — копия больше не может запушить ничего в реальный GitHub-репозиторий |
| 2 | Удалить из копии `docs/issues/open/20260806-bug-test-plan-tc-untracked/` целиком | Директория отсутствует; в `docs/issues/open/` копии остаётся только фикстура |
| 3 | Разложить фикстуру в копии репозитория: `docs/issues/open/zz-fixture-tc-link/{prompt.md,test_plan.md,code_review.md}` и `test/zz-fixture-tc-link-check.sh` (см. Требуемые данные) | Файлы на месте; `test_plan.md` содержит одну строку Status Tracker для TC-ID `TC-001`, типа `Auto`, приоритета `High`, статус ещё не отмечен; `code_review.md` содержит `verdict: PASS`; `test/zz-fixture-tc-link-check.sh` вызывает `pf_pass "TC-001 step 1: ..."` при успехе; в этой копии `docs/issues/open/` не содержит других issue, так что активная issue определяется однозначно |
| 4 | Запустить `/pf-test` в сессии, указывающей на копию | Skill проходит Phase 1-3 без ошибок (предусловие про `code_review.md` из `pf-test/SKILL.md` удовлетворено `verdict: PASS`) |
| 5 | Открыть `docs/issues/open/zz-fixture-tc-link/test_plan.md` после прогона | Строка `TC-001` в Status Tracker сменила `[ ]` на `✓` |
| 6 | Проверить, что Phase 5 отработала (для этой фикстуры Auto-TC — единственная строка, и она `✓`) | Skill не останавливается на предусловии Phase 5; отчёт Phase 6 печатается |
| 7 | Удалить временную копию репозитория целиком | Основной рабочий чекаут и история реальной issue не затронуты |

**Требуемые данные:**
- `docs/issues/open/zz-fixture-tc-link/prompt.md`
- `docs/issues/open/zz-fixture-tc-link/test_plan.md`
- `docs/issues/open/zz-fixture-tc-link/code_review.md` — минимальный валидный
  файл с полем `verdict: PASS` (например: `# Code Review\n\nverdict: PASS`) —
  `pf-test/SKILL.md` (строка 9) отказывается запускаться без него
- `test/zz-fixture-tc-link-check.sh`

**Expected Outcome:** TC, объявленный `Auto` в `test_plan.md` по bash-
конвенции репозитория, действительно находится и корректно размечается
`/pf-test` в реальном прогоне — связка, названная в заголовке issue,
подтверждена не только текстом инструкции, но и поведением.

**Priority:** Critical

**Примечание:** `/pf-test` по дизайну не трогает строки `Type: Manual` в
Status Tracker (Phase 3.4) — эта строка (`TC-008` в *этом* документе, если
его используют для отслеживания собственного прогресса issue) остаётся
`[ ]` после прогона `/pf-test` и закрывается позже, отдельным ручным
подтверждением результата шагов 1-7 (как любой другой `Manual` TC в этом
или любом другом test_plan.md этого фреймворка) — это не дефект связки,
а стандартное поведение, не специфичное для этой issue.

---

## Status Tracker

| TC     | Test Case | Type   | Priority | Status | Remarks |
| ------ | --------- | ------ | -------- | ------ | ------- |
| TC-001 | Phase 3.2 покрывает TC-ID внутри строковых литералов/аргументов | Auto | Critical | [ ] | |
| TC-002 | Phase 3.1 не ограничена диффом ветки | Auto | Critical | [ ] | |
| TC-003 | Ветка Phase 4 → Phase 5 замкнута явным стоп-сообщением | Auto | High | [ ] | |
| TC-004 | Правило сопоставления исключает TC-ID внутри тестовых фикстур/данных | Auto | High | [ ] | |
| TC-005 | `pf-test-plan/SKILL.md` описывает объявление, которое `pf-test` может найти | Auto | High | [ ] | |
| TC-006 | JS/Python-конвенции TC-ID по-прежнему распознаются | Auto | High | [ ] | |
| TC-007 | Ни один существующий файл под `test/` не изменён фиксом | Auto | Medium | [ ] | |
| TC-008 | Сквозная сцепка: Auto TC из `/pf-test-plan` находится `/pf-test` в живом прогоне | Manual | Critical | [ ] | закрывается ручным подтверждением, не автопрогоном `/pf-test` |
