# Test Plan: Сходимость ревью и честность отчётов стадий

**Date:** 2026-08-12

## Overview

Предмет изменений этой issue — не UI и не библиотека, а **проза семи
`pf-*` skill-файлов** (`pf-codereview`, `pf-check`, `pf-execute`, `pf`,
`pf-close` — плюс справочные `pf-roles`/`pf-git`, не тронутые этой issue),
которую во время реального прогона интерпретирует LLM-агент. Бинарный
тест-раннер не может «запустить» `/pf-codereview` — он может только:

1. **Транскрибировать** механическое правило (бюджет раундов, словарь
   состояний ledger, гейт полноты плана) в bash-хелпер и прогнать его на
   фикстурах — и в том же TC, отдельным шагом, **сверить хелпер с реальным
   текстом `SKILL.md`** (drift-guard). Хелпер, согласный сам с собой,
   ничего не доказывает — доказывает только сверка с первоисточником. Этот
   урок стоил `20260806-feat-product-test-plan` живого прогона, в котором
   59 из 59 ассертов остались зелёными при живом дефекте формата, потому
   что каждый ассерт звал транскрибированный хелпер, а не текст скилла.
2. **Проверить форму, а не только присутствие** — там, где дефект является
   дефектом формы (порядок шагов, направление проверки, широта критерия),
   один `grep` на ключевое слово одинаково доволен и правильной, и
   сломанной версией текста. Ниже такие TC используют несколько независимых
   сигналов (несколько grep на разные обязательные свойства правила) или
   сравнение номеров строк, а не единственный неспецифичный grep.
3. **Честно объявить test-first drift-guard'ы красными.** Правила этой
   issue ещё не существуют в `skills/`, поэтому grep, ищущий их, обязан
   сегодня возвращать `FAIL` — это ожидаемо и диагностически ценно, а не
   брак теста. Каждый такой TC ниже прямо помечен «test-first, ожидаемо
   RED до реализации» и отдельно указывает, чем настоящий провал (правило
   не реализовано) отличается от брака тестовой инфраструктуры (фикстура
   отсутствует, `SKILL.md` не найден, скрипт хелпера падает с синтаксической
   ошибкой) — сообщение об ошибке в Auto-кейсах называет причину явно.

Живое поведение модели (реально ли ревьюер держит гейт, реально ли триаж
понижает важность находки без сценария отказа) никакой конечный тест не
доказывает с полной гарантией — это общее ограничение для каждого `Manual`
TC ниже, а не брак конкретного кейса; поэтому `Manual` зарезервирован для
того, что действительно недостижимо статическим аудитом текста или
фикстурой, и их количество сознательно минимизировано.

**Цель прогона** — подтвердить все 26 AC из `brd.md` (шесть user stories) и
акцептанс уровня issue: эквивалент `20260709-feat-dockerize` сходится за
2-3 раунда либо честно возвращается на `/pf-execute`; `PASS` никогда не
выдаётся при открытом P0/P1 или по неотревьюенному финальному диффу;
находка не исчезает молча; стадия, не сделавшая работу, не выглядит
чистой.

## Prerequisites

- Implementation по `implementation_plan.md` этой issue завершена:
  `skills/pf-codereview/SKILL.md`, `skills/pf-check/SKILL.md`,
  `skills/pf-execute/SKILL.md`, `skills/pf/SKILL.md`,
  `skills/pf-close/SKILL.md` обновлены.
- `test/lib.sh` доступен как есть — Auto-кейсы ниже следуют его конвенциям
  (`pf_setup_case`, `pf_repo_copy`, `assert_repo_untouched`,
  `HOME=$TMP_HOME`; правила безопасности S-1…S-5 в его заголовке
  load-bearing).
- **Каждый шаг каждого Auto-кейса отчитывается через `pf_pass "TC-NNN step K:
  …"` / `pf_fail "TC-NNN step K: …"`** — это единственная форма, которую
  распознаёт сканирование TC-ID в `/pf-test` (Phase 3.2, «label passed to a
  test-reporting helper»). Баннер `printf '=== TC-NNN …'` не считается меткой:
  он печатается независимо от исхода. Auto-строка Status Tracker, не давшая ни
  одной такой метки в выводе прогона, остаётся `[ ]` и **останавливает**
  `/pf-test` на его Phase 5 precondition — поэтому конвенция здесь
  предусловие, а не стилевое пожелание.
- Фикстуры под `test/fixtures/pf-codereview-convergence/…`, перечисленные
  в «Требуемые данные» каждого TC, созданы — задача реализующей стадии
  (`/pf-execute`), не этого документа.
- Для `Manual`-кейсов: рабочее дерево `D:/dev/planning-framework` чистое,
  тестировщик может создавать/удалять временные фикстур-issue под
  `docs/issues/open/zz-fixture-*/` и реально запускать `/pf-codereview`,
  `/pf-check`, `/pf-execute`, `/pf-close` в живой сессии Claude Code.
  Codex не требуется ни для одного TC ниже — все сценарии используют
  дефолтный `[claude]`-путь ревью; проверка `codex`/`both`-путей для этой
  конкретной фичи не входит в объём (роль резолвится тем же
  `pf-roles`-механизмом независимо от issue).

## Acceptance Criteria Coverage

Каждый из 26 AC покрыт минимум одним TC. Столбец «Статус текста» отмечает,
существует ли уже сегодня соответствующая формулировка в `skills/`
(`baseline` — да, TC работает как регрессионный guard) или нет
(`test-first` — TC ожидаемо RED до реализации).

| AC | TC | Статус текста |
|---|---|---|
| AC-1.1 | TC-001 | test-first |
| AC-1.2 | TC-002 | test-first |
| AC-1.3 | TC-003 | test-first |
| AC-1.4 | TC-004 | test-first |
| AC-2.1 | TC-005 | test-first |
| AC-2.2 | TC-006 | test-first |
| AC-2.3 | TC-007 | test-first |
| AC-2.4 | TC-008 | test-first |
| AC-3.1 | TC-009 | test-first |
| AC-3.2 | TC-009 | test-first |
| AC-3.3 | TC-010 | test-first |
| AC-3.4 | TC-011 | baseline (regression guard) |
| AC-3.5 | TC-012 | test-first |
| AC-4.1 | TC-013 | test-first |
| AC-4.2 | TC-014 | test-first |
| AC-4.3 | TC-015 | test-first |
| AC-4.4 | TC-016 | test-first |
| AC-4.5 | TC-017 | test-first |
| AC-5.1 | TC-018, TC-019 | test-first |
| AC-5.2 | TC-018, TC-020 | test-first |
| AC-5.3 | TC-021 | test-first |
| AC-5.4 | TC-022 | test-first |
| AC-5.5 | TC-023 | test-first |
| AC-6.1 | TC-024 | test-first |
| AC-6.2 | TC-025 | test-first (текущий текст прямо противоречит AC) |
| AC-6.3 | TC-026 | test-first |

TC-027 — сквозной интеграционный прогон, дополнительно подтверждающий
акцептанс уровня issue (п. 1-5 `brd.md`) поверх точечных TC выше; не
заменяет ни один из них.

Untestable AC: нет. Все 26 AC проверяемы либо статическим аудитом формы
текста, либо фикстурой с транскрибированным хелпером и drift-guard'ом,
либо живым прогоном.

## Test Cases

### Round budget & escalation (US-1)

### TC-001: `review_rounds` — поле в `prompt.md`, дефолт 3

**Description:** Бюджет раундов читается из `prompt.md`; при отсутствии поля используется дефолт 3, а не бесконечность и не 1.

**Preconditions:**
- Фикстуры `prompt.md` с полем `review_rounds` заданным и без него.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный хелпер `pf_cr_review_rounds(prompt.md)` читает `review_rounds: 5` из фикстуры `with-rounds-5/prompt.md`. | Возвращает `5`. |
| 2 | Тот же хелпер на фикстуре `no-rounds-field/prompt.md` (поле отсутствует). | Возвращает `3` (дефолт). |
| 3 | Drift-guard: `grep -n "review_rounds" skills/pf-codereview/SKILL.md` и рядом — дефолтное значение `3`. | Поле `review_rounds` и дефолт `3` задокументированы дословно в `pf-codereview/SKILL.md`. Test-first: сегодня этого текста нет — шаг ожидаемо RED; провал шага 3 при зелёных шагах 1-2 означает «правило не реализовано», а не брак фикстур. |

**Требуемые данные:**
- `test/fixtures/pf-codereview-convergence/with-rounds-5/prompt.md`
- `test/fixtures/pf-codereview-convergence/no-rounds-field/prompt.md`
- `skills/pf-codereview/SKILL.md`

**Expected Outcome:** Бюджет раундов резолвится из `prompt.md` с дефолтом 3, и это задокументировано в `pf-codereview/SKILL.md`.

**Priority:** High

### TC-002: Ранний бросок назад — ≥3 блокирующих находки в раунде 1

**Description:** Если раунд 1 находит три и более P0/P1, цикл исправлений не запускается — issue возвращается на `/pf-execute`, находки становятся задачами плана реализации. Меньше трёх — обычный fix-цикл.

**Preconditions:**
- Фикстуры `code_review.md`-подобных списков находок раунда 1 с 2, 3 и 4 блокирующими (P0/P1) записями.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный хелпер `pf_cr_round1_decision(findings)` на фикстуре с 2 блокирующими находками. | Возвращает `fix-cycle` (обычный ход). |
| 2 | Тот же хелпер на фикстуре с 3 блокирующими находками. | Возвращает `escalate-to-execute`. |
| 3 | Тот же хелпер на фикстуре с 4 блокирующими находками. | Возвращает `escalate-to-execute`. |
| 4 | Drift-guard: `grep` в `skills/pf-codereview/SKILL.md` на формулировку «раунд 1 … три или более … возврат на /pf-execute» (или эквивалент по смыслу) и на то, что находки становятся задачами `implementation_plan.md`. | Обе части правила задокументированы. Test-first: ожидаемо RED — сегодня Phase 4 не содержит понятия «раунд 1» вообще. |

**Требуемые данные:**
- `test/fixtures/pf-codereview-convergence/round1-2-blocking.md`
- `test/fixtures/pf-codereview-convergence/round1-3-blocking.md`
- `test/fixtures/pf-codereview-convergence/round1-4-blocking.md`
- `skills/pf-codereview/SKILL.md`

**Expected Outcome:** Порог «≥3» диагностирует «реализация не готова» и обходит fix-цикл; ниже порога — обычный цикл.

**Priority:** Critical

### TC-003: Исчерпание бюджета — возврат на `/pf-execute`, не `PASS` и не бесконечный цикл

**Description:** Когда номер раунда достиг бюджета и остаются открытые P0/P1, решение — `return-to-execute` с оставшимися находками как задачами; никогда `PASS`, никогда «ещё раунд».

**Preconditions:**
- Фикстуры состояния: раунд == бюджет, есть открытые P0/P1; раунд < бюджета, есть открытые P0/P1; раунд == бюджет, открытых P0/P1 нет.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный хелпер `pf_cr_budget_decision(round, budget, open_blocking)` на `round=3, budget=3, open_blocking=2`. | Возвращает `return-to-execute`. |
| 2 | Тот же хелпер на `round=2, budget=3, open_blocking=2`. | Возвращает `continue-fix-cycle` (бюджет ещё не исчерпан). |
| 3 | Тот же хелпер на `round=3, budget=3, open_blocking=0`. | Возвращает `pass` (чистый раунд закрывает цикл независимо от номера раунда). |
| 4 | Drift-guard: `grep` в `skills/pf-codereview/SKILL.md` на формулировку «исчерпание бюджета … возврат на /pf-execute» и на явный запрет «не PASS и не бесконечный цикл». | Обе части правила задокументированы. Test-first: ожидаемо RED — сегодняшний Phase 4 зацикливается «until verdict: PASS» без учёта бюджета вовсе. |

**Требуемые данные:**
- `test/fixtures/pf-codereview-convergence/budget-exhausted-open.md`
- `test/fixtures/pf-codereview-convergence/budget-remaining-open.md`
- `test/fixtures/pf-codereview-convergence/budget-exhausted-clean.md`
- `skills/pf-codereview/SKILL.md`

**Expected Outcome:** Бюджет — триггер эскалации, не потолок для `PASS`; исчерпание с открытыми находками не порождает ни ложный `PASS`, ни бесконечный цикл.

**Priority:** Critical

### TC-004: Раунд N (N>1) проверяет и закрытие прежних находок, и собственный дифф исправлений

**Description:** Форма инструкции для раунда 2+ обязана называть обе проверки — не только «проверь, что старое закрыто» и не только «проверь новый дифф», а обе вместе.

**Preconditions:** Нет (статический аудит текста).

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Локализовать в `skills/pf-codereview/SKILL.md` блок инструкции для раунда 2+ (Phase 4 loop / re-run Phase 2). | Блок найден. |
| 2 | Внутри этого блока — grep на упоминание проверки закрытия находок предыдущего раунда (`closure of`, `verifies… closed`, «закрыт[иы]е … прежних» или эквивалент). | Найдено. |
| 3 | В том же блоке — grep на упоминание проверки собственного диффа фикса текущего раунда («review… own diff», «дифф … исправлений» или эквивалент). | Найдено. |
| 4 | Оба сигнала должны быть найдены СОВМЕСТНО в одном и том же блоке — единственного упоминания одного из двух недостаточно (одностороннее «только верификация» ровно то, что сломало раунд 9 первого кейса `codereview-tool`). | Оба сигнала присутствуют. Test-first: ожидаемо RED — сегодняшний текст Phase 4 говорит только «re-run Phase 2», без разделения на «закрытие» и «свой дифф». |

**Требуемые данные:**
- `skills/pf-codereview/SKILL.md`

**Expected Outcome:** Инструкция раунда 2+ требует обеих проверок одновременно, а не одной вместо другой.

**Priority:** High

### Findings ledger (US-2)

### TC-005: Ledger append-only, стабильный `CR-NNN`, номер раунда, состояние

**Description:** Каждая находка получает устойчивый ID `CR-NNN`; добавление новой записи никогда не изменяет и не удаляет уже написанные строки.

**Preconditions:**
- Фикстура существующего `code_review.md`-ledger с записями `CR-001`..`CR-003` из раунда 1.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный хелпер `pf_cr_append_finding(ledger, finding)` добавляет находку раунда 2 к фикстуре. | Возвращает файл с новой строкой `CR-004`, раунд `2`. |
| 2 | Сравнить содержимое строк `CR-001`..`CR-003` до и после вызова (побайтово). | Идентичны — append-only соблюдён. |
| 3 | Новая строка содержит все три поля: ID (`CR-NNN`), номер раунда, состояние. | Все три поля присутствуют и непусты. |
| 4 | Drift-guard: `grep` в `skills/pf-codereview/SKILL.md` на формат `CR-NNN` и на слово «append-only» / «только дополняется» рядом с описанием `code_review.md`. | Формат и правило задокументированы. Test-first: ожидаемо RED — сегодняшний `code_review.md` не имеет ни стабильных ID находок, ни правила append-only (Findings просто перезаписываются список-блоками P0/P1/P2 при каждом Phase 3). |

**Требуемые данные:**
- `test/fixtures/pf-codereview-convergence/ledger-round1-3-entries.md`

**Expected Outcome:** Ledger растёт только добавлением; прежние записи неприкосновенны; новая запись несёт ID/раунд/состояние.

**Priority:** Critical

### TC-006: Закрытый словарь состояний находки

**Description:** Состояние находки — одно из ровно пяти значений: `fixed`, `accepted-risk`, `deferred`, `wont-fix`, `duplicate-of CR-NNN`. Шестое, придуманное значение — отклоняется.

**Preconditions:** Нет.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный валидатор `pf_cr_valid_state(value)` на каждом из пяти легальных значений (для `duplicate-of` — с конкретным `CR-NNN`, например `duplicate-of CR-002`). | Все пять приняты. |
| 2 | Тот же валидатор на придуманном значении `wontfix-later` (не входит в словарь). | Отклонено. |
| 3 | Drift-guard: `grep` в `skills/pf-codereview/SKILL.md` находит все пять литералов словаря рядом друг с другом (`fixed`, `accepted-risk`, `deferred`, `wont-fix`, `duplicate-of`). | Все пять присутствуют вместе, ни одно не подменено синонимом. Test-first: ожидаемо RED — понятие «состояние находки» сегодня в `pf-codereview/SKILL.md` отсутствует вовсе. |

**Требуемые данные:**
- (нет — валидатор проверяется на строковых литералах внутри теста)
- `skills/pf-codereview/SKILL.md`

**Expected Outcome:** Словарь состояний закрыт и зафиксирован дословно; произвольное значение не проходит.

**Priority:** High

### TC-007: Промпт ревьюера раунда N несёт список разобранного и инструкцию не повторять

**Description:** Диспетчеризация ревьюера для раунда 2+ (и Claude-путь, и Codex-путь) обязана включать ledger предыдущих раундов с явной инструкцией «это уже разобрано, не повторяй; ищи новое, внесённое фиксами».

**Preconditions:** Нет (статический аудит промпт-шаблонов).

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Локализовать в `skills/pf-codereview/SKILL.md` блок диспетчеризации ревьюера (`### Claude review path` и `### Codex path`), в применении к раунду 2+ (Phase 4 loop). | Блоки найдены. |
| 2 | Grep внутри этих блоков (в контексте повторного вызова) на упоминание передачи ledger/предыдущих находок ревьюеру. | Найдено. |
| 3 | Grep на инструкцию «не повторяй» + «ищи новое» как два независимых сигнала (не одна фраза, вмещающая оба смысла случайно). | Оба сигнала присутствуют. Test-first: ожидаемо RED — сегодняшний повторный вызов («re-run Phase 2») не передаёт ревьюеру ничего, кроме заново вычисленного диффа. |

**Требуемые данные:**
- `skills/pf-codereview/SKILL.md`

**Expected Outcome:** Ревьюер каждого раунда после первого явно получает историю разобранного и инструкцию искать новое, не повторяя старое.

**Priority:** High

### TC-008: На выходе `PASS` каждая незакрытая находка имеет явное состояние

**Description:** Валидатор ledger на выходе `PASS` обязан находить хотя бы одно значение State у каждой строки; пустая ячейка состояния — нарушение, а не тихий пропуск (случай «P2 из раунда 1» первого кейса).

**Preconditions:**
- Фикстура ledger с одной строкой без значения State.
- Фикстура ledger, где у всех строк State заполнено.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный валидатор `pf_cr_validate_pass_ledger(ledger)` на фикстуре с полностью заполненным State. | Возвращает 0 нарушений. |
| 2 | Тот же валидатор на фикстуре с одной пустой ячейкой State. | Возвращает ≥1 нарушение, называя конкретный `CR-NNN` с пустым State (не общее «invalid ledger»). |
| 3 | Drift-guard: `grep` в `skills/pf-codereview/SKILL.md` на формулировку «отсутствие записи запрещено» / «каждая незакрытая находка имеет состояние» рядом с условием выдачи `PASS`. | Правило задокументировано. Test-first: ожидаемо RED. |

**Требуемые данные:**
- `test/fixtures/pf-codereview-convergence/ledger-all-states-set.md`
- `test/fixtures/pf-codereview-convergence/ledger-missing-state.md`
- `skills/pf-codereview/SKILL.md`

**Expected Outcome:** `PASS` не выдаётся (в терминах валидатора — ledger не проходит проверку) при хотя бы одной находке без явного состояния.

**Priority:** Critical

### Blocking rubric & triage (US-3)

### TC-009: Находка без сценария отказа понижается в важности, единая ось важности сохраняется

**Description:** Живой прогон: находка, сформулированная без конкретного сценария отказа (вход/состояние → неверный вывод/креш/регрессия/нарушение требования), переводится в P2 переклассификацией важности — без введения второго, параллельного признака «блокирует».

**Preconditions:**
- Фикстур-issue с диффом, содержащим один реальный баг (даёт валидный P0/P1 со сценарием отказа) и одно чисто стилистическое замечание, которое ревьюер может сформулировать без сценария отказа.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Запустить `/pf-codereview` на фикстур-issue. | Ревью производит находки по обоим местам диффа. |
| 2 | Прочитать находку по стилистическому месту. | Находка либо изначально не несёт сценария отказа, либо сформулирована ревьюером без него. |
| 3 | Прочитать её итоговую важность в `code_review.md`. | Находка имеет важность P2 (переклассифицирована), не отдельный флаг «non-blocking» рядом с исходной P0/P1. |
| 4 | Прочитать находку по реальному багу. | Сохраняет P0/P1 (сценарий отказа присутствует), участвует в гейте `PASS`/`FAIL` как блокирующая. |
| 5 | Проверить `code_review.md` целиком на отсутствие второго признака важности/блокировки помимо приоритета P0/P1/P2. | Не найдено ни одного дополнительного поля вроде `blocking: yes/no` рядом с приоритетом — единственная ось важности управляет гейтом. |

**Требуемые данные:**
- `docs/issues/open/zz-fixture-triage-severity/prompt.md` (временная фикстура: `size_tier: small`, минимальный `implementation_plan.md`/`test_plan.md`)
- `docs/issues/open/zz-fixture-triage-severity/` — issue branch с диффом, содержащим один реальный баг и одно стилистическое замечание (готовится тестировщиком по описанию выше)

**Expected Outcome:** Находка без сценария отказа не блокирует гейт; находка со сценарием — блокирует; признак важности остаётся единственным.

**Priority:** Critical

### TC-010: Переклассификация — явный шаг, причина сохраняется, видна в отчёте

**Description:** Форма отчёта обязана показывать переклассификацию как отдельно видимое действие с причиной — не молчаливую замену итоговой важности.

**Preconditions:** Нет (статический аудит текста `pf-codereview`; TC-009 подтверждает поведение живьём, этот TC — что оно задокументировано и наблюдаемо в отчёте).

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Grep в `skills/pf-codereview/SKILL.md` на явный шаг переклассификации (не просто «расставь приоритеты», а отдельно названный шаг «reclassify… no failure scenario → lower priority» или эквивалент). | Найден отдельный шаг. |
| 2 | Grep рядом на требование сохранять причину решения по каждой переклассифицированной находке. | Найдено. |
| 3 | Grep на требование, что переклассификация видна в `code_review.md` (не только применяется «за кадром»). | Найдено. Все три сигнала должны присутствовать вместе — присутствие только «расставь приоритеты» без слов «шаг», «причина», «виден в отчёте» не защитывается. Test-first: ожидаемо RED. |

**Требуемые данные:**
- `skills/pf-codereview/SKILL.md`

**Expected Outcome:** Переклассификация — явный, обоснованный и видимый шаг, а не невидимая корректировка числа.

**Priority:** Medium

### TC-011: Открытый P0 никогда не даёт `PASS`, независимо от номера раунда (regression guard)

**Description:** Существующее правило Phase 3 («любой открытый P0/P1 → `FAIL`») уже верно сегодня и обязано остаться верным после введения бюджета раундов — номер раунда не должен становиться дополнительным условием, разрешающим `PASS` при открытом P0.

**Preconditions:**
- Фикстуры ledger: открытый P0 на раунде 1; открытый P0 на раунде N (например 7, за пределами обычного бюджета).

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный гейт `pf_cr_gate_verdict(ledger)` на фикстуре с открытым P0, раунд=1. | Возвращает `FAIL`. |
| 2 | Тот же гейт на фикстуре с открытым P0, раунд=7 (за пределами обычного бюджета в 3). | Возвращает `FAIL` — номер раунда не участвует в решении. |
| 3 | Drift-guard: `grep` в `skills/pf-codereview/SKILL.md` Phase 3, что условие `FAIL` — «любой открытый P0/P1», без исключения по номеру раунда или по исчерпанию бюджета. | Условие сформулировано без обхода по раунду. Baseline: этот текст существует уже сегодня («If any P0 or P1 finding is open: write FAIL») — ожидается GREEN уже сейчас; регрессия возможна только если реализация бюджета раундов (TC-002/TC-003) по ошибке припишет исключение. |

**Требуемые данные:**
- `test/fixtures/pf-codereview-convergence/ledger-open-p0-round1.md`
- `test/fixtures/pf-codereview-convergence/ledger-open-p0-round7.md`
- `skills/pf-codereview/SKILL.md`

**Expected Outcome:** Открытый P0 блокирует `PASS` при любом номере раунда; бюджет раундов не становится обходным путём.

**Priority:** Critical

### TC-012: Вынос в follow-up issue — доступен для P1, недоступен для P0

**Description:** Опция «вынести замечание в отдельную issue, чтобы пройти гейт» — валидна только для средней важности (P1); для P0 такой опции не существует ни при каких условиях.

**Preconditions:**
- Фикстуры попытки применить `resolved-via-followup-issue` к находке P0 и к находке P1.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный валидатор `pf_cr_followup_allowed(priority)` на `P1`. | Возвращает `allowed`. |
| 2 | Тот же валидатор на `P0`. | Возвращает `rejected`. |
| 3 | Drift-guard: `grep` в `skills/pf-codereview/SKILL.md` на две раздельные формулировки — куда попадают остатки на выходе `PASS` (P2/deferred → `tech-debt.md`) и что доступно как способ пройти гейт (follow-up issue → только P1) — по BR-5 они не должны быть слиты в одну. | Оба механизма названы раздельно, follow-up issue привязан именно к P1, не к P0 и не ко всем остаткам целиком. Test-first: ожидаемо RED — у Phase 4 сегодня всего два варианта гейта («Fix now» / «I'll fix manually»), опции follow-up issue нет вовсе. |

**Требуемые данные:**
- (нет — валидатор проверяется на строковых литералах приоритета)
- `skills/pf-codereview/SKILL.md`

**Expected Outcome:** Follow-up issue — легальный путь прохождения гейта только для P1; для P0 такого пути не существует.

**Priority:** High

### Execute completeness gate (US-4)

### TC-013: Гейт полноты — все пункты плана реализации отмечены выполненными

**Description:** Перед передачей в ревью механически проверяется, что в `implementation_plan.md` не осталось `- [ ]` (невыполненных) пунктов.

**Preconditions:**
- Фикстуры `implementation_plan.md`: все пункты `- [x]`; один пункт остался `- [ ]`.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный хелпер `pf_execute_all_tasks_checked(plan.md)` на фикстуре «все выполнены». | Возвращает `true` (проверка пройдена). |
| 2 | Тот же хелпер на фикстуре с одним невыполненным пунктом. | Возвращает `false`, называя конкретный пункт. |
| 3 | Drift-guard: `grep` в `skills/pf-execute/SKILL.md` на механическую проверку чекбоксов плана перед передачей в ревью, с явным словом «блокирует»/«blocking» рядом. | Правило задокументировано и помечено блокирующим. Test-first: ожидаемо RED — сегодня `pf-execute` не содержит гейта полноты перед передачей в `/pf-codereview` вовсе. |

**Требуемые данные:**
- `test/fixtures/pf-codereview-convergence/impl-plan-all-checked.md`
- `test/fixtures/pf-codereview-convergence/impl-plan-one-unchecked.md`
- `skills/pf-execute/SKILL.md`

**Expected Outcome:** Незавершённый план не проходит гейт полноты и не передаётся в ревью.

**Priority:** High

### TC-014: Гейт полноты — у каждого заявленного поведения есть тест (прямое направление)

**Description:** Именно то направление, что поймало бы раунды 5 и 8 первого кейса: для каждой задачи `implementation_plan.md` должен существовать хотя бы один сопоставленный TC из `test_plan.md`. Это НЕ обратная проверка (та — TC-015) — BRD-ревью этой самой issue уже поймало P0 на перепутанном направлении, поэтому drift-guard ниже сверяет направление явно, а не факт наличия правила вообще.

**Preconditions:**
- Пара фикстур `implementation_plan.md` + `test_plan.md`: (a) у каждой задачи есть мапленный TC; (b) одна задача не имеет ни одного мапленного TC.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный хелпер `pf_execute_task_has_test(plan.md, test_plan.md)` на фикстуре (a). | Возвращает `true` для каждой задачи. |
| 2 | Тот же хелпер на фикстуре (b), где задача «Task 3» не упомянута ни в одном TC. | Возвращает `false` для `Task 3`, называя её явно. |
| 3 | Drift-guard, чувствительный к направлению: `grep` в `skills/pf-execute/SKILL.md` (или `pf-codereview/SKILL.md`, где реально размещён гейт) на формулировку именно «у каждой ЗАДАЧИ/ПОВЕДЕНИЯ есть тест», а НЕ на формулировку «каждый TC сопоставлен задаче» (это TC-015; присутствие только обратной формулировки не защитывает этот шаг). | Найдена формулировка прямого направления отдельно от обратного. Test-first: ожидаемо RED. |

**Требуемые данные:**
- `test/fixtures/pf-codereview-convergence/coverage-forward-complete/implementation_plan.md`
- `test/fixtures/pf-codereview-convergence/coverage-forward-complete/test_plan.md`
- `test/fixtures/pf-codereview-convergence/coverage-forward-gap/implementation_plan.md`
- `test/fixtures/pf-codereview-convergence/coverage-forward-gap/test_plan.md`
- `skills/pf-execute/SKILL.md`

**Expected Outcome:** Задача без единого сопоставленного теста обнаруживается механически и блокирует передачу в ревью.

**Priority:** Critical

### TC-015: Гейт полноты — каждый TC плана тестирования сопоставлен задаче (обратное направление)

**Description:** Дополняющая, а не дублирующая TC-014 проверка: требование из `test_plan.md`, которое никто не взялся реализовать, обнаруживается отдельно.

**Preconditions:**
- Пара фикстур: (a) каждый TC сопоставлен хотя бы одной задаче; (b) один TC не сопоставлен ни одной задаче.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированный хелпер `pf_execute_tc_has_task(test_plan.md, plan.md)` на фикстуре (a). | Возвращает `true` для каждого TC. |
| 2 | Тот же хелпер на фикстуре (b), где `TC-004` не упомянут ни в одной задаче. | Возвращает `false` для `TC-004`. |
| 3 | Drift-guard: `grep` находит формулировку обратного направления («каждый тест-кейс … сопоставлен … задаче») как отдельную от прямой (TC-014), с явной пометкой, что она не заменяет прямую проверку. | Обе формулировки присутствуют раздельно. Test-first: ожидаемо RED. |

**Требуемые данные:**
- `test/fixtures/pf-codereview-convergence/coverage-converse-complete/implementation_plan.md`
- `test/fixtures/pf-codereview-convergence/coverage-converse-complete/test_plan.md`
- `test/fixtures/pf-codereview-convergence/coverage-converse-gap/implementation_plan.md`
- `test/fixtures/pf-codereview-convergence/coverage-converse-gap/test_plan.md`
- `skills/pf-execute/SKILL.md`

**Expected Outcome:** Требование теста без реализующей задачи обнаруживается механически, отдельно от проверки TC-014.

**Priority:** High

### TC-016: Все три проверки полноты блокирующие, не предупреждения

**Description:** Форма текста обязана явно квалифицировать чекбокс-проверку, прямую и обратную проверки покрытия как блокирующие («вывод 4 первого кейса» — неблокирующий гейт не удержал).

**Preconditions:** Нет.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Локализовать в `skills/pf-execute/SKILL.md` (или там, где реально размещён гейт) блок, вводящий все три проверки TC-013/014/015. | Блок найден. |
| 2 | Для каждой из трёх проверок — grep на слово, квалифицирующее её как блокирующую («blocking», «блокирует», «останавливает выполнение»), а не как «warning»/«предупреждение»/«note». | Все три явно квалифицированы блокирующими. |
| 3 | Убедиться, что рядом отсутствует формулировка вида «достаточно предупреждения» — если такая есть, TC проваливается независимо от шага 2. | Не найдено. Test-first: ожидаемо RED. |

**Требуемые данные:**
- `skills/pf-execute/SKILL.md`

**Expected Outcome:** Ни одна из трёх проверок не выражена как необязательное предупреждение.

**Priority:** High

### TC-017: Проверки полноты — механические, не опираются на суждение модели

**Description:** Формулировка гейта не должна оставлять решение на усмотрение ревьюера/агента («реши, достаточно ли тестов») — только детерминированный парсинг чекбоксов и таблиц сопоставления.

**Preconditions:** Нет.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Локализовать блок с тремя проверками полноты. | Блок найден. |
| 2 | Grep на слово «механически»/«mechanical»/«deterministic» рядом с описанием всех трёх проверок. | Найдено. |
| 3 | Убедиться в ОТСУТСТВИИ формулировок, перекладывающих решение на суждение («judge», «decide whether», «на усмотрение», «по вашему мнению») в этом же блоке. | Не найдено — присутствие такой фразы проваливает TC независимо от шага 2. Test-first: ожидаемо RED (блока пока нет вовсе). |

**Требуемые данные:**
- `skills/pf-execute/SKILL.md`

**Expected Outcome:** Гейт нельзя «уговорить» — он либо парсит и находит несоответствие механически, либо нет; решения ревьюера не участвуют.

**Priority:** Medium

### Honest stage reporting (US-5)

### TC-018: Реальные git-топологии — пустой дифф и документ вне ветки issue воспроизводимы механически

**Description:** Инфраструктурный TC, доказывающий, что оба сценария из BRD (US-5) — не гипотеза, а реально строящиеся git-состояния, на которых TC-019/TC-020 могут быть воспроизведены. Не транскрибирует правило `pf-codereview`/`pf-check` — только строит и измеряет реальную git-топологию (`pf_repo_copy`-подобный throwaway-репозиторий).

**Preconditions:** Нет — TC сам строит throwaway git-репозитории через `pf_setup_case … --git`.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | В throwaway-репозитории создать ветку `develop` и ветку `issue/zz-empty-diff` из той же точки, не коммитить в неё ничего. | `git diff develop...issue/zz-empty-diff --name-only` возвращает пусто. |
| 2 | В другом throwaway-репозитории закоммитить документ issue (`docs/issues/open/zz-off-branch/prompt.md`) прямо в `develop`, ветку `issue/zz-off-branch` не создавать вовсе. | `git rev-parse --verify issue/zz-off-branch` завершается ошибкой (ветки не существует) — ровно повторяет реальное измерение из `session-log.md` этой issue (`git diff --name-only develop...HEAD` → 0 файлов при работе над `brd.md`). |
| 3 | Зафиксировать оба измерения как известные точные предусловия для TC-019/TC-020. | Оба состояния воспроизведены детерминированно, без участия LLM. |

**Требуемые данные:**
- (фикстура не нужна — throwaway-репозитории строятся самим тестом через `pf_setup_case ... --git`)

**Expected Outcome:** Оба граничных случая (пустой дифф на ветке, документ вовсе вне ветки issue) реально строятся и измеряются, не являются умозрительными.

**Priority:** Medium

### TC-019: Пустой дифф — ошибка стадии, а не чистый результат

**Description:** Живой прогон `/pf-codereview` (или `/pf-check`) на issue-ветке без единого коммита поверх родителя обязан сообщить об ОШИБКЕ стадии («нечего ревьюить»), а не выдать «замечаний нет» / чистый `PASS`.

**Preconditions:**
- Фикстур-issue доведена до `implementation_plan.md`, ветка `issue/zz-fixture-empty-diff` создана командой `/pf-execute`, но остаётся идентичной родителю (см. TC-018 шаг 1).

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Убедиться, что `git diff develop...HEAD` на ветке issue пуст. | Подтверждено (TC-018 воспроизводит это механически). |
| 2 | Запустить `/pf-codereview` на этой ветке. | Стадия сообщает об ОШИБКЕ («нечего ревьюить» / «empty review target»), НЕ пишет `code_review.md` с `verdict: PASS` и пустыми списками находок. |
| 3 | Прочитать итоговое сообщение. | Явно отличает «нечего ревьюить» от «отревьюено, чисто» — форма сообщения не совпадает с обычным чистым `PASS`. |

**Требуемые данные:**
- `docs/issues/open/zz-fixture-empty-diff/prompt.md` (временная фикстура: `size_tier: small`, доведена до `implementation_plan.md`, ветка issue создана, но без единого коммита с изменением кода)

**Expected Outcome:** Пустой предмет ревью не проходит как чистый результат — стадия останавливается с явной ошибкой.

**Priority:** Critical

### TC-020: Документ вне ветки issue — ревью по пути, с явным указанием в отчёте

**Description:** Живой прогон `/pf-check` на документ, лежащий в `develop` (ветки issue ещё нет — ровно ситуация, реально произошедшая в этой самой issue при ревью `brd.md`), обязан ревьюить документ по его пути и явно сообщить причину (ветки нет), а не молча вернуть «замечаний нет» по пустому диффу.

**Preconditions:**
- Фикстур-issue с `brd.md`, закоммиченным прямо в `develop` (ветки `issue/…` не существует — см. TC-018 шаг 2).

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Убедиться, что ветки `issue/zz-fixture-off-branch` не существует. | Подтверждено. |
| 2 | Запустить `/pf-check` на `brd.md` этой фикстур-issue. | Скилл обнаруживает отсутствие ветки issue, переключается на ревью по пути документа (не пытается вычислить `git diff` против несуществующей ветки как «пусто = чисто»). |
| 3 | Прочитать отчёт ревью. | Явно указано, что ревью выполнено по пути документа, а не по дифф-ветке, и названа причина (ветки issue не существует). |

**Требуемые данные:**
- `docs/issues/open/zz-fixture-off-branch/prompt.md` (временная фикстура)
- `docs/issues/open/zz-fixture-off-branch/brd.md` (закоммичен прямо в `develop`, без ветки issue)

**Expected Outcome:** Отсутствие ветки issue не превращается в «пустой дифф = чисто»; ревью переключается на путь документа и сообщает об этом явно.

**Priority:** High

### TC-021: Явный признак пройденной проверки, не выводимый из готовности следующего документа

**Description:** `/pf` обязан различать «check не запускали» и «check запускали и прошли» через явную запись (маркер/строка `session-log.md`), а не через круговое определение «следующий документ готов → check прошёл».

**Preconditions:** Нет (статический аудит `pf/SKILL.md` — воспроизводит дефект item 6 `prompt.md`: `/pf-check` уже прогоняли, `/pf` этого не увидел, потому что определял «check passed» через готовность следующего документа).

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Локализовать в `skills/pf/SKILL.md` определение «Note on 'check passed'». | Найдено. |
| 2 | Проверить, что определение ссылается на явную запись (маркер в `session-log.md` issue, либо файл-маркер) как источник истины. | Найдено. |
| 3 | Убедиться, что круговая формулировка «check passed = готовность следующего документа» удалена или не является единственным критерием. | Циклическое определение как единственный критерий отсутствует. Test-first: ожидаемо RED — сегодняшний текст буквально: «A check counts as passed only when… the next document in sequence is complete as well», без какого-либо независимого маркера. |

**Требуемые данные:**
- `skills/pf/SKILL.md`

**Expected Outcome:** «Check прошёл» — самостоятельный, явно записываемый факт, не выводимый из состояния соседнего документа.

**Priority:** High

### TC-022: `/pf-close` — ни одна стадия не завершается частично без явного отчёта

**Description:** Статический аудит: каждая пронумерованная фаза `pf-close/SKILL.md` либо ведёт к следующей фазе, либо явно останавливается с сообщением, называющим шаг остановки — воспроизводит дефект item 8 (`/pf-close` реально останавливался после Phase 4 на `20260731-feat-pluggable-reviewers`, оставляя issue в `open/` без единой записи).

**Preconditions:** Нет.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Извлечь номера строк всех заголовков `## Phase N` в `skills/pf-close/SKILL.md` (техника анкеров, как в `check_order` из `test/skills-role-matrix-static.sh`). | Список фаз получен по порядку. |
| 2 | Для каждой фазы (кроме последней, Phase 9 «Report») — в её текстовом блоке искать либо явный переход к следующей фазе, либо конструкцию остановки («stop:», «Merge conflict detected», «Stop and surface» и т.п.). | Каждая фаза имеет один из двух путей — ни одна не заканчивается без явного продолжения или явной остановки. |
| 3 | Отдельно проверить Phase 4.5 (уже сегодня содержит «Recovering from a failure inside this phase» — baseline) и остальные фазы (test-first — цель этой issue расширить требование дальше Phase 4.5). | Phase 4.5 — GREEN уже сегодня; полное покрытие всех фаз — ожидаемо RED до реализации, если реализация не расширяет это правило на каждую фазу. |

**Требуемые данные:**
- `skills/pf-close/SKILL.md`

**Expected Outcome:** Ни одна фаза `/pf-close` не может «повиснуть» без явного сообщения о том, где она остановилась.

**Priority:** Critical

### TC-023: Частичная остановка оставляет issue пригодной к повторному запуску с описанным путём

**Description:** Одного сообщения «остановился на шаге N» недостаточно (AC-5.5) — рядом обязан быть конкретный, выполнимый путь доведения до конца, а не отсылка «разберитесь вручную».

**Preconditions:** Нет.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Для каждого сообщения об остановке, найденного в TC-022 (шаг 2), проверить наличие рядом конкретных дальнейших действий (команда для повтора, шаг ручного вмешательства, ссылка на процедуру восстановления). | У каждого сообщения об остановке есть такой путь. |
| 2 | Отдельно — Phase 4.5's «Recovering from a failure inside this phase» как эталон формы (пронумерованные шаги 1-3, конкретные команды `git checkout --`, `rm`, `git checkout issue/ISSUE-ID`). | Эталон присутствует (baseline). |
| 3 | Убедиться, что путь восстановления не сводится к фразе «resolve manually» без конкретики хотя бы в одном месте, найденном в TC-022 шаге 2. | Не найдено голой отсылки без конкретных шагов. Test-first для фаз за пределами Phase 4.5 — ожидаемо RED, если реализация не распространяет ту же полноту описания на другие точки остановки (например Phase 4's «Merge conflict detected» сегодня не описывает шаги разрешения так же подробно, как Phase 4.5). |

**Требуемые данные:**
- `skills/pf-close/SKILL.md`

**Expected Outcome:** Каждая точка остановки оставляет читателю конкретный путь до завершения, не просто факт остановки.

**Priority:** Medium

### Self-contained task dispatch (US-6)

### TC-024: Задание сабагенту перечисляет минимальный самодостаточный состав

**Description:** Инструкция диспетчеризации задачи (`pf-execute`, «For Each Task») обязана явно перечислять пять элементов: что делать, какие файлы затронуть, какой результат считается достигнутым, какие TC проверяют работу, от чего задание зависит.

**Preconditions:** Нет.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Локализовать в `skills/pf-execute/SKILL.md` блок «Create each task with:» (Phase 1). | Блок найден — уже сегодня перечисляет часть состава («Clear description including… files», «Mapped test cases», `blocked_by`/`blocks`). |
| 2 | Проверить наличие всех пяти элементов явно: (а) что делать, (б) файлы, (в) критерий достигнутого результата, (г) мапленные TC, (д) зависимости. | Baseline: (а),(б),(г),(д) уже присутствуют сегодня; (в) «какой результат считается достигнутым» как отдельный явный критерий — отсутствует (сегодня есть только «Implement functionality to pass mapped TCs» внутри инструкций сабагенту, не как критерий на уровне задания). |
| 3 | Убедиться, что формулировка не сводится к «достаточное описание» без перечисления — именно эта фраза была признана непроверяемой при ревью `brd.md` (AC-6.1). | Формулировки «достаточное описание» / «sufficient description» без перечня нет. Test-first по пункту (в); частично baseline по (а)(б)(г)(д). |

**Требуемые данные:**
- `skills/pf-execute/SKILL.md`

**Expected Outcome:** Задание сабагенту перечисляет весь пятиэлементный минимум явно, включая критерий «результат достигнут», а не только четыре из пяти.

**Priority:** High

### TC-025: Задание не предписывает действий, требующих недоступных инструментов (`TaskGet`/`TaskUpdate`)

**Description:** Подтверждённый на практике факт (item 7, `prompt.md`): сабагенты `write: claude` не имеют доступа к `TaskGet`/`TaskUpdate`. Инструкция не должна больше предписывать их вызов как первый/последний шаг сабагента.

**Preconditions:** Нет.

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Локализовать в `skills/pf-execute/SKILL.md` блок «For Each Task — write: claude (Sub-Agent Instructions)». | Блок найден. |
| 2 | Grep внутри блока на `TaskGet`/`TaskUpdate` как предписываемые сабагенту действия. | **Известное текущее состояние (baseline-нарушение):** блок сегодня буквально предписывает «Use TaskGet to read full task details» первым шагом и «Use TaskUpdate to mark task complete» пятым — оба инструмента подтверждённо недоступны сабагенту. Это прямое противоречие AC-6.2, зафиксированное как текущий факт, а не гипотеза. |
| 3 | После реализации: тот же grep не должен находить предписание сабагенту вызывать эти два инструмента (вместо этого — самодостаточное описание задачи и отметка оркестратором, как уже сделано для пути `write != claude`). | Ожидается отсутствие предписания. Test-first, явно инвертированный: сегодня шаг 2 находит нарушение (что и делает TC ценным уже сейчас, как живой baseline-defect-guard), после реализации шаг 3 должен перестать его находить. Провал шага 3 после реализации — это «правило не реализовано»; отсутствие файла `skills/pf-execute/SKILL.md` или ошибка grep — это брак тестовой инфраструктуры, а не то же самое. |

**Требуемые данные:**
- `skills/pf-execute/SKILL.md`

**Expected Outcome:** Инструкция для `write: claude`-сабагента перестаёт предписывать вызов инструментов, подтверждённо ему недоступных.

**Priority:** Critical

### TC-026: Расхождение нумерации оркестратор/план не может привести к выполнению другой задачи

**Description:** Воспроизводит item 7 (второй абзац): сабагент, лишённый `TaskGet`, ушёл читать план и взял задачу «по номеру», а нумерация оркестратора не совпадала с нумерацией плана. Задание обязано содержать полное содержание задачи, а не только номер, чтобы такая путаница была структурно невозможна.

**Preconditions:**
- Фикстура `implementation_plan.md`, где внутренняя нумерация задач («Task 3») не совпадает с порядком, в котором оркестратор перечисляет их при диспетчеризации (например план нумерует по разделам, оркестратор — по порядку создания).

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Транскрибированная функция `pf_execute_resolve_task_content(dispatch_payload)` вызывается с payload, содержащим ТОЛЬКО номер задачи (`"Task 3"`), без текста задачи. | Возвращает ошибку/пусто — по одному номеру без содержания задача не резолвится (это и есть уязвимость item 7). |
| 2 | Та же функция вызывается с payload, содержащим номер И полное содержание задачи (описание, файлы, TC), даже если номер задачи в payload не совпадает с фактическим номером в `implementation_plan.md`. | Возвращает содержание задачи как есть (по встроенному описанию, не по номеру) — расхождение номеров не влияет на результат. |
| 3 | Drift-guard: `grep` в `skills/pf-execute/SKILL.md`, что задание сабагенту всегда включает полное содержание задачи, а не только идентификатор/номер, с явной формулировкой на случай расхождения нумерации. | Найдено. Test-first: ожидаемо RED — сегодняшний текст не содержит явного правила именно про несовпадение нумерации оркестратор/план (хотя описание файлов уже присутствует, гарантии на случай расхождения номеров нет). |

**Требуемые данные:**
- `test/fixtures/pf-codereview-convergence/impl-plan-mismatched-numbering.md`

**Expected Outcome:** Задание самодостаточно по содержанию; несовпадение нумерации не может привести к выполнению не той задачи.

**Priority:** High

### Integration

### TC-027: Сквозной прогон, эквивалентный `20260709-feat-dockerize` — сходимость за 2-3 раунда либо честный возврат

**Description:** Единственный TC, реально управляющий циклом ревью от начала до конца — фикстура-issue с реализацией, намеренно содержащей несколько блокирующих дефектов (аналог первых двух раундов первого кейса: 6+6 находок), прогоняется через реальные `/pf-execute` → `/pf-codereview` (несколько раундов) и подтверждает: ранний бросок назад ИЛИ сходимость в пределах бюджета; ledger переживает раунды с устойчивыми ID; `PASS` не выдаётся при открытой находке или по неотревьюенному финальному диффу. Это акцептанс уровня issue (`brd.md`, пп. 1-5), не заменяющий точечные TC-001..026 — при провале интеграционного теста именно они локализуют, какое конкретное правило сломано.

**Preconditions:**
- Фикстур-issue с `implementation_plan.md` из 3-4 задач, доведена до ветки issue; код на ветке содержит не менее трёх намеренных дефектов, дающих P0/P1 при честном ревью (например: обработчик ошибки — no-op; путь конфигурации неверный; отсутствует вызов задокументированной функции — сознательно похоже на находки раунда 1 первого кейса).

**Steps:**

| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Запустить `/pf-execute` на фикстур-issue. | Реализация с намеренными дефектами закоммичена на ветку issue. |
| 2 | Запустить `/pf-codereview` (раунд 1). | Находит ≥3 блокирующих — по AC-1.2 запускается ранний бросок назад: `implementation_plan.md` получает новые задачи из находок, issue возвращается на `/pf-execute`, НЕ входит в fix-цикл. |
| 3 | Исправить намеренные дефекты через `/pf-execute` (задачи из шага 2), вернуться к `/pf-codereview`. | Раунд считается заново (не продолжением прежнего цикла — новый вход после возврата). |
| 4 | Наблюдать раунды 2 и (если нужно) 3. | Каждый раунд после первого проверяет закрытие находок предыдущего раунда И собственный дифф своих фиксов (AC-1.4); сходимость достигается не позже бюджета (по умолчанию 3) — либо, если нет, происходит контролируемый возврат на `/pf-execute` (AC-1.3), а не бесконечный цикл. |
| 5 | Прочитать `code_review.md` целиком по всем раундам. | Записи первого раунда присутствуют с исходными `CR-NNN` и обновлённым состоянием (`fixed`), ни одна не переписана и не исчезла (AC-2.1, AC-2.4). |
| 6 | Проверить финальный `verdict`. | `PASS` появляется только тогда, когда последний раунд отревьюил именно свой дифф исправлений и не нашёл открытых P0/P1 (BR-1, BR-2) — если сходимости не произошло, `verdict` отсутствует и issue стоит на `/pf-execute`, а не на неверном `PASS`. |

**Требуемые данные:**
- `docs/issues/open/zz-fixture-e2e-convergence/prompt.md` (временная фикстура: `size_tier: small`, `review_rounds: 3`)
- `docs/issues/open/zz-fixture-e2e-convergence/implementation_plan.md` (3-4 задачи, спроектированные так, чтобы честная реализация первой попытки содержала ≥3 находимых дефекта — по образцу таблицы раундов `codereview-tool` в `prompt.md`)
- `docs/issues/open/zz-fixture-e2e-convergence/test_plan.md` (минимальный, мапленный на задачи выше)

**Expected Outcome:** Полный цикл ведёт себя так же, как требует `brd.md` «Приёмка»: сходится за 2-3 раунда либо честно возвращается — без ложного `PASS` и без потерянных находок.

**Priority:** Critical

## Status Tracker

| TC     | Test Case | Type   | Priority | Status | Remarks |
| ------ | --------- | ------ | -------- | ------ | ------- |
| TC-001 | `review_rounds` — поле и дефолт 3 | Auto | High | [ ] | test-first |
| TC-002 | Ранний бросок назад — ≥3 блокирующих в раунде 1 | Auto | Critical | [ ] | test-first |
| TC-003 | Исчерпание бюджета — возврат на /pf-execute | Auto | Critical | [ ] | test-first |
| TC-004 | Раунд N проверяет закрытие И свой дифф | Auto | High | [ ] | test-first |
| TC-005 | Ledger append-only, CR-NNN, раунд, состояние | Auto | Critical | [ ] | test-first |
| TC-006 | Закрытый словарь состояний находки | Auto | High | [ ] | test-first |
| TC-007 | Промпт ревьюера раунда N несёт ledger + «не повторяй» | Auto | High | [ ] | test-first |
| TC-008 | На выходе PASS каждая находка имеет состояние | Auto | Critical | [ ] | test-first |
| TC-009 | Находка без сценария отказа понижается, ось важности одна | Manual | Critical | [ ] | требует живого прогона /pf-codereview |
| TC-010 | Переклассификация — явный шаг, причина, видимость | Auto | Medium | [ ] | test-first |
| TC-011 | Открытый P0 никогда не даёт PASS (regression guard) | Auto | Critical | [ ] | baseline |
| TC-012 | Follow-up issue — доступно для P1, не для P0 | Auto | High | [ ] | test-first |
| TC-013 | Гейт полноты — все пункты плана выполнены | Auto | High | [ ] | test-first |
| TC-014 | Гейт полноты — у задачи есть тест (прямое направление) | Auto | Critical | [ ] | test-first |
| TC-015 | Гейт полноты — у TC есть задача (обратное направление) | Auto | High | [ ] | test-first |
| TC-016 | Все три проверки полноты блокирующие | Auto | High | [ ] | test-first |
| TC-017 | Проверки полноты механические, не судейские | Auto | Medium | [ ] | test-first |
| TC-018 | Реальные git-топологии — пустой дифф / вне ветки | Auto | Medium | [ ] | инфраструктурный |
| TC-019 | Пустой дифф — ошибка стадии, не чистый результат | Manual | Critical | [ ] | требует живого прогона |
| TC-020 | Документ вне ветки issue — ревью по пути | Manual | High | [ ] | требует живого прогона |
| TC-021 | Явный признак пройденной проверки | Auto | High | [ ] | test-first |
| TC-022 | /pf-close — ни одна фаза не виснет без отчёта | Auto | Critical | [ ] | частично baseline (Phase 4.5) |
| TC-023 | Частичная остановка — описанный путь до конца | Auto | Medium | [ ] | частично baseline (Phase 4.5) |
| TC-024 | Задание сабагенту — пятиэлементный минимум | Auto | High | [ ] | частично baseline |
| TC-025 | Задание не требует недоступных TaskGet/TaskUpdate | Auto | Critical | [ ] | test-first, текущий текст нарушает AC |
| TC-026 | Расхождение нумерации не путает задачу | Auto | High | [ ] | test-first |
| TC-027 | Сквозной прогон — сходимость за 2-3 раунда либо возврат | Manual | Critical | [ ] | требует живого многораундового прогона |

## Known Issues

| Issue | Description | TC Affected | Steps to Reproduce | Severity |
| ----- | ----------- | ----------- | ------------------ | -------- |
| KI-1 | `skills/pf-execute/SKILL.md`, блок «For Each Task — write: claude» сегодня буквально предписывает вызов `TaskGet`/`TaskUpdate`, подтверждённо недоступных сабагенту (item 7, `prompt.md`) — прямое противоречие AC-6.2 до реализации. | TC-025 | Прочитать `skills/pf-execute/SKILL.md`, раздел «For Each Task — `write: claude` (Sub-Agent Instructions)», шаги 1 и 5. | High |
| KI-2 | Статический аудит текста (TC-004, TC-007, TC-010, TC-016, TC-017, TC-021-024) доказывает только, что инструкция СФОРМУЛИРОВАНА правильно — не то, что живой прогон LLM-агента реально ей следует. Полная уверенность недостижима конечным тестом; частичная поведенческая проверка — только через Manual TC (009, 019, 020, 027). | TC-004, TC-007, TC-010, TC-016, TC-017, TC-021, TC-022, TC-023, TC-024 | — (методологическое ограничение, не воспроизводимый дефект). | Low |
| KI-3 | `code_review.md` сегодня перезаписывает секции `### P0`/`### P1`/`### P2` целиком при каждом прогоне Phase 3 — нет ни стабильных ID находок, ни append-only гарантии. Это ровно тот пробел, который TC-005/TC-008 обнажают. | TC-005, TC-008 | Прочитать `skills/pf-codereview/SKILL.md`, раздел «Phase 3: Write `code_review.md`» — секции находок не имеют идентификаторов и переписываются заново на каждом Phase 3. | High |
