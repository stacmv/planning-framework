## Implementation Plan: Фикстура пустого предмета ревью

### Overview

Единственная задача — заведомо не реализована (сознательно, для этой
фикстуры): ветка задачи существует, но никакого диффа поверх родительской
ветки в ней нет. Цель — проверить, что запуск ревью на пустом дифф
сообщает об ошибке стадии, а не о чистом результате.

### Files to Create/Modify

- `tools/noop.sh` — заявлено, но не создано (дифф остаётся пустым)

### Implementation Tasks

#### Task 1: `tools/noop.sh` — заглушка (не реализуется в этой фикстуре)

**Task Type:** code
**Mapped Test Cases:** TC-001
**Files:** `tools/noop.sh` — новый файл (не создаётся намеренно)

**Implementation Notes:**
- Эта задача намеренно не выполнена файлово — фикстура проверяет
  поведение ревью на пустом дифф, а не реальную реализацию.

**Acceptance Criteria:**
- [x] TC-001 passes
