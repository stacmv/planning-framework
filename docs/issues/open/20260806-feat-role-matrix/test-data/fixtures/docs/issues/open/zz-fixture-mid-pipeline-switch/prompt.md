---
doc_language: Russian
size_tier: small
profile: solo-claude
---

Тестовая фикстура для TC-008 (роль-матрица, 20260806-feat-role-matrix):
маленький CLI-инструмент `env-dump.sh`, печатающий отсортированный список
переменных окружения. `brd.md`/`specs.md` уже завершены под профилем
`solo-claude` (см. Test Data) — фикстура нужна, чтобы посреди пайплайна
вручную сменить `profile:` на `codex-implements` и убедиться, что уже
пройденные стадии не переделываются задним числом.
