# Analysis: 20260715-bug-gates-verification-and-tooling-fixes

**Тип:** bug · **Размер:** small · **Дата анализа:** 2026-07-15
**Источник:** follow-up к `20260713-bug-v2-to-v3-migration-defects` (закрыт, `c6ee908..095cb20`). Все находки перепроверены на `develop` `836b04e` (добавил `/pf-autopilot`); ни одна не исправлена этим коммитом.

---

## Сводка

Четыре разнородные находки. Одна (№1) — **верификация уже сделанной починки**, кода не требует. Три (№2-4) — локальные баги инструментария, каждый в 1-10 строк. Плюс производный риск: `/pf-autopilot` наследует сломанную Phase 3 из `/pf-close` (№2). Суммарно — честный `small`: ни одна находка не многоподсистемна, все правки точечные.

---

## Находка 1 — входные гейты можно удовлетворить «по памяти» (ВЕРИФИКАЦИЯ, не починка)

**Корневая причина.** Инструкция гейта была верной прозой («документ обязан существовать → stop»), но **не принуждала к вызову инструмента**. В живой сессии удалённый файл всё ещё лежал в контексте модели, и она судила по памяти, а не по диску. Соседний oversized-guard принуждает (`wc -l`, подсчёт заголовков) — и работает именно поэтому.

**Починка уже в коде** (коммит `d865899` закрытого issue, присутствует на `836b04e`):
- `skills/pf-size-tiers/SKILL.md` — раздел «Evaluating it is a MECHANICAL check, never a memory exercise» с обязательным `ls -1 docs/issues/open/<ID>/` перед любым суждением;
- `skills/pf-impl-plan/SKILL.md`, `skills/pf-execute/SKILL.md` — оба входных гейта предписывают вызов первым действием («Run the check, do not recall it» — подтверждено grep'ом в обоих);
- `test/skills-static.sh` — 5 статических проверок не дают потерять принуждение.

**Что требуется в этом issue.** Только **живой прогон**: статика доказывает, что принуждение записано; что модель ему *подчиняется* — не докажет ни один автотест. Прогон: в мигрированном issue удалить `test_plan.md` → `/pf-impl-plan` обязан отказать; удалить `implementation_plan.md` → `/pf-execute` обязан отказать. Уходит в `manual_test_checklist.md` (человек в живой сессии).

**Повышенная актуальность.** `/pf-autopilot` гонит issue к закрытию автономно, без человека у экрана — то есть ровно в сценарии, где гейт «по памяти» опаснее всего. Верификацию стоит гонять и под автопилотом.

## Находка 2 — `/pf-close` путает upstream-ветку с родительской

**Корневая причина.** `skills/pf-close/SKILL.md:60` (Phase 3):

> Run `git config branch.issue/ISSUE-ID.merge`. If this returns a value such as `refs/heads/develop`…

`branch.<name>.merge` — **ветка отслеживания на remote (upstream)**, а не родитель. При пуше ветки issue с `-u` (что делает `/pf-execute`/пользователь) команда возвращает `refs/heads/issue/<ID>` — саму себя. Фолбэк на `develop` в Phase 3 не срабатывает: команда вернула значение, просто неверное. **Наблюдалось вживую** при закрытии `20260713-...`; родителя пришлось определять через `git merge-base --is-ancestor develop HEAD`.

**Что нужно.** Определять родителя не по upstream. Рекомендуемо: фолбэк-детект — первая из `develop`/`main`, являющаяся предком HEAD (`git merge-base --is-ancestor <cand> HEAD`); опционально `branch.<name>.pf-parent`, проставляемый `/pf-execute` при создании ветки. **Проверить `/pf-autopilot`** (`skills/pf-autopilot/SKILL.md`) — Step 3 зовёт `/pf-close`; он наследует ту же сломанную Phase 3.

## Находка 3 — Manual Test UI: два дефекта парсинга (`tools/manual-test-ui/lib/checklist.js`)

**3a. Экранированный пайп `\|` в ячейке таблицы ломается.** `:30` разбивает строку по сырому `"|"`; комментарий `:25` прямо признаёт «Does not support escaped `\|` inside cells». `\|` — стандартное экранирование пайпа в GFM, должно работать. UI превращает `\|` в `\ | `, ячейка разваливается, «Expected Result» уезжает в «Result». **Наблюдалось вживую** (шаг `ls ~/.claude/skills/ \| wc -l`).

**3b. Секции не в форме `## TC-NNN` не отображаются.** `:9` — `TC_HEADING_RE = /^##\s+TC-(\d+):/`; парсер (`:48-49`) пропускает любой не-TC заголовок. Отдельная секция вроде «Общая подготовка» до тестировщика не доезжает. **Наблюдалось вживую:** раздел подготовки был невидим, пока я не переименовал его в `## TC-000`.

**Что нужно.** (3a) распознавать `\|` при разбиении (замена на плейсхолдер перед split, восстановление после). (3b) решить: показывать не-TC секции отдельными карточками либо хотя бы не терять их содержимое. Учесть локализованные заголовки (тест `checklist-ru.test.js` уже проверяет русские секции — расширить его).

## Находка 4 — `make converge` не умеет передавать `--yes`

**Корневая причина.** `Makefile`, цель `converge`:

```
converge:
	bash scripts/converge-to-v3.sh $(if $(TARGET),--target $(TARGET),)
```

Проброса `--yes` нет → цель не автоматизируется: `make converge TARGET=…` не из терминала справедливо отменяется («Converge cancelled — stdin is closed»). **Наблюдалось вживую** при подготовке фикстур ручного теста. Прецедент проброса в `Makefile` есть — `make tui` использует `$(if $(TARGET),--target $(TARGET),)`.

**Что нужно.** Добавить `$(if $(YES),--yes,)`, чтобы `make converge TARGET=/tmp/x YES=1` работал неинтерактивно.

---

## Суждение о размере

`small` подтверждён. Правки: №1 — 0 строк кода (только живой прогон + чеклист); №2 — переписать Phase 3 `pf-close` (+ проверить `pf-autopilot`); №3 — ~10-15 строк в `checklist.js` + тест; №4 — 1 строка в `Makefile`. Ни одной новой подсистемы, ни одной многофайловой перестройки.

## Затронутые файлы

| Файл | Находка | Правка |
|---|---|---|
| `skills/pf-impl-plan/SKILL.md`, `skills/pf-execute/SKILL.md`, `skills/pf-size-tiers/SKILL.md`, `test/skills-static.sh` | 1 | уже исправлены (`d865899`) — только верификация |
| `skills/pf-close/SKILL.md` (Phase 3, `:58-66`) | 2 | детект родителя не по upstream |
| `skills/pf-autopilot/SKILL.md` | 2 | проверить/исправить наследование Phase 3 |
| `tools/manual-test-ui/lib/checklist.js` (`:9`, `:25`, `:30`, `:48-49`) | 3 | `\|` + не-TC секции |
| `tools/manual-test-ui/test/checklist-ru.test.js` | 3 | покрыть оба дефекта |
| `Makefile` (цель `converge`) | 4 | проброс `--yes` |

## Условия приёмки

- **№1:** живой прогон удалённых предусловий (`/pf-impl-plan`, `/pf-execute` отказывают) — вручную и под `/pf-autopilot`; записан в `manual_test_checklist.md`.
- **№2:** `/pf-close` на ветке, запушенной с `-u`, находит `develop` как родителя, а не саму ветку; авто-тест на детект родителя; `/pf-autopilot` не наследует баг.
- **№3:** UI рендерит ячейку с `\|` корректно и не теряет не-TC секции; `checklist-ru.test.js` покрывает оба; `node --test` зелёный.
- **№4:** `make converge TARGET=/tmp/x YES=1 < /dev/null` доходит до converge (не отменяется); существующий `make converge` без YES по-прежнему интерактивен.
- `make test` зелёный; ссылки/скилы не зовут несуществующего.
