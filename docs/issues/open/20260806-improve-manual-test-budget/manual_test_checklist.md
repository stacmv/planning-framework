# Manual Test Checklist

**Feature Name:** Manual test-case budget by size tier with 3-option gate
**Issue ID:** 20260806-improve-manual-test-budget
**Date:** 2026-08-13
**Tester:**

---

## How to use

Этот чек-лист проведёт вас по ручной проверке логики 3-вариантного гейта `AskUserQuestion` (Step 4d в `skills/pf-test-plan/SKILL.md`) при превышении Manual-кейсов после automation pass.

1. Прочитайте каждый раздел целиком перед началом.
2. Выполните предусловия, перечисленные в начале каждой проверки — если что-то выполнить не удаётся, остановитесь и сообщите разработчику.
3. Выполняйте шаги по порядку. После каждого шага сверьтесь с ожидаемым результатом и запишите, что произошло на самом деле, в колонку «Результат».
4. Ставьте `[x]` при успехе и `[ ]`, если что-то пошло не так.
5. Строку «Заметки» в конце каждой проверки используйте для всего необычного.
6. Закончив все проверки, верните этот документ разработчику.

---

## TC-011: Гейт — вариант «Разбить issue»

**Description:** Проверяет, что при выборе пользователем варианта «Разбить issue» в 3-вариантном гейте система корректно регистрирует это решение в `test_plan.md` с рекомендацией о разбиении и перечислением лишних кейсов.

**Prerequisites:**
- Framework репо (`planning-framework/`) готов к запуску `/pf-test-plan`.
- Команда `/pf` доступна (проверить: `which pf` или просто запустить `/pf` в сессии Claude Code).
- Тестовый issue создан в `docs/issues/open/` на основе fixture'а для TC-011 (путь: `test/fixtures/manual-budget-tc-011/` или создан вручную с `size_tier: medium` в `prompt.md` и 5 Manual-кейсами в черновике `test_plan.md` таких, что automation pass не решит проблему полностью).
- Черновой `test_plan.md` содержит достаточно Manual-кейсов, чтобы их число превышало бюджет Medium (>3) даже после возможной конвертации.
- Подготовленные данные: `/tmp/pf-test-data/20260806-improve-manual-test-budget/TC-011` (запустите `node docs/issues/open/20260806-improve-manual-test-budget/test-data/setup.mjs TC-011`, если каталога ещё нет)

**Test Data:**
- `test/fixtures/manual-budget-tc-011/docs/issues/open/manual-budget-tc-011/prompt.md` — fixture с `size_tier: medium`.
- `test/fixtures/manual-budget-tc-011/docs/issues/open/manual-budget-tc-011/test_plan.md` — черновик тест-плана с 5 Manual-кейсами (превышение бюджета medium ≤3).

**Steps:**

| Step | Action | Expected Result | Result |
|------|--------|-----------------|--------|
| 1 | Перейдите в каталог issue и запустите `/pf-test-plan` (или продолжите с `/pf` если issue уже выбран). | Skill проверяет Manual-кейсы: найденное число > бюджета tier (например, 5 > 3 для Medium). Гейт срабатывает с сообщением о превышении. | [x] Подтверждено — automation pass не сконвертировал ни одного из 5 (все причины легитимно неавтоматизируемы), 5 > 3 |
| 2 | Дождитесь появления `AskUserQuestion` с 3 вариантами (Split / Raise tier / Defer). | Вопрос задан явно; все три варианта видны и доступны для выбора. | [x] Реальный AskUserQuestion с ровно 3 вариантами |
| 3 | Выберите вариант 1: «Split the issue». | Выбор зарегистрирован; гейт не требует дополнительного ввода. | [x] Выбрано, без доп. вопросов |
| 4 | Дождитесь завершения skill. | Skill завершилась успешно без ошибок; сообщена кратко об успешном завершении. | [x] Завершилось штатно |
| 5 | Откройте финальный `test_plan.md` и найдите примечание о разбиении. | В `test_plan.md` добавлено примечание (например, абзац выше Status Tracker или в отдельной секции), которое указывает, что рекомендуется разбить issue и перечисляет, какие кейсы являются лишними. | [x] Добавлен blockquote-абзац перед Status Tracker |
| 6 | Проверьте точность перечисления лишних кейсов. | Примечание правильно называет те кейсы, число которых превышает бюджет (например, если Manual-кейсов 5 при бюджете 3, то названы последние 2 или выделена группа, составляющая избыток). | [x] Названы TC-004, TC-005 (избыток = 2) |
| 7 | Убедитесь, что Status Tracker сам по себе не изменился. | Статус кейсов (Auto/Manual) остался прежним; гейт добавил только примечание, не переводя кейсы. | [x] Таблица побайтно та же, только добавлен абзац сверху |

**Notes:** Прогон выполнен агентом (не внешним тестировщиком) 2026-08-17 — реальный `/pf-test-plan` Step 4b→4c→4d исполнен вручную против копии фикстуры (`/tmp/manual-qa-run/tc-011/`), включая настоящий вызов `AskUserQuestion`. Все 7 шагов подтверждены. `brd.md` в фикстуре отсутствует, поэтому буквальный запуск `/pf-test-plan` с нуля упёрся бы в её собственный входной гейт (`brd.md` required) — гейт Step 4d при этом тестируется независимо от этого предусловия.

---

## TC-012: Гейт — вариант «Поднять tier»

**Description:** Проверяет, что при выборе варианта «Поднять tier» система требует написать обоснование, обновляет `size_tier` в `prompt.md`, регистрирует обоснование и старый→новый tier в `test_plan.md`, и что Manual-кейсы теперь укладываются в новый бюджет.

**Prerequisites:**
- Framework репо готов к запуску `/pf-test-plan`.
- Команда `/pf` доступна.
- Тестовый issue создан на основе fixture'а для TC-012 (путь: `test/fixtures/manual-budget-tc-012/`) или вручную с `size_tier: medium` в `prompt.md` и Manual-кейсами, которые даже после automation pass превышают бюджет Medium (например, 5 Manual-кейсов).
- Черновой `test_plan.md` содержит достаточно Manual-кейсов, чтобы их число превышало текущий tier-бюджет.
- Подготовленные данные: `/tmp/pf-test-data/20260806-improve-manual-test-budget/TC-012` (запустите `node docs/issues/open/20260806-improve-manual-test-budget/test-data/setup.mjs TC-012`, если каталога ещё нет)

**Test Data:**
- `test/fixtures/manual-budget-tc-012/docs/issues/open/manual-budget-tc-012/prompt.md` — fixture с `size_tier: medium`.
- `test/fixtures/manual-budget-tc-012/docs/issues/open/manual-budget-tc-012/test_plan.md` — черновик с 5 Manual-кейсами.

**Steps:**

| Step | Action | Expected Result | Result |
|------|--------|-----------------|--------|
| 1 | Запустите `/pf-test-plan` на issue. | Гейт срабатывает с сообщением о превышении Manual-кейсов (например, 5 > 3 для Medium). | [x] 5 > 3 подтверждено после automation pass (0 конвертировано) |
| 2 | Дождитесь `AskUserQuestion` с 3 вариантами. | Вопрос задан; все три варианта доступны. | [x] Реальный AskUserQuestion, 3 варианта |
| 3 | Выберите вариант 2: «Raise the tier». | Система запрашивает обоснование для подъёма tier-а (вторая `AskUserQuestion` или текстовое поле). | [x] Второй AskUserQuestion за обоснованием |
| 4 | Введите обоснование (например, «этот feature требует 5 Manual проверок для комплексного тестирования UX-сценариев»). | Обоснование принято; система не требует дополнительного ввода. | [x] Принято |
| 5 | Дождитесь завершения skill. | Skill завершилась успешно. | [x] Завершилось штатно |
| 6 | Откройте `prompt.md` и проверьте поле `size_tier`. | `size_tier` обновлён с `medium` на следующий tier (ожидается `large` — бюджет ≤5). | [x] `size_tier: large` |
| 7 | Откройте финальный `test_plan.md` и найдите примечание о повышении tier. | Примечание записано и содержит: старый tier (medium), новый tier (large), введённое обоснование. | [x] Blockquote содержит medium→large и текст обоснования |
| 8 | Проверьте, что Manual-кейсы теперь в бюджете. | Если новый tier имеет бюджет ≥5, то 5 Manual-кейсов укладываются в бюджет; гейт не срабатывает повторно. | [x] `pf_check_manual_budget 5 large` → within budget |

**Notes:** Прогон выполнен агентом 2026-08-17, тем же методом, что TC-011 (копия фикстуры `/tmp/manual-qa-run/tc-012/`, реальные `AskUserQuestion`). Все 8 шагов подтверждены.

---

## TC-013: Гейт — вариант «Отложить избыток»

**Description:** Проверяет, что при выборе варианта «Отложить избыток» система добавляет примечание в `test_plan.md`, которое указывает, какие Manual-кейсы отложены и почему.

**Prerequisites:**
- Framework репо готов к запуску `/pf-test-plan`.
- Команда `/pf` доступна.
- Тестовый issue создан на основе fixture'а для TC-013 (путь: `test/fixtures/manual-budget-tc-013/`) или вручную с `size_tier: medium` в `prompt.md` и Manual-кейсами, превышающими бюджет (например, 4 Manual-кейса при бюджете 3).
- Черновой `test_plan.md` готов к запуску `/pf-test-plan`.
- Подготовленные данные: `/tmp/pf-test-data/20260806-improve-manual-test-budget/TC-013` (запустите `node docs/issues/open/20260806-improve-manual-test-budget/test-data/setup.mjs TC-013`, если каталога ещё нет)

**Test Data:**
- `test/fixtures/manual-budget-tc-013/docs/issues/open/manual-budget-tc-013/prompt.md` — fixture с `size_tier: medium`.
- `test/fixtures/manual-budget-tc-013/docs/issues/open/manual-budget-tc-013/test_plan.md` — черновик с Manual-кейсами (превышение бюджета).

**Steps:**

| Step | Action | Expected Result | Result |
|------|--------|-----------------|--------|
| 1 | Запустите `/pf-test-plan` на issue. | Гейт срабатывает: Manual-кейсов больше, чем позволяет бюджет (например, 4 > 3 для Medium). | [x] 4 > 3 после automation pass (0 конвертировано) |
| 2 | Дождитесь `AskUserQuestion` с 3 вариантами. | Вопрос виден; все три варианта доступны. | [x] Реальный AskUserQuestion, 3 варианта |
| 3 | Выберите вариант 3: «Defer the excess». | Выбор зарегистрирован. | [x] Выбрано (после уточнения — по умолчанию был предложен «Raise tier», согласовано вернуться к Defer, как задумано в TC-013) |
| 4 | Дождитесь завершения skill. | Skill завершилась успешно. | [x] Завершилось штатно |
| 5 | Откройте финальный `test_plan.md` и найдите примечание об отложенных кейсах. | Добавлено примечание (например, секция «Deferred Manual cases» или рекомендация) с указанием, какие кейсы отложены. | [x] Blockquote «Deferred Manual cases» добавлен |
| 6 | Проверьте, что примечание содержит перечисление кейсов и обоснование. | Перечислены конкретные Test Case ID-ы (например, TC-015, TC-017), которые отложены; указано, почему (например, «отложены для второго этапа разработки»). | [x] Назван TC-004 (избыток = 1), причина — cost |
| 7 | Убедитесь, что Status Tracker остался без изменений. | Отложенные кейсы остаются в Status Tracker с тем же типом (Manual); примечание — только информационное средство. | [x] Таблица не изменена, только добавлен абзац |

**Notes:** Прогон выполнен агентом 2026-08-17 (копия фикстуры `/tmp/manual-qa-run/tc-013/`, реальные `AskUserQuestion`). Все 7 шагов подтверждены.

---
