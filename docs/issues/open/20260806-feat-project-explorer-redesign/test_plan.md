# Test Plan: Редизайн Project Explorer (`tools/manual-test-ui`)

**Version:** 1.0
**Date:** 2026-08-17
**Predecessors:** `brd.md` (v1.0), `specs.md` (v1.0)

## Overview

Инструмент (`tools/manual-test-ui`) сегодня тестируется исключительно на
уровне сервера/lib-модулей: `test/*.test.js` поднимает реальный HTTP-сервер
(`test/helpers/server.js`, форк `server.js` на эфемерном порту) и обращается
к нему через `fetch`, либо дёргает функции `lib/*.js` напрямую — без headless
браузера, без jsdom (проект намеренно zero-dependency, см. `lib/roles.js` и
`lib/markdown.js`). Этот план продолжает тот же принцип: подавляющее
большинство пунктов этой issue формулируются как проверяемые на уровне
API/lib/CSS-текста/git-состояния — числовые контрастные пороги, форма ответа
эндпоинтов, построчные git-проверки, разбор ограниченного YAML-подмножества.
`Manual` оставлен только для трёх случаев, которые действительно требуют
живого браузера или субъективного суждения владельца: подтверждение палитры
(AC-02k), визуальная приёмка панельной раскладки по референсу (AC-02f/02g),
и живая проверка табуляции/фокуса — в этом проекте нет headless-браузерного
харнесса, которым это можно было бы прогнать в CI.

## Prerequisites

- Реализация по `specs.md` завершена: `public/launcher.js`, `public/workspace.js`,
  `public/inbox.js`, `lib/inbox.js`, `lib/roles-resolve.js` существуют;
  `public/style.css` содержит токены Pine; `test/contrast.test.js` существует.
- `skills/pf-roles/SKILL.md`, `skills/pf-close/SKILL.md`, `skills/pf-autopilot/SKILL.md`
  обновлены согласно §4.1/§4.1a `specs.md`.
- Референсные скриншоты GLog v.0.7 (`reference-glog-list.png`,
  `reference-glog-detail.png`) сохранены в папку issue (AC-02e).
- `node --test` прогоняется из `tools/manual-test-ui/` (`make test-ui` из
  корня репозитория) — окружение, в котором уже воспроизводимы все
  существующие `test/*.test.js`.
- Тестировщик может создавать временные git-репозитории через
  `tools/manual-test-ui/test/helpers/fixtures.js` (`makeTempRepo`) — тот же
  механизм, которым уже пользуются `test/multi-project.test.js` и
  `test/doc-states.test.js`.
- Известная недетерминированность `tools/manual-test-ui/test/`
  (`prepare.test.js`, `read-paths.test.js` TC-011, см. BRD Non-Goals) — не
  путать со свежей регрессией: см. Known Issues ниже.

### Test cases

### TC-001: Уровень 1 (лаунчер) — состав экрана и источник данных

**Description:** Проверяет, что лаунчер даёт выбор роли, карточку глобального инбокса и сетку карточек проектов на одном экране (AC-01a), и что карточки проектов по-прежнему рендерятся из существующего `GET /api/projects` без изменения формата ответа.

**Preconditions:**
- Сервер запущен с `projects.json`, содержащим не менее двух проектов.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Выполнить `GET /api/projects`. | Форма ответа идентична существующей (регрессия не введена редизайном экрана лаунчера). |
| 2 | Проверить, что `public/launcher.js` содержит рендер трёх блоков: переключатель роли, `.inbox-card`, `.project-grid`. | Все три блока присутствуют в одном модуле уровня 1, роль выбирается на этом же экране (AC-01a). |
| 3 | Проверить обработку hash `#/` в `app.js`. | Роутер монтирует `launcher.js`, не `workspace.js`/`inbox.js`. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (генерирует временный multi-project `projects.json`)

**Expected Outcome:** Лаунчер — единственная точка выбора роли + проекта/инбокса, без обращения к изменённому формату API.

**Priority:** High

---

### TC-002: Уровень 2 — рабочее пространство без сайдбара, `.doc-tabs` по объединению выбранных ролей

**Description:** Проверяет, что рабочее пространство проекта не содержит постоянного сайдбара (AC-01b), что Issue переключается выпадающим списком, а роль — мультивыбор-фильтром в шапке (тот же компонент, что и на остальных экранах, после второго раунда догфудинга), и что `.doc-tabs` показывает объединение документов всех выбранных ролей — включая таб «Дела», дедуплицированный до одной вкладки — без потери документов, уникальных для конкретной роли (AC-01c/AC-01d, specs.md §2.2).

**Preconditions:**
- Проект с issue, содержащей документы всех трёх ролей (`brd.md`, `specs.md`, `test_plan.md` и т.д.).

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Проверить `public/index.html`/`public/style.css` на отсутствие постоянного `.sidebar`-контейнера, использовавшегося в прежней раскладке. | Персистентного сайдбара нет — только `.workspace-header` с выпадающим списком issue и мультивыбор-фильтром роли. |
| 2 | Выполнить `GET /api/roles`, затем `GET /api/projects/:name/issues/:id/roles/tester` (`buildRoleContents` для роли «Тестировщик», `server.js`, specs.md §2.2 — НЕ `GET .../docs`, который отдаёт один документ по `?path=` и не принимает роль вовсе). | Ответ (`payload.items`) содержит ровно набор `roles.itemsOf("tester")` — `test_plan.md`, `manual_test_checklist.md`, `qa_report.md`, `.qa-workflow.md`, `prepare`; ни `brd.md`, ни `specs.md`, ни `implementation_plan.md` не входят в этот набор (переиспользует инвариант `test/roles.test.js` TC-006 — «роль показывает ровно то, что объявляет»). Клиентский таб «Дела» добавляется поверх этого ответа самим `workspace.js`, не отдаётся `GET .../roles/:role` (specs.md §2.2). |
| 3 | Выбрать в мультивыбор-фильтре одновременно «Тестировщик» и «Разработчик» (эквивалент на уровне API — вызвать И `.../roles/tester`, И `.../roles/developer`, передать оба ответа в `buildTabSet([...])`, без браузера). | Набор вкладок — объединение `roles.itemsOf("tester")` И `roles.itemsOf("developer")` (`test_plan.md`, `manual_test_checklist.md`, `qa_report.md`, `specs.md`, `implementation_plan.md`, …), без дублей по id, каждый документ виден одновременно. Сняв «Разработчик» обратно (пустой выбор -> объединение ВСЕХ ролей, а не одна произвольная), набор вновь расширяется, а не сужается до нуля. |
| 4 | Grep `public/workspace.js` на захардкоженный литеральный список имён документов (например массив, одновременно содержащий `"specs"`, `"implementation_plan"`, `"test_plan"`, `"brd"`). | Такого списка нет — `.doc-tabs` строится из данных, полученных от одного или нескольких `GET .../issues/:id/roles/:role` (`buildRoleContents`), а не из константы в клиентском коде (`lib/roles.js` не потребовал правок — специфицировано в §2.2). |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (issue-фикстура с полным набором документов по всем ролям)

**Expected Outcome:** Ровно один контейнер вкладок, содержимое которого детерминировано объединением выбранных ролей (или всех ролей, если ничего не выбрано) и существующим `GET /api/roles`.

**Priority:** High

---

### TC-003: Переключение issue сохраняет активный таб документа в рамках роли

**Description:** Проверяет правило specs.md §2.2 (снимает P0 из `/pf-check`): переключение issue в выпадающем списке не сбрасывает активный таб — если открыт `brd` issue A, переключение на issue B оставляет активным `brd` (или ближайший существующий документ, если `brd` у B ещё нет, по тем же правилам `missing`/`not_applicable`, что и сегодня). Это клиентское состояние, недостижимое через HTTP напрямую — тест предполагает, что `workspace.js` экспортирует чистую функцию `resolveActiveTab(prevTabId, docsOfNewIssue)`, а не только вплетает правило в обработчик клика. Второй раунд догфудинга расширил, кто ещё вызывает эту функцию: переключение (добавление/снятие) одной роли в мультивыбор-фильтре теперь тоже сохраняет активный таб через `resolveActiveTab`, а не сбрасывает его — полную замену выбора роли (переход «ровно к этой одной роли») даёт отдельная функция `selectRole`, которая продолжает брать свежий первый таб, как раньше делал единственный `[Роль ▾]`. Загрузка модуля — динамическим `import()` (см. Preconditions): `public/workspace.js` — нативный ES-модуль (`export`/`import`, specs.md §2.3), не UMD-модуль вроде `lib/roles.js`, поэтому `require()`/`vm.runInContext` для него не работают (`export` — синтаксическая ошибка вне модульного контекста); `test/roles.test.js`'s `node:vm`-загрузка `lib/roles.js` не прецедент для файлов из `public/`.

**Preconditions:**
- Проект с двумя issue: issue A имеет `brd.md`, issue B — не имеет `brd.md` (документ `missing`).
- `public/workspace.js` экспортирует (`export function resolveActiveTab(...)`) чистую функцию, реализующую правило «сохранить таб при смене issue» — задача на это должна быть заведена в `implementation_plan.md`, если модуль сейчас пишет это правило только внутри DOM-обработчика.
- Тест загружает модуль динамическим `import()`: `const { resolveActiveTab } = await import(pathToFileURL(path.join(TOOL_DIR, "public", "workspace.js")).href)` (`node:url`'s `pathToFileURL`). Это работает из CommonJS-тестового файла без флагов — Node's dynamic `import()` поддерживает ESM независимо от типа вызывающего модуля, в отличие от `require()`/`vm.runInContext` (non-module context), которым `export`-синтаксис недоступен без `vm.SourceTextModule` за флагом `--experimental-vm-modules` (нигде не подключённым в этом проекте).

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Вызвать `resolveActiveTab("brd", docsOfIssueA)`, где `docsOfIssueA` — ответ `GET .../issues/A/roles/analyst` (роль `analyst`, `brd.md` присутствует). | Возвращает `"brd"` — таб не меняется, когда документ есть у обеих issue. |
| 2 | Вызвать `resolveActiveTab("brd", docsOfIssueB)`, где `docsOfIssueB` — ответ `GET .../issues/B/roles/analyst` (`brd.md` отсутствует, статус `"missing"` — `lib/docstate.js`). | Возвращает `"brd"` (таб остаётся тем же, документ рендерится в состоянии `missing`) — не переключение на другой документ автоматически, по тем же правилам, что и сегодня для `missing`/`not_applicable`. |
| 3 | Отдельно проверить: смена роли не проходит через `resolveActiveTab` — набор вкладок для новой роли берётся заново из `roles.itemsOf(newRoleId)` (TC-002 шаг 3), не из предыдущего активного таба. | Правило «сохранить таб» применяется только внутри одной роли при смене issue, не при смене роли. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (две issue-фикстуры, у одной отсутствует `brd.md`)

**Expected Outcome:** Активный документ переживает смену issue внутри роли; смена роли — отдельная, независимая операция.

**Priority:** High

---

### TC-004: Клик-путь проект → документ — не больше двух/трёх кликов

**Description:** Проверяет M1/AC-01e: от карточки проекта на уровне 1 до документа последней активной issue — не больше двух кликов; переход к другой issue того же проекта — до трёх кликов (дополнительный клик за счёт `[Issue ▾]`, не нарушение). Клик-счёт проверяется не браузерным кликаньем, а числом хэш-переходов, которое порождает один клик — тест предполагает, что `app.js`/`launcher.js` выражают выбор целевого маршрута чистой функцией, экспортированной для тестирования, например `resolveLandingRoute(project, lastIssueByProject)`, возвращающей итоговый hash (`#/p/<project>/i/<issue>` или `#/p/<project>` для списка issue) за один вызов — если сейчас это решение вплетено только в обработчик клика, задача на экспорт функции должна быть в `implementation_plan.md`.

**Preconditions:**
- Фикстура значений `pf.lastIssue.<project>`: для проекта A запись есть, для проекта B — нет.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Вызвать `resolveLandingRoute("A", { A: "20260806-feat-example" })`. | Возвращает hash, ведущий напрямую на уровень 2 issue `20260806-feat-example` (без промежуточного списка issue) — один клик по карточке проекта уже приводит к этому вызову, второй — переход к самому документу: итог 2 клика (проект → документ). |
| 2 | Вызвать `resolveLandingRoute("B", {})` (записи `pf.lastIssue.B` нет). | Возвращает `#/p/B` — hash списка issue проекта B (специфицировано specs.md §2.3), не документ наугад и не пустой экран. |
| 3 | Смоделировать выбор другой issue того же проекта из выпадающего списка на уровне 2 как отдельный вызов роутера (не через `resolveLandingRoute` — это уже происходящая на уровне 2 операция). | Итоговый маршрут достигается за 3 перехода (проект → issue → документ) — специфицировано как ожидаемое поведение, не нарушение M1 (AC-01e). |

**Test Data:**
- не требуются

**Expected Outcome:** Хэш-роутинг (`#/`, `#/p/<project>`, `#/p/<project>/i/<issue>`) и `pf.lastIssue.<project>` дают ≤2 клика для уже активной issue и ровно на 1 клик больше при явном выборе другой issue.

**Priority:** High

---

### TC-005: Страница скроллится штатно

**Description:** Регрессионная проверка Problem Statement #3 — `body { overflow: hidden }` не должен встречаться в новой раскладке (AC-01f).

**Preconditions:** не требуются.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Прочитать `public/style.css`, найти правило для селектора `body`. | Правило не содержит `overflow: hidden`. |
| 2 | Проверить оставшиеся `overflow-y`/`overflow-x` объявления (например, на `.doc-panel`). | Локальные `overflow` на внутренних контейнерах допустимы (это не баг сам по себе), но не на `body`/на корневом `.shell`, который раньше и рвал скролл страницы. |

**Test Data:**
- `tools/manual-test-ui/public/style.css`

**Expected Outcome:** Ноль вхождений `overflow: hidden` на `body`/корневом контейнере страницы.

**Priority:** High

---

### TC-006: Автотест контраста `test/contrast.test.js`

**Description:** Проверяет, что новый `test/contrast.test.js` существует, входит в стандартный `node --test`-прогон (`make test`/`make test-ui`), и падает при нарушении любого из порогов AC-02a–02c: `--text`↔`--surface` ≥7:1 (тёмная тема — около 10:1, не 15–21:1), каждый цвет текста (включая литералы) ↔`--bg` и ↔`--surface` ≥4.5:1, границы ≥1.5:1, ни чистого чёрного фона, ни чистого белого текста (M2).

**Preconditions:**
- `public/style.css` содержит финальные токены Pine (см. TC-007).
- Логика вычисления контраста вынесена в переиспользуемые чистые функции (например `parseRootTokens(cssText)` и `contrastRatio(hexA, hexB)`, экспортированные из `test/contrast.test.js` или отдельного `lib/contrast.js`) — так, чтобы негативные кейсы (шаги 2–3) прогонялись на строке-фикстуре, не требуя правки реального `public/style.css`. Если сейчас парсер и вычисление контраста не разделены на вызываемые функции, задача на это должна быть в `implementation_plan.md`.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Запустить `node --test tools/manual-test-ui/test/contrast.test.js` как есть, против реального `public/style.css`. | Сьют завершается 0 (все пары проходят пороги) на текущем файле. |
| 2 | Вызвать `contrastRatio` для пары значений, где `--text`-подобный цвет заведомо даёт контраст <7:1 к `--surface`-подобному фону (inline-строки, не файл). | Функция возвращает значение <7, и обёрточная проверка (та же, что использует сьют на реальном файле) фиксирует нарушение — тест реально что-то проверяет, а не всегда зелёный. |
| 3 | Вызвать `parseRootTokens` на inline-CSS-строке, содержащей `color: #ffffff` как значение цвета текста, прогнать через ту же проверку «ни чистого белого текста». | Нарушение обнаружено на этой inline-фикстуре (AC-02c), реальный `public/style.css` не редактируется. |
| 4 | Убедиться, что `test/contrast.test.js` подхватывается стандартным раннером без отдельного вызова (лежит в `test/`, конвенция `node --test`). | Файл не требует правки `Makefile`/скриптов запуска. |

**Test Data:**
- `tools/manual-test-ui/public/style.css` (читается, не модифицируется)

**Expected Outcome:** M2 = 0 нарушений подтверждается автоматически и воспроизводимо, тест не является плацебо (шаги 2–3 доказывают чувствительность).

**Priority:** Critical

---

### TC-007: Токены `:root` — точное соответствие палитре Pine

**Description:** Проверяет AC-02d — ровно 10 указанных токенов с точными hex-значениями, без альтернативных цветовых схем.

**Preconditions:** не требуются.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Распарсить `:root` блок `public/style.css` построчным парсером `--name: #hex;` (тем же уровнем сложности, что и `test/contrast.test.js`). | Найдены ровно: `--bg #16241b --surface #1e3126 --border #3c5c46 --text #c6d5c8 --muted #96ac9c --ok #6ec97f --fail #f08d84 --warn #e0b95c --held #b6a0ef --accent #5cc4d4`. |
| 2 | Проверить отсутствие второго набора токенов (media query светлой темы, альтернативной палитры, `data-theme` переключателя). | Ровно одна цветовая схема — соответствует Non-Goals (мультитемность не запрашивалась). |
| 3 | Проверить отсутствие старых имён `--panel`/`--panel-2`/`--pass` в CSS (переименованы в `--surface`/`--ok`). | Старые имена токенов не остались как мёртвый код рядом с новыми. |

**Test Data:**
- `tools/manual-test-ui/public/style.css`

**Expected Outcome:** `:root` — единственный источник палитры, полностью совпадающий с AC-02d.

**Priority:** High

---

### TC-008: Захардкоженные цветовые литералы заменены токенами

**Description:** Проверяет AC-02j — ни один хардкоженный hex-литерал не используется как `color:` в CSS; конкретно устранены `#fff`, `#dbe6ff`, `#f0d9a0`, `#f2b5b1` (найдены в текущем `style.css` строками `.role-btn.active`, `.list li.active .badge`, `.notice.warn`, `.notice.error`).

**Preconditions:** не требуются.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Grep `public/style.css` на `color:\s*#[0-9a-fA-F]{3,6}` (буквальный hex как значение `color`). | Ноль совпадений — все значения `color:` ссылаются на `var(--…)`. |
| 2 | Отдельно проверить 4 конкретных литерала из Problem Statement/`/pf-check`-находки P1-4 (`#fff`, `#dbe6ff`, `#f0d9a0`, `#f2b5b1`). | Ни один не встречается нигде в файле как значение `color`. |
| 3 | Прогнать `test/contrast.test.js` (TC-006) после замены. | Заменённые токены проходят контрастные пороги — AC-02b становится проверяемым по факту, не только декларативным. |

**Test Data:**
- `tools/manual-test-ui/public/style.css`

**Expected Outcome:** Полное покрытие AC-02j: замена не частичная, подтверждено grep'ом по всему файлу.

**Priority:** High

---

### TC-009: Соотношение размеров `h1`/body — 1.3–1.8x

**Description:** Проверяет AC-02e (фикс текущего бага h1=15px vs body=14px) — заметная, но не драматическая разница.

**Preconditions:** не требуются.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Распарсить `font-size` для `body`/`html` (базовый размер) и для `h1` из `public/style.css`. | Значения найдены (в `em`/`px`/`calc()` — парсер вычисляет итоговый px). |
| 2 | Вычислить отношение `h1 / body`. | Отношение ≥1.3 и ≤1.8 (specs.md выбирает 1.5 как середину — `h1 { font-size: calc(1em * 1.5); }`). |
| 3 | Убедиться, что референсные скриншоты GLog v.0.7 сохранены в папке issue. | `reference-glog-list.png` и `reference-glog-detail.png` существуют в `docs/issues/open/20260806-feat-project-explorer-redesign/` (файловая проверка существования — не оценка соответствия, это TC-012). |

**Test Data:**
- `tools/manual-test-ui/public/style.css`
- `docs/issues/open/20260806-feat-project-explorer-redesign/reference-glog-list.png`
- `docs/issues/open/20260806-feat-project-explorer-redesign/reference-glog-detail.png`

**Expected Outcome:** Числовой диапазон соблюдён; референсы физически присутствуют для трассируемости.

**Priority:** Medium

---

### TC-010: `.panel-table` — мягкая тональная заливка вместо жёстких grid-линий

**Description:** Проверяет AC-02h — таблицы/списки внутри `.panel` используют `color-mix()` от `--surface`/`--accent` для чередующихся строк, не жёсткие border-линии между каждой строкой, сохраняя контраст AC-02a–02c.

**Preconditions:** не требуются.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Найти правило `.panel-table` в `public/style.css`. | Существует, использует `background: color-mix(in srgb, var(--surface) 85%, var(--accent))` (или эквивалент на тех же переменных) для нечётных строк. |
| 2 | Проверить отсутствие `border-collapse` со сплошными `border: 1px solid` на каждой строке таблицы внутри `.panel-table`. | Заливка — основной визуальный разделитель строк, не сетка линий. |
| 3 | Прогнать `test/contrast.test.js` для пары «текст на заливке нечётной строки» ↔ порог AC-02a–02c. | Контраст не деградирует из-за подмешивания `--accent` в фон. |

**Test Data:**
- `tools/manual-test-ui/public/style.css`

**Expected Outcome:** Реализован приём из референса владельца (мягкая тональная заливка), без потери контраста.

**Priority:** High

---

### TC-011: Sans-serif для прозы, monospace только для кода

**Description:** Проверяет AC-02i — существующая договорённость сохранена редизайном (шрифт без засечек для интерфейса, моноширинный — только для фрагментов кода).

**Preconditions:** не требуются.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Проверить `font-family` для `body`/`.doc-panel`/`.panel`. | Sans-serif стек (без `serif`, без `monospace` в качестве основного). |
| 2 | Проверить `font-family` для `code`/`pre`/`.mono`-подобных селекторов. | Моноширинный стек, ограниченный этими селекторами. |

**Test Data:**
- `tools/manual-test-ui/public/style.css`

**Expected Outcome:** Разделение шрифтов не нарушено редизайном.

**Priority:** Medium

---

### TC-012: Визуальная приёмка панельной раскладки по референсу

**Description:** Проверяет AC-02f (каждая смысловая область — отдельная панель с заголовком акцентным цветом, не сплошное полотно), AC-02g (контекстные действия и быстрые переходы видны сразу, не в меню/модалках) и то, что референсные скриншоты GLog v.0.7 (`reference-glog-list.png`/`reference-glog-detail.png`) действительно приложены и итоговая раскладка узнаваемо следует им — это требует визуального суждения, не выражается числовым порогом.

**Preconditions:**
- Реализация лаунчера и рабочего пространства завершена и запущена локально.
- Референсные скриншоты открыты рядом для сравнения.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Открыть рабочее пространство проекта (уровень 2) в браузере. | Каждая смысловая область (шапка, табы, содержимое документа/чек-листа) читается как отдельная панель с подписанным заголовком акцентным цветом — не «сплошное полотно» (AC-02f). |
| 2 | Осмотреть экран чек-листа/инбокса на предмет действий (переход к делу, фильтр периода/раздела и т.п.). | Контекстные действия видны сразу на экране — не спрятаны в дополнительное меню или модальное окно (AC-02g). |
| 3 | Сравнить общее ощущение плотности/панельности с `reference-glog-list.png`/`reference-glog-detail.png`. | Владелец подтверждает, что раскладка узнаваемо следует референсу (панели, плотная но не перегруженная подача, акцентные заголовки). |

**Test Data:**
- `docs/issues/open/20260806-feat-project-explorer-redesign/reference-glog-list.png`
- `docs/issues/open/20260806-feat-project-explorer-redesign/reference-glog-detail.png`

**Expected Outcome:** Владелец подтверждает соответствие референсу; расхождения фиксируются как замечания, а не молчаливо принимаются.

**Priority:** High

---

### TC-013: Owner sign-off палитры и типографики до старта `/pf-execute`

**Description:** Проверяет AC-02k — процессная веха: владелец явно подтверждает итоговую палитру/типографику как «удобно для длительной работы» (демо или скриншот-ревью), и `/pf-execute` для этой issue не начинается без этой отметки.

**Preconditions:**
- `specs.md` и референсные скриншоты готовы.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Проверить `session-log.md` issue на отметку об owner sign-off, датированную до первого коммита `/pf-execute`. | Строка подтверждения присутствует и предшествует началу реализации. |
| 2 | Владелец лично просматривает демо/скриншот и подтверждает субъективное «удобно для длительной работы» (или указывает на отклонение). | Явное «да»/«нет» от владельца, не молчаливое согласие. |

**Test Data:**
- `docs/issues/open/20260806-feat-project-explorer-redesign/session-log.md`

**Expected Outcome:** Sign-off зафиксирован до `/pf-execute`, следуя порядку «бриф → критика Codex → выбор владельцем → реализация» из Non-Goals.

**Priority:** Critical

---

### TC-014: `:focus-visible` — контраст индикатора и нативные `<button>`

**Description:** Проверяет статически проверяемую часть US-03: контраст `outline` фокус-индикатора соответствует AC-02a–02c, и кнопки действий реализованы нативным `<button>`, а не `<div onclick>` (AC-03b, специфицировано §6 — «без ARIA-костылей»).

**Preconditions:** не требуются.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Найти правило `:focus-visible` в `public/style.css`. | `outline: 2px solid var(--accent); outline-offset: 2px` (или эквивалент на токене `--accent`), не `:focus` (чтобы не показывать рамку при клике мышью). |
| 2 | Прогнать `test/contrast.test.js` для пары `--accent` ↔ `--bg`/`--surface`. | Контраст индикатора проходит пороги AC-02b (уже разделяемый тест, не новый). |
| 3 | Grep `public/*.js` на `onclick=` и на `<div` с обработчиком клика вместо `<button>` в местах действий (`.human-task-actions`, действия чек-листа). | Ноль совпадений — действия реализованы `<button>`. |

**Test Data:**
- `tools/manual-test-ui/public/style.css`
- `tools/manual-test-ui/public/workspace.js`
- `tools/manual-test-ui/public/inbox.js`

**Expected Outcome:** Статически проверяемая часть a11y-требований выполнена без ARIA-костылей.

**Priority:** Medium

---

### TC-015: Живая проверка табуляции и видимого фокуса по всему приложению

**Description:** Проверяет AC-03a/03c — все интерактивные элементы (вкладки ролей, выпадающие списки issue/роли, табы документов, элементы инбокса, действия на чек-листе) достижимы по Tab в логичном порядке, и что фокус реально виден глазом при навигации — это требует живого браузера, которого в этом zero-dependency проекте нет в тестовом харнессе (`test/` работает только через `fetch`/lib-функции, не через DOM).

**Preconditions:**
- Инструмент запущен локально (`make test-ui` или прямой запуск).

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | С лаунчера последовательно нажимать Tab, не трогая мышь. | Порядок фокуса: переключатель роли → `.inbox-card` → карточки `.project-grid`, соответствует DOM-порядку разметки (§6). |
| 2 | Перейти на уровень 2, продолжить Tab. | Порядок: элементы `.workspace-header` → `.doc-tabs` → содержимое активной панели, без «прыжков» и без недостижимых мышью-only элементов. |
| 3 | На каждом шаге визуально проверить видимость рамки фокуса. | Рамка видна на каждом фокусируемом элементе, ни на одном — не теряется. |
| 4 | Пройти таб «Дела» и элементы человеческой задачи/чек-листа по Tab, попробовать выполнить действие (открыть задачу, отметить шаг) без мыши. | Действие выполнимо полностью с клавиатуры. |

**Test Data:**
- не требуются

**Expected Outcome:** Полная клавиатурная навигация подтверждена вживую; отклонения фиксируются как P0/P1 для правки до закрытия issue.

**Priority:** High

---

### TC-016: `GET /api/inbox` — агрегация across projects, форма ответа

**Description:** Проверяет AC-04a и форму ответа из specs.md §3.2 — эндпоинт собирает дела из всех настроенных проектов (не только из того, откуда запущен инструмент), возвращает `{ manualTests[], humanTasks[], totalCount }`.

**Preconditions:**
- `projects.json` с ≥2 проектами; проект A содержит `pending`-строку в Status Tracker, проект B — human-задачу, резолвящуюся из `prompt.md`.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Выполнить `GET /api/inbox`, не находясь «внутри» ни проекта A, ни проекта B. | Ответ 200, содержит дела из обоих проектов одновременно. |
| 2 | Проверить структуру `manualTests[]`. | Каждый элемент несёт `project, issueId, ptcId, area, testCase, priority, origin`. |
| 3 | Проверить структуру `humanTasks[]`. | Каждый элемент несёт `project, issueId, stageKey, operation, mode, artifactPath, instruction, status`. |
| 4 | Проверить `totalCount`. | `totalCount === manualTests.length + humanTasks.length`. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (multi-project фикстура: одна issue с `pending` TC в `docs/planning/test-plan.md`, другая — с `roles.<key>` резолвящимся в `human`)

**Expected Outcome:** Один эндпоинт, один запрос, оба типа дел из всех проектов сразу.

**Priority:** Critical

---

### TC-017: Парсинг `Origin` для `manualTests[].issueId` — обязательность и защита от повреждённых строк

**Description:** Проверяет specs.md §3.2 п.1 — `issueId` обязателен (не `issueId?`), парсится из колонки `Origin` (`ISSUE-ID#TC-NNN`); строка без валидного `Origin` — признак повреждённого файла и исключается из `manualTests[]` с предупреждением в серверном логе, не отдаётся клиенту с пустым `issueId`.

**Preconditions:**
- `docs/planning/test-plan.md` проекта содержит: одну корректную `pending`-строку с `Origin = 20260806-feat-example#TC-003`, и одну строку с пустым/некорректным `Origin`.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Выполнить `GET /api/inbox`. | Корректная строка присутствует в `manualTests[]`, с `issueId === "20260806-feat-example"`. |
| 2 | Проверить, что строка с некорректным `Origin` отсутствует в `manualTests[]`. | Ни одного элемента с пустым/`undefined` `issueId`. |
| 3 | Проверить stderr/лог сервера. | Предупреждение о повреждённой строке (файл/строка), не тихое отбрасывание. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (проект с `docs/planning/test-plan.md`, содержащим валидную и невалидную строки Origin)

**Expected Outcome:** `issueId` — гарантированно непустое поле для каждого элемента `manualTests[]`, повреждённые строки видимы в логе, а не молча теряются.

**Priority:** High

---

### TC-018: Инбокс — два раздела из одного ответа `/api/inbox`

**Description:** Проверяет AC-04b/specs.md §3.3 — клиент рендерит «Ручные тесты» и «Человеческие задачи» как раздельные секции/вкладки внутри экрана инбокса, оба из одного ответа, без второго запроса.

**Preconditions:** не требуются.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Проверить `public/inbox.js` на количество вызовов `fetch`/эквивалента к `/api/inbox` при открытии экрана инбокса. | Ровно один запрос. |
| 2 | Проверить, что рендер обоих разделов (`manualTests`, `humanTasks`) построен из одного и того же полученного объекта. | Раздельные визуальные секции — не два экрана и не смешанная лента с общим бейджем (BRD Interview Notes: «не сваливаются в одну ленту с одинаковыми на вид карточками»). |
| 3 | Grep `public/inbox.js` на обработчик переключения раздела: убедиться, что он меняет только локальное состояние компонента (например, переменную/атрибут `activeSection`), а не присваивает `location.hash`. | Переключение раздела не порождает переход по хэшу — не отдельные маршруты (§3.3), подтверждено на уровне исходного кода, не кликом в браузере. |

**Test Data:**
- `tools/manual-test-ui/public/inbox.js`

**Expected Outcome:** Один запрос, два визуально различных раздела.

**Priority:** High

---

### TC-019: Карточка глобального инбокса на лаунчере — суммарное число дел

**Description:** Проверяет AC-04d — `.inbox-card` показывает `totalCount` из `GET /api/inbox` (пример из specs.md: «Инбокс — все проекты, 7 дел»).

**Preconditions:**
- Фикстура, где `manualTests.length + humanTasks.length === 7` (например 4 + 3).
- `public/launcher.js` экспортирует (`export function formatInboxCardLabel(...)`) чистую функцию форматирования подписи карточки, `formatInboxCardLabel(totalCount)`, отдельную от DOM-рендеринга — если сейчас это не выделено, задача на это должна быть в `implementation_plan.md`.
- Загрузка — динамическим `import()`: `await import(pathToFileURL(path.join(TOOL_DIR, "public", "launcher.js")).href)`. `public/launcher.js` — нативный ES-модуль (`export`/`import`, specs.md §2.3), не UMD-модуль вроде `lib/roles.js` — `require()`/`vm.runInContext` не годятся (`export` — `SyntaxError` вне модульного контекста; `test/roles.test.js`'s `node:vm`-загрузка не прецедент для файлов из `public/`). Dynamic `import()` работает из CommonJS-тестового файла без флагов.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Вызвать `formatInboxCardLabel(7)` на данных из `GET /api/inbox` (`totalCount === 7`). | Строка вида «Инбокс — все проекты, 7 дел» (пример из specs.md §2.1). |
| 2 | Вызвать `formatInboxCardLabel(0)`. | Строка с явным «0 дел» — карточка не скрывается и не показывает placeholder без числа. |
| 3 | Grep `public/launcher.js`/`public/app.js` на обработчик клика `.inbox-card`. | Устанавливает hash `#/inbox` (не привязан к конкретному проекту) — подтверждено на уровне исходного кода. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (фикстура на 7 суммарных дел, вариант с 0)

**Expected Outcome:** Счётчик на лаунчере всегда синхронен с `/api/inbox`.

**Priority:** High

---

### TC-020: Обязательные поля каждого элемента инбокса

**Description:** Проверяет AC-04c — для каждого элемента инбокса видно как минимум: проект, issue, что конкретно требуется, и куда вести пользователя по клику.

**Preconditions:**
- Фикстура с одним элементом каждого типа (manual TC и human-task).

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Проверить `manualTests[0]`. | `project`, `issueId`, `testCase` (что требуется) присутствуют и непусты; `ptcId`/`area` дают достаточно контекста для перехода к конкретному чек-листу. |
| 2 | Проверить `humanTasks[0]`. | `project`, `issueId`, `stageKey`/`operation` (что требуется), `artifactPath`/`instruction` присутствуют и непусты. |
| 3 | Проверить клиентский рендер (`inbox.js`) на наличие ссылки/обработчика перехода для каждого элемента. | Клик по элементу ведёт на соответствующий документ/чек-лист/таб «Дела» нужного проекта и issue. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js`

**Expected Outcome:** Ни один элемент инбокса не отдаётся без указания «что» и «куда».

**Priority:** High

---

### TC-021: `lib/roles-resolve.js` — разбор ограниченного flow-YAML подмножества

**Description:** Проверяет specs.md §3.2 п.2 — новый модуль читает `roles: { <key>: { write: ..., review: [...], mode: ... } }` в однострочном flow-стиле, включая вложенность ровно в 2 уровня, flow-списки, и голые скалярные значения (`dev_docs: skip`) — на реальных примерах, как они уже написаны в `prompt.md` этого репозитория.

**Preconditions:** не требуются.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Скормить `lib/roles-resolve.js` строку `roles: { specs: { write: claude, review: [codex] } }`. | Верно разобраны `write: "claude"`, `review: ["codex"]` для ключа `specs`. |
| 2 | Скормить `roles: { code: { write: codex, review: [claude], mode: blocking } }`. | Поле `mode` разобрано и возвращено в объекте резолвинга наравне с `write`/`review` (новое поле схемы §4.1). |
| 3 | Скормить `roles: { dev_docs: skip }` (голый скаляр на уровне `<key>`). | Разобрано как `dev_docs: "skip"`, не как ошибка формата. |
| 4 | Скормить `lib/roles-resolve.js` фикстурный `roles:`-блок из нескольких ключей, зафиксированный как commit-нутый файл `tools/manual-test-ui/test/fixtures/prompt-roles-flow.md` (снимок реального однострочного flow-стиля, каким он написан в `prompt.md` этого репозитория на момент написания этого плана — не живой grep по `docs/issues/open/` при каждом прогоне), например:<br>`roles:`<br>`  brd: { write: claude, review: [codex] }`<br>`  specs: { write: codex, review: [claude], mode: blocking }`<br>`  code: { write: codex, review: [claude] }`<br>`  dev_docs: skip` | Все перечисленные `<key>` разобраны без исключений, с `write`/`review`/`mode`/голым-скаляром как на шагах 1-3 — набор фикстуры фиксирован коммитом, не зависит от текущего состояния `docs/issues/open/` в момент прогона теста (устраняет риск вакуумного прохождения при 0 совпадений или ложного падения от чужого issue в будущем). |

**Test Data:**
- `tools/manual-test-ui/test/fixtures/prompt-roles-flow.md` (committed fixture, не живой glob по `docs/issues/open/`)

**Expected Outcome:** Узкое, но полностью достаточное для сегодняшних `prompt.md` подмножество YAML разбирается корректно и воспроизводимо, на стабильном наборе фикстур.

**Priority:** High

---

### TC-022: `lib/roles-resolve.js` — задокументированное ограничение (multi-line block-style)

**Description:** Проверяет явно принятое ограничение specs.md §3.2 п.2 — запись `roles.<key>`, разбитая на несколько строк (block-style YAML), не гарантированно разбирается корректно; поведение при встрече такого случая должно быть предсказуемым (не тихая порча данных), а не крашем.

**Preconditions:**
- Фикстура `prompt.md` с `roles:`-блоком, где один `<key>` записан в multi-line block-стиле:
  ```yaml
  roles:
    specs:
      write: claude
      review: [codex]
  ```

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Скормить `lib/roles-resolve.js` эту фикстуру. | Парсер не бросает необработанное исключение и не завершает процесс сервера. |
| 2 | Проверить результат разбора для ключа `specs`. | Результат — либо явно помечен как нераспознанный/неполный для этого ключа, либо не содержит этот ключ вовсе; в любом случае не содержит вымышленных/частично разобранных значений `write`/`review`, которые выглядели бы как валидный, но неверный результат. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (fixture `prompt.md` с multi-line `roles.specs` блоком)

**Expected Outcome:** Ограничение задокументировано и ведёт себя предсказуемо — см. также Known Issues.

**Priority:** Medium

---

### TC-023: Операция → `human` — очередь вместо авто-выполнения, `mode` blocking/non-blocking; новая проверка непустого Result (AC-05c)

**Description:** Проверяет AC-05a/AC-05b — операция, зарезолвившаяся в `human`, не выполняется автоматически, а записывается в очередь с полями issue/стадия/операция/путь к артефакту/инструкция/статус; поддерживает `mode: blocking` и `mode: non-blocking` (дефолт), и `GET /api/projects/:name/issues/:id/human-tasks` отдаёт эти задачи. Отдельно проверяет AC-05c для ручных TC («UI не даёт закрыть кейс с пустыми результатами шагов») как **новое поведение, которое эта issue должна реализовать, а не существующую защиту**: прямая проверка кода показывает, что сегодня её нет — `lib/checklist.js`'s `patchStepResult(content, tcId, stepNum, { checked, note })` принимает `note || ""` без какой-либо проверки непустоты, `summarize()` считает только `checked` (не `note`), а маршрут `PATCH .../checklist/steps` в `server.js` валидирует только типы тела запроса (`tcId` — строка, `step` — число), не содержимое `note`. Понятия «закрыть кейс» в клиенте сегодня тоже не существует — есть только поштучная запись Result по шагам.

specs.md §4.4 утверждает обратное («существующая проверка непустого Result по шагам (`lib/checklist.js`) уже это делает — не меняется, только становится видна как часть инбокса») — это тот же ошибочный тезис, что и в прежней редакции этого TC, зафиксированный уже в самом specs.md. Это пробел спецификации, который реализация должна закрыть: `implementation_plan.md` заводит отдельную задачу на саму проверку, а не только на её показ в инбоксе. Поскольку specs.md не определяет отдельный API «закрытия» ручного TC (Manual TC явно не проходит через `/human-tasks`, см. §4.4), самый конкретный и проверяемый механизм — расширить существующий путь записи Result: `PATCH .../checklist/steps` отклоняет попытку записать `checked: true` с пустым (после `trim()`) `note`. При таком правиле кейс физически не может оказаться «закрытым» (все шаги отмечены) с пустыми результатами — цель AC-05c достигается на уровне единственной существующей точки записи, без изобретения отдельного «closed»-статуса, которого нет ни в специфике, ни в формате `manual_test_checklist.md`.

**Preconditions:**
- Issue с `prompt.md`, где `roles.specs.write` резолвится в `human` актора (`kind: human` в `agents.yml`).
- Отдельная issue-фикстура (или тот же чек-лист) с TC, у которого есть хотя бы 2 шага, для проверки шагов 5-6.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Резолвинг роли для ключа `specs` этой issue. | Возвращает объект вида `{ kind: "human", ... }`, не выполняет никакого письма/действия автоматически. |
| 2 | Выполнить `GET /api/projects/:name/issues/:id/human-tasks`. | Задача для `specs` присутствует со статусом `queued`, полями `stageKey, operation, mode, artifactPath, instruction, status`. |
| 3 | Проверить `mode`, когда `prompt.md` не указывает его явно. | `mode === "non-blocking"` (дефолт). |
| 4 | Установить в фикстуре `roles.specs.mode: blocking`, повторить `GET .../human-tasks`. | `mode === "blocking"` отражён в ответе. |
| 5 | (AC-05c, негативный кейс — новая проверка) `PATCH .../checklist/steps` с телом `{ tcId, step, checked: true, note: "" }` (пустой/только-пробельный `note`) для шага manual TC. | Запрос отклонён (`4xx`, например `422 empty_result`) — сервер не позволяет отметить шаг выполненным без непустого текста в Result. До реализации этой issue тот же запрос проходил бы (см. Description) — это фиксирует именно новое поведение, не регрессию существующей защиты. |
| 6 | (AC-05c, позитивный кейс) `PATCH .../checklist/steps` с тем же `tcId`/`step`, `checked: true, note: "ok"` (непустой Result). | `200`, шаг записан с `checked: true` и записанным `note` — путь успешной записи не задет новой проверкой. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (issue с `roles.specs` → `human`, вариант с explicit `mode: blocking`; чек-лист-фикстура с TC на 2+ шага для шагов 5-6)

**Expected Outcome:** Human-операция никогда не выполняется автоматически; режим блокировки читается корректно, дефолт — non-blocking; AC-05c проверена как новая функциональность на конкретном, существующем эндпоинте (`PATCH .../checklist/steps`), а не задекларирована как уже работающая.

**Priority:** Critical

---

### TC-024: `POST .../human-tasks/:key/complete` — валидация по типам (review / write-документ / write-код)

**Description:** Проверяет AC-05c/AC-05d/AC-05e и specs.md §4.4 — три разных пути проверки выполнения: human-review требует непустой `verdict` (422 `invalid_verdict`, «замечаний нет» — валидное значение); human-write документного ключа проверяет существование/не-заглушку (422 `artifact_missing`) и git-коммит через `git status --porcelain` (422 `artifact_not_committed`, включая `??`); human-write `code`/`tests` проверяет `git rev-list --count <parent>..issue/<ID> > 0` и `git diff --name-only` на путь вне `docs/issues/` (для `tests` — дополнительно совпадающий с `test/*.test.js`).

**Preconditions:**
- Issue-ветка `issue/<ID>` создана из родительской ветки, с фикстур-git-репозиторием (через `makeTempRepo`).
- `roles.specs.review` резолвится в `human` (тест использует ключ пайплайна `specs` для review-ветки — `:key` в этих эндпоинтах всегда один из ключей `roles:`, а не отдельное имя «review-задачи»).

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | `POST .../human-tasks/specs/complete` с телом `{}` (без `verdict`), при `roles.specs.review → human`. | `422 invalid_verdict`. |
| 2 | `POST .../human-tasks/specs/complete` с `{ verdict: "замечаний нет" }`. | `200 { status: "done", contentHash }` — непустой текст валиден независимо от содержания. |
| 3 | `POST .../human-tasks/user_docs/complete`, файл `user_docs.md` не существует/пуст. | `422 artifact_missing`. |
| 4 | Создать `user_docs.md` с содержимым, не закоммитить (оставить `??` в `git status --porcelain`), повторить `complete`. | `422 artifact_not_committed`, отдельно от `artifact_missing`. |
| 5 | Закоммитить `user_docs.md`, повторить `complete`. | `200 { status: "done", contentHash }` (sha256 файла). |
| 6 | `POST .../human-tasks/code/complete` на ветке без коммитов впереди родительской. | `422 artifact_missing` (`git rev-list --count` = 0). |
| 7 | Добавить коммит с файлом вне `docs/issues/`, повторить `complete` для `code`. | `200`, `contentHash` — sha1 `HEAD`, не файловый sha256. |
| 8 | Тот же коммит, но путь только внутри `docs/issues/`, повторить `complete` для `code`. | `422 artifact_missing` — коммит есть, но нет пути вне `docs/issues/`. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (`makeTempRepo` с issue-веткой, родительской веткой, управляемыми коммитами)

**Expected Outcome:** Каждая из трёх веток проверки (review/write-документ/write-код) даёт корректный код и корректное условие отказа/успеха.

**Priority:** Critical

---

### TC-025: Успешное завершение — хеш и маркер в `session-log.md`; переход в `stale`

**Description:** Проверяет specs.md §4.2/AC-05f/M5 — после успешного `complete` содержимое (или состояние ветки для `code`/`tests`) хешируется, маркер `[human-task done] <key> @ <ts> content-hash=<sha…>` пишется в `session-log.md` issue; если артефакт меняется после отметки, при следующем построении инбокса задача возвращается в очередь со статусом `stale`.

**Preconditions:**
- Задача `specs` уже отмечена выполненной (TC-024 шаг 5 как основа).

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Прочитать `session-log.md` issue после успешного `complete`. | Содержит строку `[human-task done] specs @ <ts> content-hash=<sha256>`, append-only (существующие строки не тронуты). |
| 2 | Изменить содержимое `specs.md` (любой байт) без повторной отметки. | Файл на диске больше не соответствует записанному `content-hash`. |
| 3 | Выполнить `GET /api/inbox` (пересчёт при построении). | Задача `specs` снова присутствует в `humanTasks[]`, со статусом `stale` (не `done`, не молча пропущена — M5 = 0 случаев «остался без возврата в очередь»). |
| 4 | Повторить `complete` после изменения. | Новый `content-hash`, новый маркер, задача снова `done`. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js`

**Expected Outcome:** Stale-переход работает симметрично «done» — задача не теряется молча ни в одну, ни в другую сторону.

**Priority:** High

---

### TC-026: `POST .../reassign` — построчная замена `write:` и её задокументированное ограничение

**Description:** Проверяет AC-05g и specs.md §4.3 — переназначение правит только подстроку `write: <old>` → `write: <new>` внутри найденной однострочной flow-записи `roles.<key>`, файл записывается байт-в-байт идентичным кроме этой подстроки, запись о переназначении добавляется в `session-log.md`; для multi-line block-style записи — явная ошибка (409/501-подобная) вместо порчи файла.

**Preconditions:**
- Issue с `prompt.md`, содержащим `roles: { specs: { write: human, review: [claude] } }` в однострочном flow-стиле.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Снять байт-снимок `prompt.md` до вызова. | Снимок сохранён для побайтового сравнения. |
| 2 | `POST .../human-tasks/specs/reassign` с `{ actor: "claude" }`. | `200 { status: "reassigned", actor: "claude" }`. |
| 3 | Сравнить новый `prompt.md` со снимком побайтово. | Единственное отличие — подстрока `write: human` → `write: claude` внутри строки `specs: { ... }`; `review`, `mode`, отступы, переносы строк, соседние комментарии не тронуты. |
| 4 | Прочитать `session-log.md`. | Append-only запись о переназначении (`specs`, старый актор, новый актор, таймштамп). |
| 5 | `POST .../human-tasks/:key/reassign` с телом, где `actor` отсутствует в `agents.yml`. | Отклонено (не проходит валидацию против `agents.yml`). |
| 6 | На issue, где `roles.specs` записан в multi-line block-стиле (см. TC-022 фикстуру), повторить `POST .../reassign`. | `409`/`501`-подобная ошибка с текстом про переназначение вручную; файл `prompt.md` не изменён (побайтовое сравнение со снимком до вызова). |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (issue с однострочным `roles.specs`, и отдельная issue с multi-line `roles.specs`)

**Expected Outcome:** Точечная замена работает для поддерживаемого формата и явно отказывает, не портя файл, для неподдерживаемого — см. также Known Issues.

**Priority:** High

---

### TC-027: `agents.yml` дефолт получает `human`; hard-stop на `kind: human` снят

**Description:** Проверяет AC-05h/AC-05i — автосоздаваемый `docs/planning/agents.yml` содержит `human: { kind: human, inbox: project-explorer }` рядом с `claude`/`haiku`/`codex`/`gemini`; резолвер `pf-roles/SKILL.md` больше не завершается ошибкой при `kind: human`, а передаёт `{ kind: "human", ... }` вызывающему коду.

**Preconditions:**
- `docs/planning/agents.yml` не существует в проекте-фикстуре (условие автосоздания).

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Grep дефолтного YAML-блока `agents.yml` в `skills/pf-roles/SKILL.md` §2. | Содержит строку `human: { kind: human, inbox: project-explorer }` наравне с существующими четырьмя акторами. |
| 2 | Инициировать первое обращение, требующее резолвинга актора (любой `pf-*`-скилл или `lib/roles-resolve.js` в проекте без `agents.yml`). | `agents.yml` создаётся автоматически с этой записью включённой — без ручного дописывания. |
| 3 | Grep `skills/pf-roles/SKILL.md` §2/§4 на старый текст hard-stop (`actor '<name>' is kind: human — not supported until ...`). | Текст, завершающий резолвер ошибкой при `kind: human`, удалён или переписан так, чтобы резолвинг `kind: human` больше не был hard-stop-условием (§2/§4 specs.md: «резолвер вместо ошибки возвращает `{ kind: "human", ... }`»). |
| 4 | Прогнать `lib/roles-resolve.js` на записи `human: { kind: human, inbox: project-explorer }`. | Возвращает структурированный объект `{ kind: "human", inbox: "project-explorer" }`, не бросает исключение. |

**Test Data:**
- `skills/pf-roles/SKILL.md`
- `tools/manual-test-ui/test/helpers/fixtures.js` (проект без `agents.yml`)

**Expected Outcome:** `human` доступен «из коробки», без ручной правки конфига; прежний hard-stop полностью снят, а не просто переформулирован.

**Priority:** High

---

### TC-028: `/pf-close` Phase 0 — human-task check; `/pf-autopilot` снимает cron при останове

**Description:** Проверяет specs.md §4.1a/AC-05b — новый пункт 4 внутри Phase 0 `pf-close`: для активной issue резолвятся все ключи пайплайна, отбираются резолвящиеся в `human`, для каждого проверяется отметка `[human-task done] <key> @ <ts> content-hash=<sha256>` в `session-log.md` (отсутствие или несовпадающий хеш = не выполнено); при хотя бы одном невыполненном — останов с перечислением ключей. `/pf-autopilot` при таком останове снимает cron-расписание и завершает отчётом, не повторяя попытку вхолостую.

**Preconditions:**
- Issue-фикстура с `roles.specs.write` → `human`, без отметки `[human-task done]` в `session-log.md`; QA report существует и PASS; ветка — `issue/<ID>`.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Смоделировать алгоритм §4.1a построчно (Read `prompt.md`, резолвинг `roles.<key>` по всем ключам пайплайна, фильтр по `kind: human`, поиск маркера в `session-log.md`) на фикстуре без отметки. | Проверка находит `specs` как невыполненный (маркер отсутствует). |
| 2 | Сверить с текстом `skills/pf-close/SKILL.md` — пункт 4 внутри Phase 0, после «QA report exists / verdict PASS / on correct branch», останавливающий при первом провалившемся пункте. | Пункт присутствует ровно в этом месте (Phase 0, не отдельная фаза), с сообщением, перечисляющим незакрытые ключи. |
| 3 | Добавить в фикстуру отметку `[human-task done] specs @ <ts> content-hash=<sha256>` с корректным хешем текущего `specs.md`. | Повторный прогон алгоритма §4.1a находит `specs` выполненным, Phase 0 п.4 проходит. |
| 4 | Изменить `specs.md` после отметки (не пересчитывая хеш), повторить прогон. | Ключ снова считается невыполненным (`stale`, хеш на диске ≠ записанный). |
| 5 | Grep `skills/pf-autopilot/SKILL.md` Step 2 (work loop). | Содержит явную обработку: если `/pf-close` останавливается из-за незавершённых human-задач — `CronDelete` и завершение отчётом, не повтор на следующем resume. |

**Test Data:**
- `skills/pf-close/SKILL.md`
- `skills/pf-autopilot/SKILL.md`
- `tools/manual-test-ui/test/helpers/fixtures.js` (issue-фикстура с human-задачей без/с отметкой, git-репозиторий)

**Expected Outcome:** Issue не закрывается, пока human-задачи не выполнены по факту; автопилот не крутится вхолостую при таком блоке.

**Priority:** Critical

---

### TC-029: Счётчик «Дела» на уровне 2 — issue-scoped сумма, стабильна при смене роли, пересчитывается при смене issue

**Description:** Проверяет AC-06a/AC-06b/specs.md §3.4 (второй раунд догфудинга: счётчик стал issue-scoped, был project-wide — см. эти документы для истории решения) — счётчик суммирует незакрытые дела по всем ролям **текущей issue** (не по всему проекту), не меняется при переключении мультивыбор-фильтра роли (виден в этом значении ещё до переключения), но пересчитывается — без повторного запроса `/api/inbox` — при переключении issue.

**Preconditions:**
- Проект с двумя открытыми issue: issue A имеет 2 `pending` manual TC, issue B — 1 human-задачу.
- `public/workspace.js` экспортирует (`export function countIssueTodos(...)`) чистую функцию, вычисляющую значение счётчика из ответа `/api/inbox`, `countIssueTodos(inboxResponse, projectName, issueId)` — **без параметра роли**: отсутствие такого параметра в сигнатуре — сама структурная проверка AC-06b (счётчик физически не может зависеть от роли, если функция её не принимает). Если сейчас вычисление вплетено в рендер и не выделено, задача на это должна быть в `implementation_plan.md`.
- Загрузка — динамическим `import()`: `await import(pathToFileURL(path.join(TOOL_DIR, "public", "workspace.js")).href)`. `public/workspace.js` — нативный ES-модуль (specs.md §2.3), не UMD — `require()`/`vm.runInContext` не годятся для его `export`-синтаксиса; `test/roles.test.js`'s `node:vm`-загрузка `lib/roles.js` не прецедент здесь (`lib/roles.js` — UMD, `public/workspace.js` — нет). Dynamic `import()` работает из CommonJS-тестового файла без флагов.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Вызвать `countIssueTodos(inboxResponse, "main", issueA)`, где `inboxResponse` — фикстура `/api/inbox` с 2 manual TC issue A + 1 human-задачей issue B, оба проекта `"main"`. Затем `countIssueTodos(inboxResponse, "main", issueB)`. | Первый вызов возвращает 2 (issue A's own manual TC — human-задача issue B не входит); второй возвращает 1 (issue B's own human-задача — manual TC issue A не входят). |
| 2 | Проверить сигнатуру `countIssueTodos` — сколько аргументов она принимает. | Ровно три (`inboxResponse`, `projectName`, `issueId`) — роль не участвует в вычислении вообще, поэтому переключение мультивыбор-фильтра роли физически не может изменить результат (AC-06b: виден в этом значении ещё до переключения). |
| 3 | Вызвать `countIssueTodos` с `projectName`/`issueId`, под которыми не числится ни одного дела. | Возвращает 0, не `undefined`/исключение. |
| 4 | Grep `public/workspace.js` на повторный вызов `fetch("/api/inbox")` при смене роли/`[Issue ▾]` внутри одного проекта. | Повторного запроса нет — переиспользуется один и тот же ответ, отфильтрованный на клиенте (specs.md §3.4); однако переключение issue пересчитывает значение из того же ответа с новым `issueId` — счётчик после переключения issue отличается, если у issue разное число дел. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (проект с двумя issue, разными типами незакрытых дел)

**Expected Outcome:** Счётчик — issue-scoped и роль-независимый, ровно как требует пересмотренный AC-06a (история решения — specs.md §3.4).

**Priority:** High

---

### TC-030: `looseSections` — рендер «Дополнительные заметки», включая защитный случай orphan `afterTc`

**Description:** Проверяет AC-07a/AC-07b/specs.md §2.4 — `parsed.looseSections` (уже сохраняемые `lib/checklist.js`) рендерятся в `.doc-panel` роли «Тестировщик» на экране `manual_test_checklist.md`, после последнего TC-панели, не внутри TC-блока; визуально отличны от `.panel` (пунктирная граница, фон на базе `--warn`, заголовок «Дополнительные заметки»); группируются по `afterTc`, в порядке `lineIndex`; элемент с `afterTc`, не совпадающим ни с одним отрендеренным TC, не отбрасывается.

**Preconditions:**
- `manual_test_checklist.md`-фикстура со строкой между двумя TC-блоками, не подходящей ни под одну известную метку (`lib/checklist.js` кладёт её в `looseSections`).
- Рендер чек-листа вынесен в чистую функцию, экспортированную для тестирования: `export function renderChecklistPanel(parsedChecklist)`, возвращающую **HTML-строку (или другое чисто сериализуемое значение) и только это** — не структуру DOM-узлов: под `node --test` в этом zero-dependency проекте нет `document.createElement`/любого DOM API, так что ветка контракта, требующая построения узлов, невыполнима здесь в принципе. Если сейчас рендер вплетён только в DOM-обработчик без выделенной функции, задача на это должна быть в `implementation_plan.md`.
- Загрузка — динамическим `import()`: `await import(pathToFileURL(path.join(TOOL_DIR, "public", "workspace.js")).href)` (предполагая, что `renderChecklistPanel` живёт в `public/workspace.js`, наследующем текущую логику рендера чек-листа, specs.md §2.4). `public/workspace.js` — нативный ES-модуль (specs.md §2.3) — `require()`/`vm.runInContext` не годятся для его `export`-синтаксиса; `test/roles.test.js`'s `node:vm`-загрузка `lib/roles.js` не прецедент здесь (`lib/roles.js` — UMD, `public/workspace.js` — нет). Dynamic `import()` работает из CommonJS-тестового файла без флагов.

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Выполнить `GET /api/projects/:name/issues/:id/checklist`. | Ответ включает `looseSections: [{ afterTc, lineIndex, text }]`, как и сегодня (сервер уже отдаёт это поле). |
| 2 | Вызвать `renderChecklistPanel(parsedChecklist)` с ответом шага 1; проверить порядок блоков в возвращённой строке по индексу вхождения подстроки (`String.prototype.indexOf`). | Блок `.loose-sections` идёт после последнего TC-блока (`.panel`) в возвращённой строке — не внутри какого-либо TC-блока; заголовок — «Дополнительные заметки», подпись «нераспознанный текст между блоками чек-листа — не тест-кейс». |
| 3 | Проверить CSS-класс блока. | Не `.panel` — отдельный класс (пунктирная граница, `color-mix(in srgb, var(--surface) 92%, var(--warn))` или эквивалент на `--warn`, не на `--accent`). |
| 4 | Проверить порядок элементов внутри блока при нескольких `looseSections` с разными `afterTc`. | Группировка по `afterTc`, порядок — по возрастанию `lineIndex` (порядок появления в файле). |
| 5 | Добавить в фикстуру элемент с `afterTc`, не совпадающим ни с одним TC-блоком чек-листа (искусственно рассинхронизированные данные). | Элемент всё равно отрендерен, в конце блока — не отброшен молча (переносит принцип `lib/checklist.js` «don't let it vanish silently» на UI). |
| 6 | Проверить, что каждая строка `text` отрендерена как есть, без markdown-разбора. | Read-only, без интерпретации разметки (Non-Goals: без ручного редактирования документов пайплайна через UI). |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (`manual_test_checklist.md` с нераспознанными строками между TC-блоками, включая случай с несуществующим `afterTc`)

**Expected Outcome:** Починка парсера от 2026-08-06 наконец видна пользователю, а не только присутствует в данных.

**Priority:** High

---

### TC-031: US-08 — отсутствие новых зависимостей, три состояния чек-листа, точечная запись

**Description:** Проверяет AC-08a/AC-08b/AC-08c/G7/M6 — редизайн не требует установки новых зависимостей сверх текущего состояния, переключение на ветку issue и три состояния чек-листа (`missing`/`branch-only`/`on-disk`) продолжают работать, и точечная запись Result/Notes не переписывает остальной документ.

**Preconditions:** не требуются (регрессионный набор поверх существующей функциональности).

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Проверить, появился ли `package.json`/`node_modules` в `tools/manual-test-ui/`. | Либо не появился вовсе, либо появился без внешних `dependencies` — `<script type="module">` не требует `package.json`-декларации (specs.md §7). |
| 2 | Запустить инструмент одной командой (`make test-ui`/эквивалент), без предварительного `npm install`. | Инструмент стартует. |
| 3 | Прогнать регрессионный сценарий трёх состояний чек-листа: документ отсутствует и на диске, и на ветке (`missing`); присутствует только на `issue/<ID>` (`branch-only`, с возможностью checkout по подтверждению); присутствует на диске (`on-disk`). | Все три состояния классифицируются как и раньше (`lib/docstate.js` не менялся содержательно), рендерятся внутри нового `.doc-panel`. |
| 4 | Через API `POST .../prepare` (или UI-эквивалент) выполнить checkout ветки issue по подтверждению. | Работает как прежде — `lib/git.js` не тронут (specs.md §7). |
| 5 | Через `lib/checklist.js` `patchStepResult`/`patchNotes` изменить один Result/Notes. | Изменена ровно одна строка файла; остальной документ (форматирование, прочие TC, `looseSections`) байт-в-байт не тронут. |

**Test Data:**
- `tools/manual-test-ui/test/helpers/fixtures.js` (репозиторий с issue в трёх состояниях чек-листа)

**Expected Outcome:** Редизайн навигации/оформления/инбокса не задел уже работающие гарантии инструмента.

**Priority:** High

---

### TC-032: Write-allowlist — новые эндпоинты не пишут ничего сверх заявленного; никакого мастера выбора актора не добавлено

**Description:** Проверяет BRD Non-Goals (точный allowlist автоматических записей: `roles.<key>.write` в `prompt.md` при переназначении — AC-05g; append-only запись о переназначении в `session-log.md` — AC-05g; запись Result/чекбоксов в `manual_test_checklist.md` — AC-05c/действующее поведение US-08c; плюс маркер `[human-task done]` в `session-log.md` — §4.2) и AC-05j (точка входа для назначения актора остаётся ручной правкой `prompt.md`, отдельного UI-визарда эта issue не добавляет). Расширяет существующий `test/readonly.test.js` на два фронта: маршрутную инвентаризацию (сегодня три разрешённых мутирующих семейства, плюс два новых POST-маршрута этой issue) **и** инвентаризацию git-аргументов в `lib/git.js` (сегодня `checkout`/`ls-tree`/`rev-parse`/`show`/`status`, TC-012 шаг 4 в `test/readonly.test.js`), которую эта issue расширяет пятью новыми обёртками (`git rev-list`, `git diff --name-only`, `git status --porcelain`, `git rev-parse`, `git config` — specs.md §4.4).

**Preconditions:**
- Существующий `test/readonly.test.js` (source-инвентаризация мутирующих маршрутов и git-аргументов в `server.js`/`lib/git.js` + поведенческая проверка через снимки рабочего дерева).

**Steps:**
| Step | Action | Expected Result |
|------|--------|-----------------|
| 1 | Прочитать `handleApi` в `server.js`, перечислить все `req.method !== "GET"` маршруты. | Ровно пять немутирующих-в-остальном семейств: три существующих (checklist patch, prepare, checkout) плюс `POST .../human-tasks/:key/complete` и `POST .../human-tasks/:key/reassign` — никакого шестого маршрута. |
| 2 | Прочитать `lib/git.js`, тем же построчным способом, что и `test/readonly.test.js` TC-012 шаг 4 (regex по литеральным первым аргументам каждого `tryGit(cwd, [...])`), перечислить все git-подкоманды — включая пять новых обёрток этой issue (`rev-list`, `diff --name-only`, `status --porcelain`, `rev-parse`, `config`, specs.md §4.4). | Полный набор подкоманд — ровно `checkout`, `ls-tree`, `rev-parse`, `show`, `status` (существующие) плюс `rev-list`, `diff`, `config` (новые); ни одна не входит в запрещённый список (`commit`/`push`/`merge`/`reset`/`clean`), и ни одна из пяти новых обёрток не строит аргумент из непровалидированного пользовательского ввода (`issueId` уже прошёл `ISSUE_ID_RE`, `:key` уже провалидирован против ключей `roles:`, `actor` — против `agents.yml`). |
| 3 | Снять fingerprint рабочего дерева репозитория (как уже делает `test/readonly.test.js` для существующих трёх действий), вызвать `POST .../human-tasks/:key/complete` (успешный путь) для документного ключа. | Diff снимков показывает изменение ровно двух файлов: артефакт-документ (уже существовал, не создан заново) и `session-log.md` (добавлена ровно строка `[human-task done] ...`) — ничего больше. |
| 4 | Тот же снимок/diff для `POST .../human-tasks/:key/reassign`. | Diff показывает изменение ровно `prompt.md` (только подстрока `write:`, см. TC-026) и `session-log.md` (append-only запись о переназначении) — ничего больше. |
| 5 | Прогнать `POST .../human-tasks/:key/complete`/`.../reassign` против каждого pipeline-документа, которого Non-Goals не называет (например произвольная попытка через `artifactPath` затронуть `brd.md` при незапрошенном ключе). | Запись ограничена ровно тем артефактом, что резолвится для данного `:key` — нет способа затронуть произвольный файл issue через эти эндпоинты. |
| 6 | Grep `public/*.js` на UI-элемент выбора актора вне действия «отдать агенту» на уже существующей human-задаче (например отдельный визард создания/переназначения роли из произвольного места интерфейса). | Такого элемента нет — назначение актора остаётся точечной правкой `prompt.md` владельцем, кроме единственного действия «отдать агенту» (AC-05g), которое уже покрыто TC-026 (AC-05j). |

**Test Data:**
- `tools/manual-test-ui/test/helpers/snapshot.js` (существующий helper для fingerprint/diff рабочего дерева)
- `tools/manual-test-ui/server.js`
- `tools/manual-test-ui/lib/git.js`

**Expected Outcome:** Allowlist мутаций остаётся исчерпывающим и явным после добавления human-actor эндпоинтов; отдельного UI-визарда назначения актора не появилось.

**Priority:** High

---

## Status Tracker

| TC     | Test Case | Type   | Priority | Status | Remarks |
| ------ | --------- | ------ | -------- | ------ | ------- |
| TC-001 | Уровень 1 (лаунчер) — состав экрана и источник данных | Auto | High | ✓ | |
| TC-002 | Уровень 2 — рабочее пространство без сайдбара, `.doc-tabs` по объединению выбранных ролей | Auto | High | ✓ | |
| TC-003 | Переключение issue сохраняет активный таб документа в рамках роли | Auto | High | ✓ | |
| TC-004 | Клик-путь проект → документ — не больше двух/трёх кликов | Auto | High | ✓ | |
| TC-005 | Страница скроллится штатно | Auto | High | ✓ | |
| TC-006 | Автотест контраста `test/contrast.test.js` | Auto | Critical | ✓ | |
| TC-007 | Токены `:root` — точное соответствие палитре Pine | Auto | High | ✓ | |
| TC-008 | Захардкоженные цветовые литералы заменены токенами | Auto | High | ✓ | |
| TC-009 | Соотношение размеров `h1`/body — 1.3–1.8x | Auto | Medium | ✓ | |
| TC-010 | `.panel-table` — мягкая тональная заливка вместо жёстких grid-линий | Auto | High | ✓ | |
| TC-011 | Sans-serif для прозы, monospace только для кода | Auto | Medium | ✓ | |
| TC-012 | Визуальная приёмка панельной раскладки по референсу | Manual | High | [ ] | Manual reason: human-judgment |
| TC-013 | Owner sign-off палитры и типографики до старта `/pf-execute` | Manual | Critical | ✓ | Подтверждено по существующей записи `[owner sign-off]` в session-log.md @ 2026-08-17T15:50:14Z (до первого /pf-execute) — см. manual_test_checklist.md |
| TC-014 | `:focus-visible` — контраст индикатора и нативные `<button>` | Auto | Medium | ✓ | |
| TC-015 | Живая проверка табуляции и видимого фокуса по всему приложению | Manual | High | [ ] | Manual reason: environment — нет headless-браузерного харнесса в этом zero-dependency проекте |
| TC-016 | `GET /api/inbox` — агрегация across projects, форма ответа | Auto | Critical | ✓ | |
| TC-017 | Парсинг `Origin` для `manualTests[].issueId` — обязательность и защита | Auto | High | ✓ | |
| TC-018 | Инбокс — два раздела из одного ответа `/api/inbox` | Auto | High | ✓ | |
| TC-019 | Карточка глобального инбокса на лаунчере — суммарное число дел | Auto | High | ✓ | |
| TC-020 | Обязательные поля каждого элемента инбокса | Auto | High | ✓ | |
| TC-021 | `lib/roles-resolve.js` — разбор ограниченного flow-YAML подмножества | Auto | High | ✓ | |
| TC-022 | `lib/roles-resolve.js` — задокументированное ограничение (multi-line block-style) | Auto | Medium | ✓ | |
| TC-023 | Операция → `human` — очередь вместо авто-выполнения, `mode` blocking/non-blocking; новая проверка непустого Result (AC-05c) | Auto | Critical | ✓ | |
| TC-024 | `POST .../human-tasks/:key/complete` — валидация по типам | Auto | Critical | ✓ | |
| TC-025 | Успешное завершение — хеш и маркер в `session-log.md`; переход в `stale` | Auto | High | ✓ | |
| TC-026 | `POST .../reassign` — построчная замена `write:` и её ограничение | Auto | High | ✓ | |
| TC-027 | `agents.yml` дефолт получает `human`; hard-stop на `kind: human` снят | Auto | High | ✓ | |
| TC-028 | `/pf-close` Phase 0 — human-task check; `/pf-autopilot` снимает cron | Auto | Critical | ✓ | |
| TC-029 | Счётчик «Дела» на уровне 2 — issue-scoped сумма, стабильна при смене роли, пересчитывается при смене issue | Auto | High | ✓ | |
| TC-030 | `looseSections` — рендер «Дополнительные заметки», включая orphan `afterTc` | Auto | High | ✓ | |
| TC-031 | US-08 — отсутствие новых зависимостей, три состояния чек-листа, точечная запись | Auto | High | ✓ | |
| TC-032 | Write-allowlist — новые эндпоинты не пишут ничего сверх заявленного; нет мастера выбора актора | Auto | High | ✓ | |

## Known Issues

| Issue | Description | TC Affected | Steps to Reproduce | Severity |
| ----- | ----------- | ----------- | ------------------ | ------- |
| `lib/roles-resolve.js` не поддерживает multi-line block-style `roles.<key>` | specs.md §3.2 п.2 явно фиксирует это как принятое ограничение (не дефект): запись `roles:` → `<key>:`, разбитая на несколько строк, не гарантированно разбирается корректно. Полноценный YAML-парсер — вне скоупа «нулевых зависимостей». | TC-022 | Создать `prompt.md` с `roles:\n  specs:\n    write: claude\n    review: [codex]` (multi-line), скормить `lib/roles-resolve.js`. | Low (принятое ограничение) |
| `POST .../reassign` отказывает на multi-line block-style `roles.<key>` | specs.md §4.3: точечная построчная замена работает только для однострочного flow-стиля; для multi-line — явная `409`/`501`-подобная ошибка вместо порчи файла. Тот же корневой YAML-пробел, что и у чтения. | TC-026 | На issue с multi-line `roles.specs` вызвать `POST .../human-tasks/specs/reassign`. | Low (принятое ограничение) |
| `lib/inbox.js` зависит от формата Status Tracker `20260806-feat-product-test-plan/specs.md` | specs.md §8: если формат колонки `Origin`/Status Tracker в `docs/planning/test-plan.md` изменится в будущем (другой issue), `lib/inbox.js` нужно свериться с актуальной версией того specs.md, а не с текстом этого документа — иначе парсинг `manualTests[]` (TC-016/TC-017) молча разойдётся с реальным форматом файла. | TC-016, TC-017 | Сравнить формат колонки `Origin`, ожидаемый `lib/inbox.js`, с текущим `20260806-feat-product-test-plan/specs.md` при следующей ревизии того документа. | Low (задокументированный риск дрейфа) |
| `tools/manual-test-ui/test/` содержит известный недетерминированный сьют | BRD Non-Goals: `prepare.test.js`/`read-paths.test.js` (TC-011 старой нумерации) флакуют независимо от этой issue — падение любого из них при прогоне `make test`/`node --test` не следует принимать за регрессию, введённую этим редизайном. | Косвенно затрагивает любой TC, прогоняемый через `node --test` в общем сьюте (TC-006–TC-011, TC-016–TC-031) | Прогнать `node --test` несколько раз подряд без изменений в коде — `prepare.test.js`/`read-paths.test.js` иногда падают без связанных правок. | Note (не баг этой issue) |
| Текст hard-stop в `pf-roles/SKILL.md` ссылается на устаревший issue-id | На момент написания этого плана `skills/pf-roles/SKILL.md` §2 содержит сообщение об ошибке `"actor '<name>' is kind: human — not supported until 20260806-improve-project-explorer-redesign"` — ссылается на более раннее имя этой же issue (`improve-...`), тогда как фактический id — `20260806-feat-project-explorer-redesign`. TC-027 должен убедиться, что при снятии hard-stop это упоминание тоже устранено (не просто исправлено на новый id, раз сам hard-stop уходит). | TC-027 | `grep -n "20260806-improve-project-explorer-redesign" skills/pf-roles/SKILL.md` | Low |
| `lib/checklist.js`'s `parseChecklist()` вносит ложные `looseSections` | Обнаружено при реализации Задачи 26: ветки Prerequisites/Test Data/таблицы Steps продвигают только внутренний цикл, не внешний — те же строки повторно проходят через внешний цикл и попадают в `looseSections` второй раз. На реальной фикстуре (2 TC, 3 шага) — 12 ложных записей, 0 подлинных. `renderChecklistPanel` (Задача 26) корректно отображает всё, что возвращает парсер («don't let it vanish silently»), поэтому TC-030 проходит по своему буквальному контракту (рендер того, что отдал парсер) — но при этом «Дополнительные заметки» будет зашумлён на любом корректно оформленном чек-листе, пока баг в `lib/checklist.js` не исправлен. `specs.md` §1 явно выводит `lib/checklist.js` за скоуп этой issue (содержательно нетронут, кроме точечной правки Задачи 9) — фикс требует отдельной issue/bug. | TC-030 | Распарсить любой реальный `manual_test_checklist.md` с ≥1 TC и ≥1 шагом через `parseChecklist()`, посчитать `looseSections.length` — должно быть 0 при корректном документе, на практике не 0. | Medium (искажает видимое поведение AC-07a, но не ломает функционально — требует отдельной issue) |
| `workspace.js`'s `docCache` может воспроизвести устаревшую отклонённую промису | Обнаружено при реализации Задачи 28: `selectIssue()` перерисовывает синхронно с ещё закэшированным табом/документом прежней issue до того, как `loadRoleContents()` разрешится; если у прежнего документа был статус `present`, этот переходный рендер может запросить `/checklist` новой issue по старому guard'у и получить `404`. `fetchDoc()`'s `docCache` мемоизирует именно промис (включая отклонённые), по одному ключу — `endpoint`, без инвалидации; если документ новой issue появится позже в рамках того же mount (например, после `/pf-test` и последующего `refresh()`), закэшированный отклонённый промис будет воспроизведён вместо повторного запроса. Подтверждено чтением кода, не воспроизведено как реальный сбой — `docCache`/`selectIssue` — код Задач 24/26, вне скоупа тестовой Задачи 28. | Косвенно TC-002/TC-003 (переключение issue/роли) | Смонтировать `workspace.js`, переключить `[Issue ▾]` на issue с ещё не готовым документом (гонка `loadRoleContents`), затем сделать документ доступным и вызвать `refresh()` в рамках того же mount — проверить, воспроизводится ли устаревшая ошибка вместо нового содержимого. | Low (узкий гоночный кейс, не воспроизведён; требует отдельной issue при подтверждении) |
