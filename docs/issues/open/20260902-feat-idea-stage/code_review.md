# Code Review Report

**Issue ID:** 20260902-feat-idea-stage
**Date:** 2026-09-03
**Reviewer(s):** Codex
**Rounds:** 4 (budget 3 — exhausted since round 3; every further round escalates to `/pf-execute` instead of looping in-review)

Диспетчеризация: роль `code` резолвлена через `profile: claude-writes-codex-reviews`
→ `review: [codex]`, tier `sol` (оба окна провайдера `idle` на момент вызова —
деградация тира не потребовалась). Форма вызова — code-diff
(`review --wait --scope branch --base develop`), PARENT-BRANCH определён
`develop` после срабатывания self-tracking upstream guard. Дифф раунда 1:
82 файла, +5955/-131.

Codex вернул готовые метки `[P0]`/`[P1]`/`[P2]`, а не schema-JSON с полем
`severity`, поэтому отображение severity→priority не применялось — приоритеты
взяты как названы.

---

## Findings Ledger

| ID | Round | Priority | Description | Follow-up Issue | State |
|----|-------|----------|--------------|------------------|-------|
| CR-001 | 1 | P1 | `skills/pf/SKILL.md:487-489` — прямой intake спайка («Run a technical spike») собирает все поля, кроме `idea_tier`, хотя скелет `prompt.md` его требует, а `/pf-idea-spike` и `/pf-check` читают его для выбора бюджетов документов. Любой созданный напрямую спайк получает невалидный `prompt.md` либо вынужден угадывать значение. |  | fixed |
| CR-002 | 1 | P1 | `skills/pf-close/SKILL.md:279` — при вердикте `spike-first` follow-up `prompt.md` спайка формируется без `idea_tier`, хотя схема спайка его требует и `/pf-idea-spike` сразу читает его для бюджетов `hypothesis.md`/`findings.md`. Каждый автоматически порождённый спайк выходит невалидным; тир исходной идеи должен наследоваться рядом с `idea_ref`. |  | fixed |
| CR-003 | 1 | P1 | `skills/pf-idea-verdict/SKILL.md:231-236` — при переопределении допущения, впервые использованного в `idea.md`, дописываются `OPEN`-маркеры на весь хвост пайплайна, и `/pf` маршрутизирует назад к проверке IDEA. Но `/pf-check` выбирает только самый недавно произведённый документ (`verdict.md`) и не может снять более ранний маркер на `idea.md` — воркфлоу застревает без выхода. |  | fixed |
| CR-004 | 1 | P1 | `skills/pf-interaction/SKILL.md:14-16` — точная схема строк `open_questions.md` делегирована в `specs.md` этого issue, но в пользовательские установки копируются только каталоги `skills/`, и этого документа там нет. Стадия, первой создающая реестр, остаётся без определения заголовка таблицы, а поздние стадии зависят от именованных колонок (`Status`, `Used in`). |  | fixed |
| CR-005 | 1 | P1 | `skills/pf-close/SKILL.md:118-120` — Phase 0 не отвергает произвольную третью ветку для спайка, а Phase 3.5 переключается на PARENT-BRANCH только при условии «текущая ветка = `issue/<spike-id>`». Стартовав с посторонней ветки, копирование документов, архивация и коммит Phase 8 лягут на неё, а настоящий родитель останется незакрытым. |  | fixed |
| CR-006 | 1 | P2 | `skills/pf-idea-critique/SKILL.md:13-16` — предпосылочная проверка на существование файла пропускает непустую заглушку, если `/pf-idea-critique` вызван сразу после прерванной записи `research.md`; следует применить общий критерий завершённости стадии, как уже делает `/pf-idea-research`. Аналогичная проверка в `/pf-idea-verdict` тоже ослаблена. |  | open |
| CR-007 | 1 | P2 | `skills/pf-close/SKILL.md:415` — для git-backed закрытий idea/spike `NO-REPO` ложен, и неизменённый отчёт по-прежнему утверждает, что добавлено два коммита и один из них `--no-ff` merge. Оба новых пути пропускают Phase 4 и создают только архивный коммит, поэтому каждое успешное закрытие отчитывается неверно. |  | open |
| CR-008 | 1 | P0 | `skills/pf-interaction/SKILL.md:95` — text-REPL адаптер включается по условию «резолвнутый `write` — `codex`», то есть по роли автора документа, тогда как наличие `AskUserQuestion` определяется рантаймом текущей сессии. Claude-сессия с `write: codex` необоснованно уйдёт в text-REPL, а Codex-сессия с `write: claude` вызовет отсутствующий инструмент. В `pf-idea-verdict` противоречие явное: Режим 2 исполняется текущей сессией независимо от write-актора. Измерение TC-029. |  | fixed |
| CR-009 | 1 | P0 | `skills/pf-interaction/SKILL.md:115` — маркер pending-state предписано вставлять «непосредственно перед `## Decision`», хотя Режим 2 запускается ровно при отсутствии этой секции (она его результат) — якоря в этот момент не существует. Схема маркера также не задаёт значения `stage-key`, текст вопроса, подшаг и выбранный объект, а закрытие маркера допускает две несогласованные стратегии (удалить либо пометить `resolved`). Измерение TC-029. |  | fixed |
| CR-010 | 1 | P1 | `skills/pf/SKILL.md:35` — ветка «сразу проект» разворачивает только `CLAUDE.md`. Codex строит проектный контекст из `AGENTS.md`, поэтому созданный этим issue проект теряет правила фреймворка под Codex. Это НОВАЯ несовместимость, вносимая данным issue, и в таблице `specs-part3` §9 её нет — AC-13b требует, чтобы ни одна строка не была пропущена молчанием. Измерение TC-029. |  | fixed |
| CR-011 | 1 | P1 | `skills/pf-idea-spike/SKILL.md:109` — строка `WebSearch`/`WebFetch` в §9 перечисляет только `pf-idea-research`, тогда как новый `pf-idea-spike` прямо требует `WebFetch` для эксперимента без кода и предполагает наличие `Bash` у оркестрирующей сессии; имя `Bash` в таблице §9 не фигурирует вовсе. Внесённая этим issue несовместимость пропущена. Измерение TC-029. |  | fixed |
| CR-012 | 1 | P2 | `skills/pf-idea-critique/SKILL.md:105` — обоснование последовательного Codex-пути опирается на утверждение, что у Codex нет оркестрирующего примитива, эквивалентного `Agent`; актуальная документация описывает параллельные subagents с управлением concurrency. Последовательный путь остаётся допустимым как консервативный fallback, но не как следствие отсутствия возможности. |  | open |
| CR-013 | 1 | P2 | `specs-part3.md:293` — §9 направляет будущий `SKILLS_ROOT`-резолвер только к `~/.codex/skills`; актуальная документация называет репозиторный `.agents/skills`, пользовательский `$HOME/.agents/skills` и plugin-discovery. Статус «принято как ограничение» сохраняется, но описание цели резолвера устарело. |  | open |
| CR-014 | 2 | P0 | `skills/pf-close/SKILL.md:101` — исправление CR-005 закрыло только архивный коммит: Phase 0 теперь разрешает старт с любой ветки, а безусловный checkout PARENT-BRANCH выполняется лишь в Phase 3.5. До него Phase 2 делает `git add -A` и коммит на текущей ветке, поэтому при закрытии спайка с посторонней ветки и непустом дереве на ней останется `chore: pre-close cleanup`. |  | fixed |
| CR-015 | 2 | P0 | `skills/pf/SKILL.md:456` — исправление CR-008 покрыло адаптером только два контентных intake-батча. До них `/pf` безусловно вызывает `AskUserQuestion` для выбора типа работы, языка и подтверждения спайка; после — для назначения ролей и `on_unavailable`; подтверждение идеи, извлечённой из файла, тоже осталось обычным вызовом. В non-Claude-сессии исполнение упрётся в отсутствующий инструмент до или после участка, где адаптер включается. |  | fixed |
| CR-016 | 2 | P0 | `skills/pf/SKILL.md:74` — исправление CR-009 создаёт intake-черновик `docs/issues/open/.pf-intake-draft-<type>.md`, но стартовый маршрут `/pf` собирает только подкаталоги вида `YYYYMMDD-TYPE-SLUG` и черновик не видит. После прерывания новая сессия заново спросит тип работы и начнёт intake с нуля, затерев существующий маркер — прямое нарушение правила «проверить открытый маркер до любых других действий». |  | fixed |
| CR-017 | 2 | P0 | `skills/pf-idea-verdict/SKILL.md:52` — исправление CR-003 сняло тупик, но условие входа в Режим 2 по-прежнему смотрит только на последний маркер `verdict.md`. Выполнив сначала `/pf-check verdict.md`, пользователь откроет сессию решения, даже если у `idea.md`/`research.md`/`critique.md` остались более поздние `OPEN` от того же override — это противоречит инварианту, введённому тем же исправлением ниже по файлу. |  | fixed |
| CR-018 | 2 | P1 | `skills/pf-interaction/SKILL.md:198` — для final-gate маркер предписано писать в `open_questions.md`, но `pf-close` явно допускает отсутствие этого файла (обычный случай для спайка и для issue без допущений). Не сказано, создавать ли его и должен ли он нести канонический заголовок и таблицу: реализация либо не сохранит состояние, либо создаст marker-only файл, не соответствующий только что введённой схеме. |  | fixed |
| CR-019 | 2 | P1 | `skills/pf-interaction/SKILL.md:260` — единственная стратегия закрытия пишет произвольный ответ прямо в однострочный HTML-комментарий как `status=resolved:<answer>`. Подшаг `override-answer` допускает свободный текст: перевод строки или последовательность `-->` разорвут маркер, и ни `status`, ни ответ, ни граница комментария больше не восстанавливаются однозначно. |  | fixed |
| CR-020 | 3 | P0 | `skills/pf/SKILL.md:29` — один ключ `step=type` обслуживал два несовместимых вопроса: развилку состояния папки в Step 0 (два варианта) и выбор типа issue в Step 3 (четыре). Холодный старт отправлял `type`/`type-confirm` к вопросу Step 0, хотя `type-confirm` существует только в Step 3. Вдобавок в черновике не сохранялось происхождение intake (bare-folder Step 0 или existing-project Step 3), от которого зависит carve-out по `roles`/`on_unavailable`. Черновик находился, но однозначно возобновиться по нему было нельзя. |  | fixed |
| CR-021 | 3 | P0 | `skills/pf-close/SKILL.md:107` — исправления CR-014 и CR-018 конфликтовали на пути спайка. Non-Claude Phase 1 писала final-gate маркер до Phase 2: на чистой посторонней ветке эта запись сама делала дерево грязным, и guard останавливал закрытие — защита блокировала сценарий, который должна была защитить. При старте с PARENT-BRANCH Phase 3.5 могла перезаписать только что созданный `open_questions.md` версией со spike-ветки. |  | fixed |
| CR-022 | 3 | P1 | `skills/pf-interaction/SKILL.md:365` — передача от type-agnostic черновика к типизированному имела окно полной потери состояния: `.pf-intake-draft-pending.md` удалялся раньше, чем создавался типизированный черновик. Прерывание в этом окне оставляло систему без открытого маркера, и следующий `/pf` начинал intake заново. |  | fixed |
| CR-023 | 3 | P1 | `test/pf-idea-semantic-static.sh:112` — семантический сьют проверял НАЛИЧИЕ ФОРМУЛИРОВОК, а не поведение: `assert_adapter_covers` считал вопрос защищённым по одной фразе в широком диапазоне, без привязки к конкретному вызову `AskUserQuestion`. Можно было удалить адаптеры двух контентных батчей, сломать выбор черновика или инвертировать branch-условие — сьют оставался зелёным. Он уже пропустил CR-020 и CR-021. |  | fixed |
| CR-024 | 3 | P2 | `test/pf-idea-semantic-static.sh:297` — три property-ассерта секции CR-019 истинны по построению: фикстура буквально содержала `-->`; проверка перевода строки доказывала лишь непустоту строки; `tr -d 'n'` гарантировал успех независимо от кодировщика; `-->` алгебраически невозможен в алфавите base64. Заменены round-trip проверкой полного маркера. |  | fixed |
| CR-025 | 4 | P0 | `skills/pf-interaction/SKILL.md:420` — intake-FSM неоднозначен на переходах между маршрутами. Контракт утверждает, что `folder-mode` и `issue-type` «never both fire in the same intake sequence», но сам же описывает ответ «сразу проект», который проходит `folder-mode`, разворачивает каркас и затем неизбежно задаёт `issue-type`. При холодном возобновлении `folder-mode` выполняется до вычисления `has_git`, хотя разворот каркаса использует этот флаг для решения о `git init`; отдельного состояния и проверяемого чекпоинта завершённости каркаса нет. |  | open |
| CR-026 | 4 | P0 | `skills/pf-close/SKILL.md:44` — из восьми клеток «ветка × наличие маркера» уверенно закрыты четыре (без маркера) плюс обычное возобновление. В клетке «чистая посторонняя + открытый маркер» preflight делает checkout PARENT-BRANCH ДО возобновления и может унести маркер вместе с веткой. В клетке «грязная посторонняя + открытый маркер» он останавливает процесс, хотя грязь может состоять ровно из незавершённого маркера — это противоречит правилу «`status=open` → возобновить прежде любого другого действия». Кроме того, reconciliation только по `asked` может заменить только что разрешённый маркер более новым OPEN. |  | open |
| CR-027 | 4 | P0 | `test/pf-idea-semantic-static.sh:144` — блок назван проверкой конечного автомата, но реализация независимо ищет три фразы действий в общем диапазоне. Поменяв местами условия clean/dirty и оставив `no action`/`checkout`/`stop` в том же диапазоне, все три ассерта останутся зелёными. Измерение порядка сравнивает вычисление PARENT-BRANCH с guard'ом Phase 2, а не с чтением/записью существующего маркера; измерение «маркер есть/нет» отсутствует вовсе — поэтому сьют не ловит ни CR-025, ни CR-026. Утверждение «41 мутация проверена» в репозитории не воспроизводимо: манифеста или раннера нет. |  | open |
| CR-028 | 4 | P1 | `test/pf-idea-semantic-static.sh:198` — `assert_adapter_covers` делает два независимых поиска: фразы «Non-Claude orchestrator» и токена `step`/имени черновика в широком диапазоне. Замена `orchestrator_provider != claude` на `== claude`, запрет вместо применения адаптера или сохранение общей фразы рядом с безусловным `AskUserQuestion` оставляют проверки зелёными. `adapter_count=7` считает только упоминания. Аналогично CR-020 проверяется наличием предложения, а CR-022 — порядком заголовочных фраз, а не фактическими переходами и значениями `entry`. |  | open |
| CR-029 | 4 | P1 | `test/pf-idea-semantic-static.sh:501` — новый round-trip проверяет собственную конструкцию теста: `marker_template_line` — это grep ОДНОЙ физической строки с фрагментом `status=<open|resolved> answer=<base64|->`, полный HTML-маркер и остальные поля не извлекаются. После успешного grep проверка отсутствия переноса неизбежно истинна; проверка `status=resolved ` неизбежна после подстановки; сам ответ тест кодирует, подставляет, извлекает и декодирует своими же средствами. Эти три ассерта краснеют лишь каскадно при исчезновении исходной фразы. |  | open |

Строки CR-008..CR-013 получены вторым, обязательным для этого issue проходом —
измерением «совместимость с Codex» (AC-13a/AC-13b, TC-029). Встроенный ревьюер
Codex не принимает фокусный бриф, поэтому измерение выполнено отдельным
read-only вызовом по семи строкам таблицы `specs-part3` §9. Его вердикт:
TC-029 засчитывать нельзя до устранения CR-008/CR-009.

Реклассификация по Phase 2.5 не применялась: каждая из P0/P1-находок называет
конкретный сценарий отказа (невалидный `prompt.md`, застрявший воркфлоу,
отсутствующее определение схемы у потребителя, коммит не в ту ветку), поэтому
ни одна не понижена до P2.

---

## Verdict

**FAIL**

**Раунд 4.** Проверка закрытия находок раунда 3 — все пять закрыты лишь
частично:

| Находка | Итог | Остаток |
|---|---|---|
| CR-020 | частично | переход `folder-mode → каркас → issue-type` не имеет устойчивого состояния → CR-025 |
| CR-021 | частично | две из восьми клеток «ветка × маркер» не закрыты; reconciliation по одному `asked` может заменить только что разрешённый маркер → CR-026 |
| CR-022 | частично | окна без маркера нет, но переходная пара обрабатывается неидемпотентно → CR-025 |
| CR-023 | частично | конечный автомат и восемь клеток не моделируются; инверсии условий проходят → CR-027, CR-028 |
| CR-024 | не закрыта | round-trip извлекает однострочный фрагмент, а не полный маркер; три ассерта снова истинны по построению → CR-029 |

**Динамика находок по раундам: 13 → 6 → 5 → 5.** Убывание прекратилось.
Существенно, что все пять находок раунда 4 — это не новые дефекты, а
углубление в те же три области (intake-FSM, состояния ветки при закрытии
спайка, качество сьюта). Ревьюер каждый раунд проходит на уровень глубже в
одном и том же месте.

**О сьюте — второй раз подряд.** После переписывания (29 → 56 ассертов)
ревьюер вновь отвечает: барьером не является. Слабые ассерты названы
поимённо (все `assert_adapter_covers`, три состояния preflight, порядок
вычисления PARENT-BRANCH, `exactly 7 substitutions`, обе проверки CR-020,
порядок handoff CR-022, и три производных ассерта CR-024). Отдельно: в
репозитории нет воспроизводимого соответствия «мутация → ассерт → красный
вывод», поэтому заявление исполнителя о 41 практически проверенной мутации
**независимо подтвердить нельзя** — оно существует только в отчёте.

Открыто на конец раунда 4: 3 P0 + 2 P1 + 4 P2.

`make test` — 24 сьюта, 1082 passed, 0 failed. За четыре раунда ни один
зелёный прогон ни разу не совпал с отсутствием блокеров.
