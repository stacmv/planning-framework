# Manual Test Checklist

**Feature Name:** Устойчивость набора тестов Manual Test UI
**Issue ID:** 20260812-bug-flaky-manual-test-ui
**Date:** 2026-08-13
**Tester:**

---

## Как пользоваться

Этот чек-лист проведёт вас по ручной проверке возможности.

1. Прочитайте каждый раздел целиком до начала работы, чтобы понимать, чего ожидать.
2. Подготовьте всё перечисленное в предусловиях раздела. Если предусловие выполнить не удаётся — остановитесь и спросите разработчика, не продолжайте.
3. Выполняйте шаги по порядку. После каждого сверьтесь с колонкой «Ожидаемый результат» и запишите, что произошло на самом деле, в колонку «Результат».
4. Отметьте `[x]`, если шаг повёл себя как ожидалось, и оставьте `[ ]`, если что-то пошло не так.
5. В строке «Заметки» внизу раздела запишите всё необычное — даже если проверка прошла.
6. Закончив все разделы, верните документ разработчику.

---

## TC-003: Когда символическую ссылку создать можно, проверка защиты действительно срабатывает

**Предусловия:**
- Среда, где создание символических ссылок разрешено: Linux, macOS, либо Windows с включённым режимом разработчика или правами администратора. Машина, на которой готовился этот чек-лист (Windows без режима разработчика), предусловию **не** удовлетворяет.
- Рабочая копия проекта и установленный Node.js.
- Подготовленные данные: `C:\Users\Stac\AppData\Local\Temp\pf-test-data\20260812-bug-flaky-manual-test-ui\TC-003`

**Требуемые данные:**
- `tools/manual-test-ui/test/read-paths.test.js`

**Шаги:**

| Шаг | Действие | Ожидаемый результат | Результат |
|------|--------|-----------------|--------|
| 1 | Выполнить `node --test tools/manual-test-ui/test/read-paths.test.js`. | Проверка с названием «step 2: a symlink pointing out of the project is refused» **не пропущена** — рядом с ней нет пометки `SKIP`. | [ ] |
| 2 | Посмотреть исход этой проверки. | Отмечена как пройденная, а не пропущенная. | [ ] |
| 3 | Открыть её содержимое и убедиться в трёх утверждениях: запрос файла `outside.md` получает отказ (код ответа 4xx); тело ответа не содержит текста `OUTSIDE-SECRET-MARKER`; функция проверки принадлежности пути возвращает «не внутри проекта». | Все три условия выполнены. | [ ] |

**Заметки:**

---

## TC-006: На Linux путь остаётся в исходном виде, без принудительного преобразования

**Предусловия:**
- Доступна среда Linux или WSL, где набор запускается нативно, а не через Git Bash на Windows.
- Рабочая копия проекта.

**Требуемые данные:** не требуются

**Шаги:**

| Шаг | Действие | Ожидаемый результат | Результат |
|------|--------|-----------------|--------|
| 1 | На Linux или в WSL выполнить `bash test/manual-test-ui.sh` целиком. В выводе найти две строки, начинающиеся с `----  TC-015:` — одна оканчивается на `tc015_repo (raw, from pf_mktemp_d) = ...`, другая на `projects.json path field = ...`. | Обе строки есть в выводе, и вторая содержит путь в привычной для Linux форме (через прямые слэши, без обратных). | [ ] |
| 2 | Сравнить значения путей из этих двух строк посимвольно. | Значения полностью совпадают — путь не преобразовывался. Это и есть наблюдаемое доказательство: сам вызов утилиты преобразования проверить нельзя, а совпадение значений — можно. | [ ] |
| 3 | В том же прогоне найти строки `TC-004 step 1` и `TC-004 step 2`. | Обе зелёные: сервер запустился и перечислил проект. | [ ] |

**Заметки:**

---

## TC-007: Проверка устойчивости результата по условиям запуска

**Предусловия:**
- Возможность запускать рабочую копию из каталогов на разных дисках (для Windows).
- Возможность задавать и снимать переменную окружения `TMPDIR`.

**Требуемые данные:** не требуются

**Шаги:**

| Шаг | Действие | Ожидаемый результат | Результат |
|------|--------|-----------------|--------|
| 1 | Зафиксировать точку отсчёта: 3 прогона `bash test/manual-test-ui.sh` и отдельно 3 прогона `node --test "tools/manual-test-ui/test/*.test.js"` из текущего рабочего каталога. Два уровня держать раздельно — у них разные причины падений, и смешение их скрывает. | Записан результат каждого из 3+3 прогонов. | [ ] |
| 2 | Повторить оба уровня из рабочего каталога на другом диске (например, `C:` вместо `D:`). | Результат совпадает с шагом 1. Расхождение означает, что где-то осталась зависимость от текущего диска. | [ ] |
| 3 | Повторить с явно заданным нестандартным `TMPDIR` и отдельно — с не заданным вовсе. | Результат от `TMPDIR` не зависит. | [ ] |
| 4 | **Только наблюдение, ничего не менять.** Для каждого прогона шагов 1-3, 5-6 зафиксировать простой проверкой существования (`[ -d /c/tmp ]`, `[ -d /d/tmp ]`), существовали ли каталоги `C:\tmp` и `D:\tmp` на момент прогона. **Не создавать и не удалять их**: они лежат вне проекта и могут содержать чужие данные. | Для каждого прогона записано, существовали ли эти каталоги. Ни один из них не создан и не удалён. | [ ] |
| 5 | Повторить на «замусоренном» временном каталоге (с остатками прошлых прогонов) и на чистом. | Результат от остатков не зависит. | [ ] |
| 6 | Прогнать серию не менее 10 подряд, и отдельно — несколько прогонов одновременно, параллельно. | Записано, стабилен ли результат на всей серии и меняет ли что-то параллельная нагрузка. **Если при параллельной нагрузке набор падений меняется — это ожидаемо и уже известно**: отметьте наблюдение и продолжайте, отдельного разбора здесь не требуется. | [ ] |
| 7 | Свести всё в таблицу «условие → совпадает или расходится с точкой отсчёта», отдельно для каждого из двух уровней. Для строки `C:\tmp`/`D:\tmp` вместо вердикта записать наблюдение из шага 4. | Таблица заполнена по каждому условию для обоих уровней; строка про `C:\tmp`/`D:\tmp` содержит наблюдение, а не вердикт. | [ ] |

**Заметки:**

---

## TC-008: Процедура различения «падало и раньше» и «сломалось из-за правки»

**Предусловия:**
- Доступны обе ветки: основная ветка проекта без проверяемой правки и ветка с правкой.
- Точка отсчёта из предыдущего раздела зафиксирована.

**Требуемые данные:** не требуются

**Шаги:**

| Шаг | Действие | Ожидаемый результат | Результат |
|------|--------|-----------------|--------|
| 1 | На основной ветке проекта, без проверяемой правки, выполнить `bash test/manual-test-ui.sh` не менее 2 раз подряд. Записать точный перечень падающих шагов (или его отсутствие). | Перечень записан для обоих прогонов. Если два прогона дали разные перечни — так и записать: это само по себе значимое наблюдение. | [ ] |
| 2 | На ветке с правкой выполнить тот же набор не менее 2 раз подряд. Записать точный перечень падающих шагов. | Перечень записан для обоих прогонов. | [ ] |
| 3 | Применить правило: падение относится к правке тогда и только тогда, когда оно отсутствует в перечне шага 1 и устойчиво присутствует в обоих прогонах шага 2. Падение, встречающееся в обоих перечнях, — существовавшее до правки. | Каждое зафиксированное падение отнесено к одной из двух категорий; неклассифицированных не осталось. | [ ] |
| 4 | Свести результат в таблицу «падение → категория (было раньше / внесено правкой)» и приложить к отчёту. | Таблица покрывает все падения из шагов 1 и 2 без пропусков и понятна без дополнительных пояснений. | [ ] |

**Заметки:**

---
