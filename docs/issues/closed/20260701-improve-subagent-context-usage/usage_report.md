# LLM Usage — 20260701-improve-subagent-context-usage

No usage data available for this issue.

- **Auto (Claude, from transcripts):** not computed — the issue folder was never committed to git, so no start timestamp could be determined to bound the transcript window. (This issue also skipped the normal issue-branch flow: work was committed directly to `develop` rather than an `issue/...` branch.)
- **Manual (other LLMs):** no `usage.md` was found in the issue folder — no non-Claude sessions were logged.

_This issue was archived without the QA/merge phases (no `issue/...` branch was created; changes were already committed and pushed to `develop` directly)._
