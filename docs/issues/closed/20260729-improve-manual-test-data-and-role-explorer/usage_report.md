# LLM Usage — 20260729-improve-manual-test-data-and-role-explorer

Окно: 2026-07-29T16:48:45+03:00 (первый коммит issue) → закрытие.
Источник: транскрипты `~/.claude/projects/-home-stac-dev-planning-framework/*.jsonl`, 13 файлов, дедупликация по `message.id`.

| Source | Model | Input tok | Output tok | Cache write | Cache read | Approx cost | Notes |
|---|---|---|---|---|---|---|---|
| auto | claude-opus-5 | 468 | 163,064 | 325,100 | 98,990,059 | ~$55.61 | 245 ответов главного цикла |
| — | субагенты (22 шт.) | — | — | — | — | **не выводится** | 2,632,521 токенов суммарно, см. ниже |
| manual | — | — | — | — | — | — | `usage.md` в папке issue отсутствует; работы не-Claude моделей не зафиксировано |

**Total approx cost (то, что поддаётся расчёту):** ~$55.61

Тарифы: `claude-opus-5` — $5.00 / $25.00 за Mtok (вход / выход); запись в кэш 1.25× входного тарифа, чтение 0.1×.

## Что в эту цифру не вошло

**Субагенты — 2,632,521 токен, стоимость не рассчитана.** По 22 запускам известна только суммарная величина `subagent_tokens` из уведомлений о завершении задач; разбивки на вход / выход / запись и чтение кэша нет, а тарифы за эти категории различаются в 50 раз (от $0.50 до $25 за Mtok). Оценка «в среднем» дала бы разброс от единиц до десятков долларов, поэтому цифра не приводится вовсе — правило отчёта прямо запрещает угадывать. Дополнительная сложность: три задачи волны 6 (Task 12, 13, 14 — 519,328 токенов) исполнялись на `claude-sonnet-5` по решению владельца, а тариф там иной.

Крупнейшие потребители среди субагентов: Task 14 (196,186), Task 13 (166,658), Task 11 (165,363), Task 8 (160,074), Task 10 (159,994).

## Оговорки к автоматической части

**Окно — эвристика, а не точная атрибуция.** Считается вся активность Claude Code в каталоге проекта между первым коммитом issue и закрытием. В это окно попали работы, к issue не относящиеся: доработка `/pf-autopilot` (коммит `5b779d4`) и исправление ложного срабатывания `qa-gates` (`839f941`). Обе выполнялись в той же сессии, поэтому цифру следует считать **верхней границей**.

**Доля чтения кэша — 98.99 млн токенов, 89% всей суммы ($49.50 из $55.61).** Это ожидаемо для длинной сессии с большим стабильным префиксом: чтение кэша тарифицируется в десять раз дешевле входа, и без него та же работа стоила бы существенно дороже.
