# LLM Usage — 20260806-improve-codereview-convergence

**Окно:** `2026-08-06T22:12:23+03:00` (первый коммит папки issue) → закрытие
`2026-08-13`.
**Источник:** 12 файлов транскриптов в
`~/.claude/projects/D--dev-planning-framework/`, записи `type == "assistant"`,
дедуплицированные по `message.id`.

| Source | Model | Responses | Input tok | Output tok | Cache creation | Cache read | Approx cost |
|---|---|---|---|---|---|---|---|
| auto | `claude-opus-5` | 1 049 | 4 294 | 1 037 371 | 5 463 314 | 484 526 995 | ~$322.85 |
| auto | `claude-sonnet-5` | 403 | 804 | 213 321 | 1 114 322 | 114 746 811 | ~$29.54 |
| auto | `<synthetic>` | 9 | 0 | 0 | 0 | 0 | — |

**Total approx cost:** ~$352.39

**Итого токенов:** input 5 098 · output 1 250 692 · cache creation 6 577 636 ·
cache read 599 273 806.

## Как посчитано

Ставки — из скилла `claude-api` (кэш таблицы моделей на 2026-06-24).

| Модель | Вход $/Mtok | Выход $/Mtok |
|---|---|---|
| `claude-opus-5` | 5.00 | 25.00 |
| `claude-sonnet-5` | 2.00 | 10.00 |

`claude-sonnet-5` посчитан по **вводной** цене ($2/$10 вместо базовых $3/$15) —
она действует до 2026-08-31, то есть на дату закрытия применима. По базовой
ставке sonnet-часть составила бы примерно $44.3, а общая сумма — около $367.

Множители кэша (`shared/prompt-caching.md`): чтение из кэша — 0.1× входной
ставки, запись в кэш — 2× при часовом TTL (в этой сессии он часовой).

**Основная статья расхода — чтение из кэша, а не генерация:** 599M токенов
кэш-чтений против 1.25M выходных. Это ожидаемо для очень длинной сессии: каждый
запрос перечитывает накопленную историю, но по цене 0.1× от входной ставки. На
чтение приходится примерно $265 из $352 — три четверти суммы. Отключение кэша
сделало бы те же 599M токенов десятикратно дороже.

## Три оговорки, каждая занижает или завышает по-своему

**1. Окно перекрывает предыдущую issue — цифры завышены.** Оно начинается
2026-08-06T22:12, а предыдущая issue этого репозитория
(`20260806-feat-product-test-plan`) закрылась 2026-08-12, то есть внутри окна.
Метод суммирует **всю** активность Claude Code в этом каталоге за период, а не
работу конкретной issue. Отчёт той issue показывал ~$208 за пересекающееся окно;
разность (~$144) ближе к реальной стоимости этой issue, но и она приблизительна,
потому что окна перекрываются не полностью.

**2. Расход субагентов, судя по всему, не попадает в эти транскрипты — цифры
занижены.** Строка `claude-sonnet-5` совпадает с отчётом предыдущей issue
**побайтово** по всем пяти счётчикам (403 ответа, 804 / 213 321 / 1 114 322 /
114 746 811). При этом в текущей issue было запущено более двадцати
sonnet-субагентов, и их суммарный расход по уведомлениям задач — порядка 3.5M
токенов. Последние вхождения строки `claude-sonnet-5` в транскриптах за сегодня
оказались текстом в содержимом сообщений, а не записями ассистента с расходом.

Вывод: строка sonnet — это **нижняя граница**, а не итог. Реальная стоимость
субагентской работы в отчёт не вошла.

**3. Не-Claude расход не зафиксирован.** Файла `usage.md` в папке issue нет.
Codex вызывался в этой issue многократно — на ревью BRD, тест-плана, плана
реализации и на четыре раунда код-ревью — но его расход в этот отчёт не попадает
и нигде не учтён.

_Все три оговорки означают, что итог `~$352` — не оценка стоимости этой issue, а
измеренная активность оркестратора в этом каталоге за окно. Как верхняя граница
он завышен перекрытием с предыдущей issue; как полная стоимость — занижен
непопаданием субагентов и Codex._
