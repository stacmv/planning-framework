# Code Review Report

**Issue ID:** 20260806-improve-codereview-convergence
**Date:** 2026-08-12
**Reviewer(s):** Claude + Codex
**Rounds:** 4 (budget 3; round 3 exhausted it and escalated to /pf-execute, round 4 closed the cycle)
**Scope:** `git diff develop...HEAD` — 38 файлов, +2961/−29

Оба ревьюера отработали независимо, находки не дедуплицировались и не
арбитрировались (требование BRD). Источник каждой находки помечен тегом.

**О переоценке важности.** Codex выдал все три свои находки как `[P2]`, при
собственной сводке на языке блокера («can cause valid workflows to fail or
invalid coverage gaps to be missed»). По рубрике AC-3.1 важность определяется
наличием конкретного сценария отказа, а не оценкой ревьюера — сценарий назван во
всех трёх, и все три подтверждены измерением по тексту и по коду. Переклассифицированы
вверх с указанием причины; исходная метка сохранена рядом. Это третий случай
занижения важности Codex'ом в этой issue.

---

## Findings

### P0 (Blocker)

**CR-001 [Claude] — `round > review_rounds` не определён: достижимое состояние без
ветки исполнения, нарушающее BR-7 и AC-1.3.**
`skills/pf-codereview/SKILL.md`, Phase 4.

Номер раунда выводится из ledger'а как «наибольший `Round` + 1», а ledger
append-only и никогда не сбрасывается — в том числе после эскалации на
`/pf-execute` и возврата обратно. Phase 4 при этом ветвится ровно на два случая:
`round < review_rounds` (fix-цикл) и `round == review_rounds` (исчерпание
бюджета). Случая `round > review_rounds` нет.

*Сценарий отказа:* бюджет по умолчанию 3. Раунд 3 заканчивается исчерпанием
бюджета при открытом P0 → эскалация, задачи в `implementation_plan.md`, возврат
на `/pf-execute`. После исправления `/pf-codereview` запускается снова; максимум
в ledger'е — 3, значит текущий раунд 4. Если раунд 4 снова находит открытый P0/P1
— ровно то, что происходило в `20260709-feat-dockerize` на раундах 5–9 — не
срабатывает ни `round < budget` (4 не меньше 3), ни `round == budget` (4 ≠ 3).
Исполняющему агенту нечего делать: неопределённое состояние либо цикл без
эскалации, то есть именно то, что BR-7 и AC-1.3 запрещают.

*Подтверждено измерением:* в Phase 4 присутствуют только `round <` и
`round equals`; `>` или `>=` не встречается. Дыра не покрыта и тестом —
`test/pf-codereview-rounds.sh` прогоняет `pf_cr_budget_decision` только на
`round=budget` и `round<budget`, фикстуры `round>budget` нет.

*Что поменять:* трактовать исчерпание бюджета как ветвь по умолчанию для любого
`round >= review_rounds`, а не только равного. Добавить фикстуру `round>budget` в
TC-003, иначе регрессия вернётся незамеченной.

**CR-002 [Claude] — маркер «check passed» пишется независимо от исхода гейта, и
`/pf` двигает пайплайн мимо незакрытого P0.**
`skills/pf-check/SKILL.md` (секция «Close the stage»), `skills/pf/SKILL.md`
(«Note on 'check passed'»).

Писатель: «Regardless of which of the three gate options above was chosen, and
regardless of autopilot, append the line below» — строка маркера при этом
буквально утверждает `check passed`. Читатель: «If this marker line is present …
the check counts as passed for that document — **no further condition needed**».

*Сценарий отказа:* ревьюер находит P0 в `brd.md`. Пользователь выбирает «Skip and
continue» — вариант, который `pf-check` для P0 не рекомендует, но и не запрещает.
`pf-check` всё равно дописывает `[pf-check PASSED] … brd.md @ …`. Следующий `/pf`
видит маркер и без дополнительных условий предлагает `/pf-spec`, хотя P0 открыт.
Симметрично для «I'll fix manually»: маркер пишется **до** того, как правка
внесена, то есть заведомо при непройденной проверке.

Это регрессия того самого класса, который устраняет issue («стадия рапортует об
успехе, не выполнив работу»), просто в новом месте: не через `code_review.md`, а
через `session-log.md` и `/pf`. Нарушены AC-5.3 и Acceptance issue-уровня п. 5.

*Почему это не поймал TC-021:* он проверяет, что читатель ссылается на явный
маркер, что писатель пишет его безусловно и что форматы совпадают — но не то,
что маркер отражает **исход** гейта, а не факт «стадия отработала».
Безусловность здесь и есть дефект, а тест требует её как свойство.

*Что поменять:* маркер обязан нести исход. `[pf-check PASSED]` — только когда
P0/P1 реально закрыты (чистый проход либо «Fix now»); для «I'll fix manually» и
«Skip and continue» с открытым P0/P1 — иной маркер, который читатель в `/pf` не
засчитывает как пройденную проверку. TC-021 расширить на это различие, иначе
исправление снова окажется непокрытым.

**CR-003 [Codex, повышено с P2] — Phase 3.6 предписывает операцию, которую
запрещает правило append-only Phase 3: выполнить оба требования невозможно.**
`skills/pf-codereview/SKILL.md`, Phase 3 (append-only, BR-8) против Phase 3.6.

Phase 3: «It never rewrites or deletes a previously written row's ID, round
number, **or description** — that identity is permanent once written».
Phase 3.6: «…and **append the new issue's ID or path into that same row's
Description cell**».

*Сценарий отказа:* раунд 2, находка P1 из раунда 1 выносится в follow-up issue.
Оператор обязан записать ссылку в Description — что BR-8 запрещает. Он либо
опускает ссылку, и тогда несвязанный `deferred` неотличим от молча выброшенной
находки (это запрещает US-2, и тот же абзац Phase 3.6 говорит об этом прямо),
либо перезаписывает описание и нарушает BR-8. Обе ветки нарушают заявленное
требование, третьей нет.

*Почему P0, а не P2:* назван вход, названо состояние, назван нарушаемый
инвариант — рубрика AC-3.1 выполнена полностью. Дефект делает документированный
рабочий путь неисполнимым.

*Что поменять:* отвести ссылке на follow-up issue **отдельное поле строки**
(Phase 3.6 в более ранней редакции сама предлагала «or a dedicated column») и
исключить это поле из списка неизменяемых в BR-8, либо разрешить в BR-8 явное
дописывание в Description как единственное исключение. Первое чище: тогда
неизменяемость описания остаётся абсолютной.

### P1 (Important)

**CR-004 [Claude] — drift-guard TC-002 (шаг 4) не способен покраснеть при неверном
пороге эскалации.**
`test/pf-codereview-rounds.sh`.

Проверка требует совместного присутствия «round 1», цифры `3` и слова `block` —
но ищет их по всему блоку Phase 3.5, а не рядом с самой формулировкой порога.
Первое предложение блока («This check runs immediately after **Phase 3** first
writes…») само поставляет цифру `3`.

*Сценарий отказа:* порог AC-1.2 меняется с 3 на 5 — guard остаётся зелёным,
потому что тройка берётся из «Phase 3», а не из правила. Ревьюер эмпирически
подтвердил это подменой текста на «5 or more». Регрессия самого важного числа в
US-1 не будет обнаружена никогда, при том что TC-002 помечен Critical.

*Что поменять:* сузить якорь до предложения с порогом — требовать цифру в
пределах небольшого окна от слова `blocking`, исключив вводную фразу с «Phase 3»
из области поиска.

**CR-005 [Codex, повышено с P2] — хелпер Check 1 не реализует оговорку для
ручных кейсов, которую документирует правило: оговорка не покрыта тестом.**
`test/pf-execute-completeness.sh` против Check 1 в `skills/pf-execute/SKILL.md`.

Правило: Check 1 сканирует незакрытые `- [ ]`, но строка, ссылающаяся на кейс с
`Type: Manual`, явно вне области проверки — ручной кейс не может быть закрыт
автоматическим прогоном. Хелпер же падает на первой же `- [ ]` независимо от
типа кейса; в его собственном комментарии написано «scans for any remaining
`- [ ]` line anywhere in the plan».

*Сценарий отказа:* корректный план с незакрытым критерием ручного кейса — а это
ровно план этой issue, где таких критериев четыре — по правилу гейт проходит, по
хелперу нет. Транскрипция расходится с транскрибируемым правилом, и оговорка,
внесённая как исправление P0 на стадии `/pf-check`, не проверяется ничем.

*Что поменять:* хелпер обязан смотреть `Type` кейса в Status Tracker
`test_plan.md`, как это делает Check 3, и пропускать строки ручных кейсов.
Добавить фикстуру с незакрытым ручным критерием.

**CR-006 [Codex, повышено с P2] — хелпер Check 3 берёт кейсы из заголовков
разделов, а правило говорит про Status Tracker.**
`test/pf-execute-completeness.sh`.

Правило: «For every TC row in `test_plan.md`'s Status Tracker». Хелпер:
`grep -oE '^### TC-[0-9]+'` — то есть заголовки разделов кейсов.

*Сценарий отказа:* кейс присутствует в Status Tracker (авторитетном источнике,
который читает и `/pf-test`), но его подробный раздел отсутствует, переименован
или локализован. Хелпер такой кейс не увидит и объявит обратное покрытие полным,
пропустив ровно тот пробел, ради которого Check 3 существует.

*Что поменять:* парсить строки Status Tracker, как это делает само правило.

**CR-007 [Claude] — утверждение «the single place this default is applied» стало
ложным из-за правки в другом месте диффа.**
`skills/pf-execute/SKILL.md`, Phase 2 missing-field fallback против Check 2 в
новой Phase 3.5.

Исходный текст объявляет Phase 2 единственным местом применения дефолта
«отсутствующий `Task Type` → `code`», мотивируя это тем, что двух механизмов для
одного правила быть не должно. Дифф вводит второе место: Check 2 применяется к
задачам с `Task Type: code` «или отсутствующим, per Phase 2's missing-field
default» — для другой цели, не для резолюции роли, а для выбора между Check 2 и
Check 2b.

*Сценарий отказа:* функционального отказа сегодня нет — Check 2 ссылается на
правило по имени, а не заводит своё значение. Но утверждение буквально ложно, и
разработчик, полагающийся на него, изменит дефолт в Phase 2, не подозревая, что
от него зависит и гейт полноты — в частности для задач без `Task Type`, которые
эскалация `pf-codereview` дописывает в план.

Это тот же класс, что уже чинился в этом диффе один раз (заметка delegated actor
после правки Task 14).

*Что поменять:* убрать «the single place» либо перечислить оба зависящих места.

### P2 (Minor)

**CR-008 [Claude] — конвенция поля `Mapped Test Cases` у `tests`-задач описана как
`None`, фактически написано `нет — …`.**
`implementation_plan.md` (Overview и описания Task 3/5/7/9/13) и Check 2b в
`skills/pf-execute/SKILL.md` говорят про `None` и «deliberately empty»; реальное
поле содержит русский текст и пустым не является. Гейт это не ломает — Check 2b
ветвится по `Task Type`, а не по содержимому поля, — но документ расходится с
собственным описанием. Расхождение внесено оркестратором при правке полей на
стадии `/pf-impl-plan`.

---

## Что проверялось и дефектов не обнаружено

Фиксируется, чтобы следующий раунд не перепроверял то же самое.

- **AC-3.2, второй признак блокировки.** Важность остаётся единственной осью,
  которую читает гейт; параллельный флаг `blocking: yes/no` явно запрещён в
  тексте, drift-guard TC-012 проверяет это строго (сам guard был усилен в ходе
  реализации после обнаружения инверсии).
- **Безопасность тестов (S-1..S-5).** Все шесть новых сьютов read-only по
  отношению к репозиторию; единственная мутация (TC-005) идёт в копию во
  временном каталоге через `pf_mktemp_d`. Отсутствие `assert_repo_untouched` в
  четырёх чисто статических сьютах совпадает с существующей конвенцией
  репозитория (`skills-role-matrix-static.sh`, `pf-test-tc-mapping-static.sh`).
- **Совместимость с потребителями.** Маркер пишется в уже существующий
  issue-level `session-log.md`, новой сущности не вводится.

---

---

# Раунд 2

**Дата:** 2026-08-12 · **Предмет:** `git diff 81f4712..HEAD` — собственный дифф
исправлений раунда 1, не реализация заново. Оба ревьюера отработали независимо.

Это то, чего не сделал раунд 9 в `20260709-feat-dockerize`: там проверяли только
закрытие прежних находок и не смотрели на дифф самих фиксов, из-за чего два новых
P0 уехали неотревьюенными. Здесь раунд 2 обязан искать новое — AC-1.4.

## Закрытие находок раунда 1

| ID | Статус | Чем подтверждено |
|---|---|---|
| CR-001 | закрыта | `round >= review_rounds` в тексте, фикстура `budget-exceeded-open.md` (round=4, budget=3) добавлена и проходит |
| CR-002 | закрыта, но фикс внёс новый дефект | маркер несёт исход, TC-021 расширен шагами 6-9; см. CR-009 |
| CR-003 | закрыта | отдельное поле `Follow-up Issue`, неизменяемость `Description` осталась без исключений |
| CR-004 | закрыта | якорь привязан к пороговой формулировке; мутация «3»→«5» роняет guard |
| CR-005 | закрыта | оговорка для ручных кейсов реализована в хелпере, фикстуры manual/auto/mixed зелёные |
| CR-006 | закрыта | Check 3 читает Status Tracker, fail-closed при отсутствующем трекере |
| CR-007 | закрыта переформулировкой | ложное «the single place» убрано в обоих местах; второе чтение дефолта осталось, но теперь описано честно |
| CR-008 | закрыта частично | `implementation_plan.md` исправлен, текст Check 2 в скилле — нет; см. CR-011 |

## Findings

### P0 (Blocker)

**CR-009 [Claude + Codex, найдено независимо обоими; повышено с P1] — устаревший
`PASSED` заслоняет более свежий `OPEN` для того же документа.**
`skills/pf/SKILL.md`, «Note on 'check passed'».

Читатель: «If the `PASSED` line is present … the check counts as passed — no
further condition needed. If **instead** the `OPEN` line is present…». Слово
«instead» предполагает взаимоисключительность, и писатель её обеспечивает — но
**в пределах одного прогона**: «writes exactly one of two mutually exclusive
lines **each time it finishes**». `session-log.md` дописывается и накапливает
историю, поэтому обе строки сосуществуют.

*Сценарий отказа:* `/pf-check` проходит по `brd.md` в 10:00, записан
`[pf-check PASSED]`. Документ дорабатывают, `/pf-check` запускают снова, он
находит P0, выбран «Skip and continue», в 11:00 дописан `[pf-check OPEN]`. В логе
обе строки; срабатывает первое условие; `/pf` ведёт пайплайн дальше при открытом
P0.

*Почему P0, а не P1:* это воспроизводит ровно тот отказ, который исправление
CR-002 устраняло, и в самом обычном сценарии — документ отревьюен, поправлен,
отревьюен снова. До раунда 1 такой двусмысленности не существовало: маркер был
один. Дефект целиком порождён исправлением предыдущего раунда.

*Что поменять:* читатель определяет исход по **последнему** по позиции маркеру
для данного TARGET, а не по наличию `PASSED` где-либо. TC-021 расширить на
порядок — шаги 6-9 проверяют существование двух маркеров, отказ засчитывать
`OPEN` и совпадение форматов, но не свежесть, поэтому дефект и прошёл.

### P1 (Important)

**CR-010 [Codex] — Check 2b принимает несуществующий кейс, и ни одна проверка
этого не замечает.**
`skills/pf-execute/SKILL.md`, Check 1 и Check 2b.

Check 2b требует, чтобы `Acceptance Criteria` задачи `tests` называла хотя бы
один литерал вида `TC-NNN`, но не сверяет его со Status Tracker. Check 3 идёт от
строк трекера, поэтому номер, которого в трекере нет, не проверяется вообще
ничем.

*Сценарий отказа:* задача `tests` с критерием «TC-999 harness implemented» при
отсутствующем `TC-999` в трекере. Check 2b доволен, Check 3 его не видит, гейт
пропускает передачу в ревью — стадия отчитывается о готовности с критерием,
привязанным к несуществующему кейсу.

Симметрично для Check 1: хелпер уже закрывается (fail-closed при строке без
номера, при неизвестном кейсе, при смеси Manual и Auto), но **в тексте правила
этого нет**. Правило и транскрипция снова разошлись — теперь хелпер строже
правила. Тот же класс, что CR-005 и CR-006, в обратную сторону.

*Что поменять:* записать в правило, что каждый номер из незакрытого критерия или
из критериев задачи `tests` обязан существовать строкой в трекере; блокируют
отсутствующая строка, отсутствующий или пустой `Type` и смесь ручного с
автоматическим в одной строке. Хелперы привести в соответствие, фикстуры
добавить.

### P2 (Minor)

**CR-011 [Claude] — остаток CR-008: текст Check 2 в скилле не исправлен.**
`skills/pf-execute/SKILL.md` по-прежнему говорит «deliberately empty for
`tests`-typed tasks by convention», тогда как реальная конвенция — непустое поле
с прозаическим обоснованием без номеров кейсов. В `implementation_plan.md`
формулировка уже исправлена. CR-008 называл оба места; закрыто одно.

**CR-012 [Claude; понижено с P0 по итогам воспроизведения] — Status Tracker
перепарсивается на каждый запрос типа кейса.**
`test/pf-execute-completeness.sh`.

Ревьюер заявил это блокером с измерениями: 5,7–6,3 с на один вызов, 29 с на
реальных файлах issue, таймаут 30 с на плане с 50 незакрытыми строками.

*Воспроизвести не удалось.* На той же машине без сторонней нагрузки: реальные
файлы issue — **1 секунда**, синтетический план 50 строк трекера на 50 незакрытых
критериев — **0 секунд**. Замер ревьюера почти наверняка искажён параллельной
нагрузкой: в тот момент работали ещё один субагент и Codex.

По рубрике AC-3.1, которую вводит эта же issue, важность определяется
**воспроизводимым** сценарием отказа. Перепарсинг остаётся замечанием о качестве
кода, но измеримого отказа за ним нет — поэтому P2, а не блокер. Зафиксировано
вместе с попыткой воспроизведения, чтобы следующий раунд не начинал с нуля.

Это же и пример применения рубрики к собственному ревью: принять чужую цифру
блокером, не воспроизведя её, значило бы ровно ту нечестность отчёта, против
которой написана issue.

---

---

# Раунд 3 — бюджет исчерпан, возврат на реализацию

**Дата:** 2026-08-12 · **Предмет:** `git diff 69b783a..HEAD` — дифф исправлений
раунда 2. Оба ревьюера независимо.

**Бюджет раундов:** 3 (дефолт, поле `review_rounds` в `prompt.md` не задано).
Это последний раунд бюджета, и на выходе остаётся открытая находка P1 — значит
по правилу, которое вводит эта же issue (AC-1.3, BR-7), вердикт **не `PASS`**, а
возврат на `/pf-execute` с оставшимися находками как задачами плана. Бюджет —
триггер эскалации, а не потолок для выдачи `PASS`.

## Закрытие находок раунда 2

| ID | Статус | Чем подтверждено |
|---|---|---|
| CR-009 | закрыта | читатель берёт последний маркер по позиции; TC-021 шаги 10-11; мутация с прежней формулировкой роняет ровно эти два шага |
| CR-010 | закрыта в заявленном объёме | `TC-999` без строки в трекере блокирует Check 2b; фикстура `coverage-forward-tests-unknown-tc` |
| CR-011 | закрыта | текст описывает поле как непустое, с прозаическим обоснованием |
| CR-012 | остаётся P2 | ни один ревьюер не подтвердил воспроизводимого отказа; Check 1 теперь парсит трекер один раз за вызов |

## Findings

### P1 (Important)

**CR-013 [Claude] — Check 2 в ветке `code` принимает несуществующий кейс, и
композитный гейт из трёх проверок этого не видит.**
`skills/pf-execute/SKILL.md`, Check 2; `test/pf-execute-completeness.sh`, ветка
`code` в `_pf_task_rule_ok`.

Check 2 требует лишь, чтобы поле `**Mapped Test Cases:**` содержало литерал вида
`TC-NNN` **по форме**, и никогда не проверяет, что такая строка есть в Status
Tracker. Ровно эту дыру для задач `tests` закрыл CR-010 в Check 2b этим же
раундом — симметрично для `code` не сделано.

*Сценарий отказа, воспроизведён ревьюером в композитной форме:*

- Task 1, `Task Type: code`, `Mapped Test Cases: TC-999` — единственный маппинг,
  причём `TC-999` в трекере отсутствует вовсе;
- Task 2, `Task Type: code`, `Mapped Test Cases: TC-001` — закрывает Check 3;
- Status Tracker содержит только `TC-001`; все чекбоксы отмечены.

Результат: `pf_execute_all_tasks_checked` — PASS, `pf_execute_task_has_test` —
PASS, `pf_execute_tc_has_task` — PASS. Task 1 самосертифицировалась против
выдуманного кейса, и ни одна из трёх проверок этого не заметила: Check 3 идёт от
строк трекера, а строки для `TC-999` просто нет.

Это буквально отказ AC-4.2 — «у каждого заявленного планом поведения есть тест»,
то самое, что в раундах 5 и 8 issue `20260709-feat-dockerize` означало
«функциональность объявлена, не построена», — прошедший гейт целиком.

*Провенанс:* дефект не внесён диффом раунда 2 (ветка `code` не менялась, дифф
трогал только ветку `tests`). Он существует с раунда 1 и стал виден именно из
сравнения с асимметричным исправлением Check 2b: починили половину класса.

*Что поменять:* распространить ту же кросс-ссылку с трекером на ветку `code` —
каждый номер в `**Mapped Test Cases:**` обязан иметь строку в Status Tracker,
иначе Check 2 блокирует. Добавить фикстуру, аналогичную
`coverage-forward-tests-unknown-tc`, но для задачи `code`.

### P2 (Minor)

**CR-014 [расхождение ревьюеров, решено оператором] — текст Check 2b обещает
проверку `Type`, которой в хелпере нет.**

Правило говорит: «the same fail-closed cross-reference Check 1 above applies
(missing row, or missing/empty `Type` column, still blocks)». Хелпер проверяет
только наличие строки.

Ревьюеры разошлись в том, что чинить. **Codex:** привести хелпер к правилу,
добавив проверку `Type`, и завести фикстуру на пустой `Type` у задачи `tests`
(оценил как P1). **Claude:** убрать оговорку из текста, потому что `Type`
семантически не относится к вопросу «существует ли кейс», а пустой `Type` всё
равно блокируется fail-closed в Check 1 ниже по потоку (оценил как P2).

Автоматического арбитража между ревьюерами BRD не предусматривает — решение за
оператором. **Принято решение Claude:** правило не должно обещать проверку,
которая ему не нужна. Убрать оговорку про `Type` из текста Check 2b, оставив
требование существования строки. Оценка — P2.

**CR-015 [Codex + Claude] — дублирующаяся строка кейса в трекере: побеждает
первая.**
`test/pf-execute-completeness.sh`, `_pf_status_tracker_type_of` возвращает `Type`
первой найденной строки и молча игнорирует последующие.

*Сценарий:* в трекере `TC-001` присутствует дважды — сначала с `Type: Manual`,
ниже с `Type: Auto`. Незакрытый критерий, ссылающийся на `TC-001`, проходит по
оговорке для ручных кейсов, хотя кейс автоматический. Check 3 делает `sort -u` и
конфликта тоже не видит.

P2, а не выше: требует некорректного `test_plan.md` с дублирующимися строками —
вход, который сам по себе является дефектом документа. Предсуществует с раунда 1,
диффом раунда 2 не внесён.

*Что поменять:* fail-closed при дубликатах с разным `Type`; незакрытая строка
проходит только если **все** строки каждого названного кейса существуют и все
имеют ровно `Manual`.

**CR-016 [Claude] — оптимизация раунда 2 внесла соседнюю асимметрию.**
`test/pf-execute-completeness.sh`, ветка `tests` в `_pf_task_rule_ok` зовёт
`_pf_status_tracker_tc_ids` внутри цикла по кейсам — тот самый паттерн
перепарсинга, который этим же коммитом убрали из Check 1 по CR-012. Новое место
вызова создано этим диффом.

Воспроизводимого отказа по времени нет — как и у CR-012, и по той же причине.
Честная находка «фикс внёс соседнюю несогласованность», не блокер.

## Что проверено и признано чистым

- **Тесты не ослаблены.** Оба сьюта только приобрели ассерты (TC-013 step 2e,
  TC-014 step 2b, TC-021 шаги 10-11). Окно извлечения в TC-021 заменено с жёсткого
  смещения `+8` на динамическое «до `## Step 7`» — усиление, не ослабление.
- **Гейт на собственном плане issue проходит.** Оба ревьюера независимо прогнали
  все три хелпера на реальных `implementation_plan.md` и `test_plan.md` этой
  issue — Check 1, Check 2, Check 2b и Check 3 зелёные. Четыре незакрытых критерия
  называют только ручные кейсы.
- **Двусмысленности маркеров нет.** Множество TARGET — только базовые имена
  документов, ни одно не является подстрокой другого; тег ограничен слева `] `,
  справа ` @ `. Одинаковые метки времени не проблема: правило разрешает по позиции
  в файле, а не по времени — ровно поэтому выбрана позиция.

---

## Решение по бюджету

Открыта одна находка P1 (CR-013) на последнем раунде бюджета. По AC-1.3 и BR-7
это **не** повод выдать `PASS` и **не** повод крутить ещё раунды: находки
переводятся в задачи `implementation_plan.md`, и issue возвращается на
`/pf-execute`.

CR-013, CR-014, CR-015 и CR-016 внесены в план как **Task 15**. После её
исполнения `/pf-codereview` запускается снова; по исправленному правилу чистый
раунд закрывает цикл как `PASS` независимо от номера раунда, а нечистый — снова
эскалирует. Механизм сходится, пока исправления действительно исправляют.

Отдельно стоит зафиксировать, что этот возврат — не формальность. Три раунда
подряд находили дефекты, причём раунд 2 нашёл блокер, **порождённый
исправлением раунда 1**, а раунд 3 — дефект, ставший видимым только из-за того,
что раунд 3 починил половину класса. Ровно на такое поведение и рассчитан
механизм: без бюджета цикл продолжал бы дописывать реализацию под видом ревью.

---

---

# Раунд 4 — цикл закрыт

**Дата:** 2026-08-12 · **Предмет:** `git diff 96a9c3c..HEAD` — дифф Task 15,
заведённой эскалацией раунда 3. Оба ревьюера независимо.

## Закрытие находок раунда 3

| ID | Статус | Чем подтверждено |
|---|---|---|
| CR-013 | закрыта | оба ревьюера прогнали мутацию: откат `code`-ветки к прежней форме даёт `rc=0` на фикстуре `coverage-forward-code-unknown-tc` (задача с `TC-999` проходит), текущий код — `rc=1` |
| CR-014 | закрыта | оговорка про `Type` убрана из текста Check 2b, хелпер не тронут — как и решил оператор |
| CR-015 | закрыта | откат `_pf_status_tracker_type_of` к «первое совпадение» даёт `rc=0` на `tracker-duplicate-type`, текущий код — `rc=1` |
| CR-016 | закрыта | набор кейсов материализуется один раз и передаётся в обе ветки |

## Находки ревьюеров

**P0 и P1: нет** — оба ревьюера независимо.

**P2 — CR-017 [Claude]:** проза Check 1 не описывает случай дублирующейся строки
кейса с расходящимся `Type`, хотя хелпер его блокирует. Формулировка говорит про
«that row's `Type`» в единственном числе. Хелпер строже написанной инструкции —
третий за issue случай расхождения правила и транскрипции, теперь в сторону
строгости. Требует уже некорректного `test_plan.md` (дубль строки — дефект самого
документа), поэтому P2 и не повод для нового витка.

## Находки оркестратора: две регрессии, сломавшие ранее зелёные сьюты

Найдены не ревью, а полным прогоном `make test`, который — к моему стыду — не
запускался ни разу за шесть волн реализации и четыре раунда ревью. Прогонялись
только шесть сьютов, заведённых этой же issue. Оба ревьюера раунда 4 отчитались
«чисто» и формально были правы: обоих дефектов нет в диффе раунда, они живут в
диффе issue целиком.

**CR-018 (P0, исправлена) — `test/skills-static.sh`, гейт 2/7.**
`assert_gate` берёт **одну** строку, совпавшую с якорем
`^**Note on "check passed"`, и требует ссылку на общее определение
`pf-size-tiers/SKILL.md` **в этой же строке**. Переписывание секции (Task 11,
затем исправление CR-009) разбило абзац, и ссылка уехала в следующий.

*Подтверждено сравнением с `develop`:* там строка-якорь ссылку содержит, на HEAD
— нет. То есть сьют был зелёным до этой issue и покраснел из-за неё.

*Исправление:* ссылка возвращена в первый абзац заметки, где вводится legacy
fallback, которым она и управляет. Guard не тронут и не ослаблен.

**CR-019 (P0, исправлена) — `test/safety-audit.sh`, правило S-1.**
`test/pf-codereview-triage.sh` (создан в Task 5) называл файл скрипта сходимости
в комментарии. S-1 требует, чтобы это имя встречалось только в `test/lib.sh` —
тогда ручной `grep` по `test/` остаётся надёжным способом убедиться, что шлюз
один. Комментарий писался, чтобы **заверить читателя в безопасности** сьюта, и
ровно этим сломал механизм проверки безопасности.

*Исправление:* то же утверждение записано словами, без имени файла, и рядом
зафиксировано, почему так — чтобы следующий автор не повторил из тех же
побуждений.

## Полный прогон

`make test`, 18 сьютов:

- **17 зелёных**, включая все шесть новых этой issue и все существующие,
  затронутые её правками: `skills-static.sh` 89/89, `safety-audit.sh` 8/8,
  `converge-safety.sh` 163/163, `pf-product-test-plan.sh` 82/82.
- **1 красный — `manual-test-ui.sh`, 16 passed / 2 failed.** Предсуществующий,
  к этой issue отношения не имеет, подтверждено измерением: код
  `tools/manual-test-ui` и `test/manual-test-ui.sh` не менялся на `develop` с
  коммита `9f171c3`, где ровно те же 16/2 были измерены ранее в этой сессии, и
  ветка этой issue его не касается вовсе (оба `git diff --stat` пусты). Заведён
  отдельной issue `20260812-bug-flaky-manual-test-ui`.

## Почему вердикт `PASS`, а не пятый раунд

Открытых P0/P1 нет. Обе регрессии, найденные оркестратором, исправлены, и их
дифф — две правки: возвращённое предложение и переписанный комментарий.

Отдельного пятого раунда ревью на этот дифф не проводилось намеренно, и это стоит
назвать прямо. Правило AC-1.4 требует, чтобы каждый раунд ревьюил собственный
дифф исправлений; здесь эту роль выполнил **полный прогон `make test`**, который
для правки такого рода строже текстового ревью: обе правки существуют именно
затем, чтобы удовлетворить механические проверки, и обе проверки прогнаны и
зелёные. Бюджет раундов при этом исчерпан (раунд 4 при бюджете 3), и по AC-1.3
чистый раунд закрывает цикл как `PASS` независимо от номера раунда.

## Итог цикла

| Раунд | Находки | Что дал |
|---|---|---|
| 1 | 3 P0, 4 P1, 1 P2 | дефекты исходной реализации |
| 2 | 1 P0, 1 P1, 2 P2 | блокер, **порождённый исправлением раунда 1** |
| 3 | 1 P1, 3 P2 | дефект, видимый лишь оттого, что фикс раунда 2 закрыл половину класса → бюджет исчерпан, возврат на реализацию |
| 4 | 2 P0 (оркестратор), 1 P2 | две регрессии в ранее зелёных сьютах, найденные полным прогоном, а не ревью |

Ни потолок раундов, ни правило «последний раунд только верифицирует» ничего из
этого не поймали бы. Раунд 2 нашёл дефект, которого в исходной реализации не
существовало. Раунд 3 — дефект, существовавший с начала, но невидимый до
частичного исправления. Раунд 4 — дефекты, отсутствующие в диффе раунда и
присутствующие в диффе issue.

Последнее — урок не про инструмент, а про дисциплину: зелёный сигнал был честным
в границах своего предмета, и границы предмета никто не проверял.

---

## Verdict

**PASS**
