# LLM Usage — 20260812-bug-flaky-manual-test-ui

Окно замера: `2026-08-12T11:00:31+03:00` (первый коммит, затронувший папку issue) → закрытие
2026-08-13. Источник — локальные транскрипты Claude Code в
`~/.claude/projects/D--dev-planning-framework/` (12 файлов, 641 ответ ассистента,
дедуплицировано по `message.id`).

| Source | Model | Input tok | Output tok | Cache create | Cache read | Approx cost | Notes |
|---|---|---|---|---|---|---|---|
| auto | `claude-opus-5` | 2 091 | 682 684 | 2 522 660 | 336 958 901 | unavailable — pricing table not found | 634 ответа; оркестрация и все стадии пайплайна |
| auto | `<synthetic>` | 0 | 0 | 0 | 0 | — | 7 служебных записей без расхода |
| manual | — | — | — | — | — | — | `usage.md` отсутствует: расход не-Claude моделей не логировался |

**Total approx cost:** не определена.

Стоимость намеренно не рассчитана: скилл `claude-api` с актуальными ставками $/Mtok в этой
сессии недоступен, а подставлять цифры по памяти означало бы выдать догадку за замер. Токены
выше — реальные, из транскриптов.

## Оговорки, без которых числа читаются неверно

1. **Окно захватывает всю активность Claude Code в этом каталоге проекта**, а не только эту
   issue. В окно попала также работа по закрытию `20260806-improve-codereview-convergence` и
   правки дефектов `/pf-qa`. Цифры — верхняя граница, не атрибуция.
2. **Расход субагентов сюда не входит.** Все 634 ответа — `claude-opus-5`, то есть
   оркестратор. За время issue было запущено семь субагентов на `claude-sonnet-5` (ревью
   плана, три волны реализации, ревью кода) — их токены логируются отдельно и в этой таблице
   не отражены. Реальный суммарный расход выше приведённого.
3. **Вызовы Codex не учтены** — они идут через отдельный CLI, а не через транскрипты Claude
   Code. За issue их было два: ревью `implementation_plan.md` и ревью диффа.
