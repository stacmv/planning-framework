# Feature: dark mode

Add a dark theme toggle to the settings page.

This issue is CLOSED. Converge must not edit its contents — in particular it
must NOT add a `doc_language` frontmatter here (D-D). The only file converge is
allowed to add to a closed issue is the `brd.md` pointer (Р1).
