# Implementation Plan

> Hyphenated name in a CLOSED issue. T10 covers `open/` **and** `closed/`:
> converge renames this to `implementation_plan.md` too. Renaming is a
> structural normalisation, not a content edit, so it does not violate Р1.

## Tasks

- [x] 1. Define the CSS custom properties
- [x] 2. Build the toggle component
- [x] 3. Persist the preference
