---
doc_language: Russian
size_tier: small
---

Минимальный issue-fixture для тестов `/pf-close` (parent-branch detection и usage-window). Ветка, upstream и дополнительные коммиты донастраиваются каждым тест-кейсом поверх копии фикстуры — в самой фикстуре только этот файл, без `.git`.
