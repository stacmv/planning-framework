---
doc_language: Russian
---

# Feature: bulk import

The frontmatter above is valid and already declares doc_language. Converge must
not overwrite the value, must not add a tier key, and must leave the rest of
this file byte-for-byte unchanged (D-G, TC-058 step 4).

Note that the prose here is Latin-heavy — a content-based detector would say
English. The existing declaration wins anyway.
