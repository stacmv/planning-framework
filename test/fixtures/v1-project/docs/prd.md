# Product Requirements Document

**Version:** 1.0

## Goal

Ship the widget pipeline.

## Requirements

1. Widgets can be created.
2. Widgets can be listed.
3. Widgets can be archived.

This file is a v1 artifact. It is NOT on converge's deletion whitelist (which
consists entirely of `planning/…` paths), so it must survive converge untouched.
