# Improve: cache the widget index  (v3 copy, OPEN)

The v3 layout is authoritative in BOTH directions: this issue was re-opened, so
the v2 "closed" copies must be merged into `docs/issues/open/…` and
`docs/issues/closed/20250103-improve-gamma/` must never be created
(TC-051, steps 4-5).
