# Implementation Plan  (v3 copy, UNDERSCORED name — DIFFERS)

This is the authoritative plan. It must survive converge byte-for-byte
(TC-053, step 5).

## Tasks

- [ ] 1. Replace `Math.floor` with `Math.ceil`
- [ ] 2. Add a regression test for `total % size === 0`
