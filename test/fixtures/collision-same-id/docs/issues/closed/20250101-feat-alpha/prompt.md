# Feature: CSV export  (v3 copy, CLOSED)

The v3 layout is authoritative: this issue is closed. Converge must merge the
v2 copies INTO `docs/issues/closed/20250101-feat-alpha/` and must never create
`docs/issues/open/20250101-feat-alpha/` — otherwise `/pf` would resurrect
finished work (TC-051, step 2).
